# Autonomous resume

Updated: 2026-08-14T23:27:03Z

- Active authority: user-authorized `planning/MOUNTED-COMBAT-PHASE-2-MASTER-MISSION.md`, exact SHA-256 `d4e7340954af34fb011434ec5952e893a2b109769f7941bd6e52c45f455b541e`. Continue through Phase 2A and stop only at the exact-build manual visual checkpoint. Combat remains forbidden until explicit acceptance.
- Repository: `C:\Dev\KingmakerMountedCombatLab\repo\KingmakerMountedCombat`; origin `https://github.com/howardreith/KingmakerMountedCombat.git`; branch `codex/mounted-combat-phase2-alpha`; current committed HEAD `92bb93f165fb9bb3a8c3417589c62759f9089573` (`feat: add transient mounted player action`), already guarded-published with exact remote equality. Branch ancestry from exact main `72dcbeb19d03985509f1ed71d3550dfb74f0ac15` passed.
- Active version: `0.1.0-phase2a-dev.1`, assembly `0.1.0.0`. Frozen Phase 1 package/version/evidence remain unchanged and must not be overwritten or relabeled.
- Current worktree: intentional one-time offline-cloud bootstrap guard changes in `RuntimeHarness.Common.ps1`, `Invoke-KingmakerRuntimeScenario.ps1`, and `Test-Harness.ps1`, plus this durable record. No unrelated user change or external payload is present. Combat source does not exist.
- Tranche A implementation: transient `HideAndDontSave` Mount/Dismount overlay; pure eligibility evaluator; exact owned-Mammoth/current-size/life/control/polymorph/loading/cutscene/combat/mode/view/agent feedback; selection normalization; fail-closed cleanup; ordered 256-record native lifecycle ledger; exact save/load prefix and global EventBus/UMM boundary instrumentation.
- Exact local native contracts mapped: `IUnitFinallyDeadHandler`, `IAreaLoadingStagesHandler`, `IUnitViewAttachedUIHandler`, `ISceneHandler`, existing combat/mode/party/life/in-game handlers, `SaveManager.SaveRoutine` `06008029`, and `LoadRoutine` `0600802C`. Runtime delivery remains unclaimed.
- Latest complete gates: source `21/0`; Release build PASS; component `122/0`; visual capture `12/0`; guarded harness/protocol `136/0`; assembly-backed `69/0` (`58/0` Kingmaker, `11/0` Wrath). DLL SHA-256 `6e4a2d9b75f2e6a485b3f4da0234243b33c797df92191872724b393f912a784d`, MVID `95e1f8fc-5aa3-4338-8921-bf5dff209d76`. The clean `92bb93f` development package is `C:\Dev\KingmakerMountedCombatLab\artifacts\KingmakerMountedCombat-0.1.0-phase2a-dev.1-diagnostic.zip`, SHA-256 `3873d5ff91d40b6d1e985f7f4f88b9de6175381e1c86e9673d6417f62255c874`, validation `10/0`; it must be regenerated after the bootstrap commit.
- Protected authority remains exact: epoch `20260814T1445257441387Z-user-fixture-preparation`, authority SHA-256 `77fd415d177423dd7da58e3685e418f38cf954dead75068424a717e2be6b3ca9`, protected digest `973f203e16aad29d7e4865bae6f81149d959a4ac179953c87781a06d1c71898d`; Baseline immutable; only exact Working writable; Auto/Quick protected.
- Runtime admission state: both new scenario `-WhatIf` passes were pure. Two actual attempts stopped before staging: first because Steam was absent; second because a freshly started `-offline -silent` Steam session had not yet emitted a current-session App `640820` `offlineMode=true` cloud line. Neither attempt created evidence, a lock, a transaction, a Kingmaker process, or external mutation. Steam PID `11644` is currently hidden and logged off; no Kingmaker process or suspicious window is present. Historical final App `640820` cloud evidence is exact `offlineMode=true` at `2026-08-14T11:25:09-04:00`, while the current session began at `2026-08-14T19:18:04-04:00` and has no current cloud-mode line.
- Bootstrap safety design: explicit `-BootstrapOfflineCloudEvidence` is rejected for every save-backed scenario. It permits only a no-save `mod-load-smoke` preflight when the current connection is logged off, no suspicious UI exists, no current cloud-mode state is present, and the historical final state is exact `offlineMode=true`. Any observed current or historical `offlineMode=false` rejects. The ordinary strict postflight is unchanged and must observe a new current-session `offlineMode=true`; otherwise the smoke run fails and restores. Deterministic coverage is `6/0` assertions across two harness cases within the `136/0` suite.
- Current claim status: Tranche A offline implementation is `IN PROGRESS`; both new live rows and native persistence/lifecycle rows are unrun. Presentation remains inherited only as `MECHANICALLY VIABLE, NEW ANIMATION/POSE WORK REQUIRED`. No Phase 2 runtime, uninstall, UI delivery, or visual PASS is claimed.
- Exact next action: commit and guarded-publish the offline-cloud bootstrap guard, regenerate/validate the exact clean package, pass bootstrap `mod-load-smoke -WhatIf`, and execute the one no-save smoke transaction. Continue only if strict postflight creates current-session offline-cloud proof and restores exact Mods/external state; then rerun the two save-backed scenario gates and A/B rows with every protected continuity pin.
