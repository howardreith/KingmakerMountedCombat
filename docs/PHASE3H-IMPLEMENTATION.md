# Phase 3H ordinary combat loop

Status: IN PROGRESS. Separate native turns; unified turn, paired scheduler and diagnostic overlay remain false. Ordinary native full attacks supersede the previous single-only ordinary contract. Explicit Primaries remain single attacks. One active mounted pair remains the boundary.

Runtime 4/8 (`20260906-phase3h-preview4-rt-A`, source `5b25b96ef232ae939fcf31e60886d2c78b987633`): FAIL before attack cases. Native Mount was refused with the actual reason "Move next to your companion before mounting"; mode Default, unpaused, UMM closed, no cooldown. This rules out the still-open UMM explanation for that refusal. The fixture had no adjacency precondition after its unmounted Horse movement. Preview.5 establishes adjacency using one normal unmounted native movement before Mount; this is fixture placement, not a PASS for out-of-range Mount or mounted attack approach. Out-of-range Mount remains a separate failed observation.

Independent restoration at 06:56:08 UTC again matches full intake saves/Mods; no game/locks. Preserved log SHA `ef9d36af0dfeafcf77f5bf081cdb6729d9deb13e202583a8d2a79eda0dbc0e11`. Preview.5 also appends bounded native partial-ground-movement observations across actor turns and native round-start identities. The evidence protocol explicitly rejects claiming these partial observations as complete resource qualification. Full double-move/exhaustion/conversion and both initiative orders remain required.

Runtime 3/8 (`20260906-phase3h-preview3-tb-A`, source `9b62e96782112ec71d3cb2703a54da06997355ec`): native Mount succeeded, ordinary rider longbow completed one owned native rule/effect and charged Standard 0->6. Rapid Shot was enabled through the real feat/toggle, but the native plan was not full and Move remained 0. This is a failed full-round row, not a full-attack PASS. The fixture then timed out expecting a second shot. Preview.4 retains that failure promptly and continues the independent cases. Exact native `UnitAttack.InitAttacks` consults current turn `EnabledFullAttack` (native smart cursor mode), movement restrictions and Staggered; scripted OnClick alone omitted hover prediction. The fixture now delivers native hover and records those predicates, with no forced full attack or cooldown write.

Preview.3 final-render observations across eight frames put pelvis root-X at approximately -0.1892, Chest centerline X=0, and anatomical rider-right at positive root-X. Preview.4 adds a named +0.19 visual root-X correction. Y remains the requested cumulative -0.16; Z remains -0.18. Frame 006 was viewed with the pair visible indoors; final centering across headings and Bite remains to be seen. Numeric animated-seat residuals below 1e-6 are projection observations, not animation certification.

Run 3 independently restored exact intake saves/Mods at 06:39:42 UTC, no game/locks; preserved log SHA `90d829bcbf2f01981607b049858ddd7f94f5f93d7e80d4f3e1b205607fbe69e1`. Automatic review rejected the thin launch wrapper before mutation; invoking the approved guard directly with the same exact pins was approved. Future runs use that direct approved entry point.

Runtime 1/8: `20260906-phase3h-preview1-tb-A`, source `a7b312ae40ac5619de61bc1c7688f50708253c86`, ZIP `609f7dd7e65b4fcaad1b323feefd35be7113a6edb6432cba26c4cc5f769a7ce8`, DLL `7c252c98f959d933ea9cb9936b364794bd1fb15b0489d32f6b4af0f74f59715c`, MVID `3f615bc5-f4de-4012-945e-e06644ac7ff5`. FAIL before attack submission: the Rapid Shot fixture's `Single` lookup found no matching element. There is no attack/full-round outcome from this run. Preview.2 resolves the native feat by its exact name/display name and obtains its toggle from its actual AddFacts references, retaining a failed fixture row while allowing independent cases to continue. The desktop capture availability probe failed with native pipe error 2; physical pointer and UI-chrome evidence remain unavailable.

Independent restoration at `2026-09-06T05:48:02.6372821Z` PASS: full current saves `bddad0065064ebc166c8414e660b938fe9be2e4894c3b9916cdd972c8bf05913`, Mods including preview.7/cache `0e45b19883f5405a076df6a92ef5175282817516aafdc6f343dbbbcf2e10e835`, no game/locks. Test log separately preserved with SHA `ff75af53e368699cda13e8e1156b36fbe66e34e6ef2c161cbc25cbec7a00a86d`. Gameplay interpretation followed this audit. No permanent deployment.

## Intake and bounded implementation plan

Runtime 2/8: `20260906-phase3h-preview2-rt-A`, source `dad1e41`, ZIP `5488e170d84d0a0cda7e41991ab9a8ee7784a1574d570c08d941397073ca43fc`, DLL `7c3414876f20f78fae4e3c4adf6b858fe264d664c796e4c333e875f2a80338a0`, MVID `58ee6e79-8949-4909-ab6c-4202a5399732`. FAIL: native Mount dispatch refused before the attack tranche, then the outer 300-second deadline fired. No full attack or approach was attempted. Host artifact reconciliation also rejected the incomplete registration mapping; no gate was waived. The old fixture's UMM-close lease and 25-second Mount deadline were restricted to Phase 3G names and omitted the new Phase 3H names. Preview.3 extends those exact existing scopes and records the native refusal text. The omission is confirmed; causality of Mount refusal remains unresolved until the next trace. Independent restoration at 06:09:55 UTC PASS with the exact intake digests, no game/locks; separately preserved log SHA `692f7c0fddc2263c383c588802c4b47ab3cdd6b9d80d0e9ccd7457a1b3f9a154`.

The actor-local movement record now also observes native `HandleUnitCommandDidEnd` (`0x06000C5E`) after it rounds partial Move/step expenditure. Native fields and callbacks remain the authority; this closes a stale-observation boundary but does not qualify refresh timing or the two-round matrix.

Started 2026-09-06 04:37 UTC on clean `codex/mounted-combat-phase3f-playable-core`, local/upstream/host remote `1b8469845ef32406ca5006290a7b86eeea99b726`. Actual preview.7 source `b8c206331f34a1aa873bfef86a37ab1066c267fc` remains installed with its human-created `.49723.cache`. No game was running. Current saves and foreign settings differ legitimately from installation-time snapshots and are the new preservation baseline.

The seven packet payloads at `handoffs/phase3h-20260906` match their manifest. All three screenshots were inspected; original bytes remain outside Git. The current stable log is preserved read-only at `analysis-cache/runtime-evidence/phase3h-intake-20260906T0444297198055Z/output_log.txt`, SHA-256 `7a839a4bd1ff0a034dce49239d0efdbe5edbcb3fdf90266461e8f8a2ca4d789d`, source last write 04:15:24 UTC. Startup and exception MVID identify preview.7. `intake.json` records full current saves/Mods inventories. Its PowerShell-drive free-space field was unavailable and serialized as zero; independent `System.IO.DriveInfo` reports 254,833,655,808 bytes free.

Human-reported: Horse TB approach and visible Bite fail; manual adjacent Horse attack and rider longbow Standard attack work; ordinary RT melee approach regressed; another rider-turn movement remains possible but its legality needs measurement. Seat improved but needs another 0.08 m down and anatomical centering. Countdown uses question marks. Dismount screenshot shows literal null metadata and automatic-use advice. Saddle aesthetics are explicitly deferred. These are human observations, not a new automated qualification ledger; prior pause, cancellation, party and stationary attack successes remain regression baselines.

1. Repair approach ownership and native/manual driver selection with red regressions. Preserve exact pair/provenance/foreign command guards and native distance/LoS. Reproduce the rider ordinary path independently.
2. Replace partial movement projection with an actor-owned native state adapter at verified refresh boundaries; separate native ordinary attack sequences from single Primary costs. Verify native plans and two turn orders without borrowing rider movement or Standard.
3. Repair the demonstrated Horse animation boundary, named visual seating and narrow native UI metadata; run integrated negative controls, final RT/TB checks and the applicable release gate. Build a distinct clean-source private artifact and publish through the host helper.

At most eight new guarded runtime transactions (zero used at intake), reserving final RT/TB. Each restores the exact current preview.7/cache/settings/saves. No permanent Phase 3H replacement is authorized. Every runtime outcome will identify exact source/package/config and evidence; unresolved movement, visual or pointer gates remain explicit.

## Confirmed first boundaries

The human log repeatedly reaches `MountedPairAttackCommand.BeginDelegatedMove`, then rejects the same mount-owned Standard wrapper because `mount.Commands.Empty` is false (lines 1004 onward). No child is started. The exact native `UnitCommands.Run` also pairs Standard and Move for interruption; admitting a Move must explicitly preserve only its compatible owned parent, and a started wrapper must permit locomotion while approaching. TB manual approach driving must be restricted to rider-current/off-executor movement.

Ordinary attacks currently use a Standard wrapper with an ignored-cooldown `IsSingleAttack` child. Full attacks require one native sequence and one authoritative charge, including eligibility after approach. Merely relaxing the child count is insufficient. Current partial prepayment and two-float projection do not establish complete movement ownership.

## Qualification

Native sequence checkpoint (preview.1): the pair command now inherits the native `UnitAttack` geometry adapter and owns its animation, plan and sole native controller charge. The historical ChildAttack compatibility property refers to that same command. Ordinary mode permits the native full plan; explicit Primary sets single mode. Native `OnStart` replans after legal approach; the attack clock begins there. Recovery waits only after the complete native plan, not the first rule. Exact-target continuation preserves native rule/effect code while preventing a cancelled/dead target from selecting a different enemy. The source contracts that assumed a free detached child are superseded. Full-plan recovery red `342/1`, current components `345/0`; source `22/0`; new protocol mutation checks `20/0`. These counts are not runtime evidence.

The movement adapter removes rider cooldown/time projection entirely. Mount-native ticks retain their actual time, forced movement and step state; delegated ticks use the verified native movement formula with mount speed/restrictions and mount expenditure. Records survive dismount and reselection. **Refresh reconciliation remains unqualified**: early rider-first refresh would change native cooldown values also used by turn timing; this candidate does not zero them early. Native Prepare reconciliation is still after native callbacks, so callback ordering and subsequent rounds remain release blockers until measured. Step opportunity eligibility and the complete mode/remount matrix also need qualification. No TB-complete claim is made.

Presentation/UI: Horse animated correction Y is now -0.16 m relative to pre-3G, another -0.08 m from preview.7; Z and gameplay/root offsets are unchanged. Anatomical lateral calibration awaits in-engine measurements. Native `AbilityData.IsSuitableForAutoUse` (`0x06002B30`) admits Personal abilities; the exact Mount/Dismount controls now opt out without changing other auto-use. Explicit empty localized duration/saving-throw values replace unset metadata. Saddle art is unchanged by request. The direct native attack type also exposes native attack command identity to UI/animation providers; actual rendering remains to be checked.

Host approved only `phase3h-combat-loop-rt` / `phase3h-combat-loop-tb` in the deployment scenario parameter. Guard SHA-256 is `83ef44c9118a15657a7cf308061bf9259028e1407ce8ead4d0f749af9e14026c`; immutable original is in intake evidence. The allowance-patch WhatIf attempt aborted because PowerShell propagated WhatIf into Get-FileHash. The actual approved patch retained exact old-hash/unique-text checks and changed only those two names; this is not a runtime WhatIf PASS. Every live runtime transaction still requires the helper's internal WhatIf and independent restoration. Runtime budget remains 0/8.

First approach checkpoint: the added ownership regression failed against the old empty-container behavior (`341 PASS / 1 FAIL`) and passes after the repair (`342 / 0`). Release/source checks pass (`22 / 0`). The exact private native paired-interruption overload is verified at `0x060026BF`; its thin prefix consults only the active command's exception-safe admission scope. Queue, previous command, group and foreign raw slots are never cleared. The wrapper permits movement only in Approaching, and manual delegated ticks now require rider-current TB; native RT and mount-current TB retain their driver. This is an implemented correction with deterministic/native-member evidence, not yet a runtime PASS.

Phase 3H gameplay, full attacks, resource reconciliation, visible animation, seating and UI: NOT RUN. Historical Phase 3G evidence retains its original build and failures. No HUMAN PASS is claimed.
# Phase 3H current checkpoint

The exact installed `OvertipAction.UpdateCommandIcon` selects its native attack sprite when the Standard command `is UnitAttack`. The new owned sequence meets that contract without changing the global unknown-command fallback. Actual action-bar/countdown presentation still requires runtime inspection.
