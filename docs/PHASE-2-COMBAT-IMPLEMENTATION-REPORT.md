# Phase 2 combat implementation report

Status: IN PROGRESS

## Core combat-control runtime finalization (2026-08-21)

The exact core-control implementation is now repeatably qualified. Fresh same-package/suite processes `20260821T125000Z-core-combat-controls-launcher-observation-passA` and `20260821T125800Z-core-combat-controls-launcher-observation-passB` each complete launcher and game `80/0`, covering invalid targets, exact pre-child target death, cleanup, and non-mounted controls. Combined credit is `8/8` subscenarios and `160/0` assertions. Both post-process audits prove exact external restoration before evidence read. The launcher observation repair completed normally and did not alter combat behavior. Opportunity-isolated movement-to-attack remains a separate pending qualification.

## Launcher failure-observation repair after exact game PASS (2026-08-21)

The repaired game path is exact: `20260821T120800Z-core-combat-controls-finalization-passA` emits one final manifested combat JSONL and game `PASS 80/0`, with all four controls green. It remains uncredited because the launcher had already caught an unknown error, waited for process exit, and then masked that cause by applying strict combat validation to its incomplete fallback manifest. Kingmaker later exited naturally; recovery and audit-before-read were exact.

The launcher now persists the original caught error and UTC time in orchestration before exit handling. `New-KmcRuntimeResultV2` may construct a truthful FAIL fallback against an exact incomplete manifest, but the normal validator path still rejects that incompleteness. This preserves diagnosis without converting it into qualification. No combat, timeout, window-detection, process, restoration, or result threshold is weakened. Complete Release gates pass `21/217/17/209/255`; fresh package-bound runtime evidence remains mandatory.

## Interrupted control process: final manifest ownership and active-outcome observation (2026-08-21)

`20260821T105600Z-core-combat-controls-admission-passA` remains immutable and uncredited. Guarded recovery plus the independent audit passed before evidence read. The game produced `79/1`: invalid, repaired target-death, and non-mounted rows pass; cleanup's single failure compared the deliberately cleared current outcome slot against the preceding target-death outcome. Active exact-command admission now requires `LastOutcome == null`, while no-command controls continue to require unchanged historical identity. This is diagnostic-only and preserves the production target-admission repair.

The run also exposed protocol ownership: PowerShell created an empty `runtime-artifacts.json` before combat evidence existed, and the game returned its hash rather than publishing the later JSONL. `PublishRuntimeArtifactManifest` now enumerates the exact closed allowlist and atomically replaces any regular pre-existing manifest; a reparse destination fails closed. External artifact validation remains immutable and strict. The game process later lingered after terminal result; no cause is invented, no timeout is weakened, and no process-exit behavior changes without new evidence. Full offline gates pass `21/217/17/207/255`; fresh clean-package A/B remains required.

## Exact active-target admission wait (2026-08-21)

The first run of the published exact-target cancellation repair, `20260821T102109Z-core-combat-controls-repair-passA`, is preserved uncredited at `77/1`; independent restoration passed before evidence inspection. Invalid-target, repeated exception cleanup, and non-mounted stock delegation all pass. Target death again proves the exact lethal transition, retained pair, zero child/rule/resource chain, and full cleanup. The game log resolves the remaining failure: the diagnostic treated a submitted/queued wrapper as active and killed the target before `MountedPairAttackCommand.OnStart` accepted it, so startup failed with the generic exact-pair/target exception and the repaired `OnTick` cancellation was unreachable.

The controller and command now expose one internal observation-only predicate requiring the exact target, a nonterminal `Approaching` or `Attacking` transaction, and zero child starts. The control engine waits for that predicate before target death or repeated exception cleanup. Deterministic source-order coverage pins admission before mutation. No production targeting, action economy, attack, range, or cancellation behavior is broadened. Complete gates pass `21/217/17/207/255`, with PowerShell/JSON parsers `28/0` / `7/0`; the changed dirty DLL SHA-256/MVID are `b4bf816b484f003749ebca74845109ca23b5614e21c869b49c63c3ebec4aac57` / `c66f51f3-1534-4d1a-99e1-d68b309a977d` and require clean-HEAD package binding.

## Core control first-process repair (2026-08-21)

The first live four-row process, `20260821T100300Z-core-combat-controls-passA`, is preserved uncredited at `76/2`; exact independent external restoration passed before its evidence was read. Invalid null targets and repeated exception cleanup pass. Target death produces the intended exact lethal transition, zero child/rule/resource chain, retained pair, and full cleanup, but generic liveness faulting overwrote the expected interruption reason. The non-mounted row records hidden actions, rejected arming, `NotHandled`, unchanged agents/resources, and no rule chain, but the test conflated an unchanged prior `LastOutcome` with active work.

Production now recognizes only an invalid exact accepted target while the transaction has zero child starts, records a bounded cancellation reason, and issues native Interrupt before generic pair-liveness faulting. It does not touch post-child lifecycle or another target/command. Diagnostics snapshot the outcome identity before each control exercise and require it to remain unchanged when proving no new command. Full offline tests pass `21/217/17/207/255`; a fresh clean package and restarted same-suite A/B remain required.

## Core control harness (2026-08-21)

An additive `RuntimeCombatControlScenarioEngine` now owns the four remaining core rider controls without sharing or weakening the qualified attack evidence schemas. It runs exact ordered invalid-null-target, active-command target-death, repeated exception-cleanup, and unmounted stock-delegation rows. Targets are placed outside the measured pair range so target death and cleanup occur after the wrapper is accepted but before a child starts. The compact evidence contract requires unchanged rider/Mammoth action resources, zero child or Rulebook chains, stable pair identity with unique disposable targets, exact production paths, and complete target/combat/agent/pause/relationship cleanup. Runtime protocol, guarded launcher allowlists, game/final-result row allowlists, artifact validation, host disposal, and deterministic mutation regressions are wired. This checkpoint makes no runtime qualification claim.

Native rider and Mammoth incapacitation cleanup is now qualified from clean commit/package `ee08638ff19923b4f0aaf0d1ca03830de802d0cc` / `e68d90a25730760a52167e7ee41366c6490f49a7f134bb447e805aed745c7dc0`, suite snapshot `37a1989d174cdb0f44026ba64470e4fda1d1febc673fabffb668d977466f5897`. Rider A/B and Mammoth A/B each pass schema v7 `49/0` with exact native callback/delivery identity, first-delivery cleanup, accepted presentation restoration, and four audit-before-read external restoration proofs. The earlier diagnostic and validator failures remain immutable uncredited evidence.

The first repaired live rider row passes internally at `49/0`: one exact native unconscious callback now performs successful first-delivery cleanup and restores avoidance without residue. Final validation rejected only Unity's omission of an empty `cleanupErrors` array. The protocol now accepts absent-or-empty errors exclusively for successful schema-v7 cleanup, requires non-empty errors for the failed diagnostic shape, and rejects success carrying errors. No production behavior changes in this follow-up, and the preserved process remains uncredited.

The bounded cause probe now identifies the exact cleanup defect: KMC released its rider avoidance CountingGuard lease, but then compared the installed effective `AvoidanceDisabled` getter only to the pre-mount value. Stock Kingmaker independently forces that getter true while the unit is unconscious. The repair records the KMC lease unowned immediately after setter success and validates effective state as captured external disablement OR native unconsciousness. It neither writes consciousness nor broadens cleanup acceptance; all impossible combinations still fail closed. Fresh rider and Mammoth native-incapacitation A/B are required before qualification.

Complete repaired gates pass source/component/visual/harness/assembly `21/215/17/203/255`, Release build, PowerShell/JSON parsers `26/0` / `7/0`, diff, and prohibited-payload checks. The dirty build DLL is `0220ab1fbe166ec8a0c4cc0d2b82486cec08f5a896d83150d38217f4a5c890aa`, MVID `fdb17595-67af-4c98-a93e-3f1e9983d61e`; clean package binding remains required.

Schema-v7 runtime narrows the first fault to `RestoreMovementAuthority`, whose wrapper reports best-effort restoration residue. The coordinator previously emitted only the outer exception and discarded its inner cause. The current follow-up changes diagnostics only by retaining a bounded ordered cause chain; it does not alter movement, placement, cleanup, or lifecycle behavior.

Schema-v6 runtime now proves the first native rider callback is exactly `Conscious -> Unconscious`, followed by stock `Unconscious -> Conscious` recovery. The first callback's cleanup entered `Faulted` and the ordinary retry later reached `Unmounted`, so run `20260821T050300Z-rider-native-incapacitation-probe-v6` remains uncredited `47/2`. Schema v7 adds only cleanup-error capture to the bounded lifecycle ledger and failed-evidence protocol; no cleanup behavior changes until that error is observed.

The first native rider process is preserved uncredited at game `44/1`; independent restoration passed before inspection. Public `Damage=46` was issued against HP/Constitution `45/11`, but schema v4 timed out without a captured life transition or native delivery and did not retain sufficient raw post-mutation/controller-eligibility evidence.

Schema-v5 probe `20260821T040400Z-rider-native-incapacitation-probe-v5` is also immutable uncredited `FAIL 44/1`, with immediate audit-before-read. It proves the actor was Conscious/awake/in `AwakeUnits`, the mutation was applied, one stock lifecycle delivery dismounted the pair, and the later actor recovered to Conscious at damage `38`. This rejects the excluded-controller theory but schema v5 cannot distinguish the exact transient callback state. Schema v6 adds a bounded exact-pair callback observation before cleanup and binds immediate damage plus later recovery. It changes no actor behavior and requires exact `Conscious -> Unconscious`; a Dead callback remains a failure requiring a separately evidenced approach.

Native rider/Mammoth incapacitation diagnostics are offline-green under additive schema v4. Each isolated scenario mounts one exact pair, mutates only the selected actor's public `Damage` property to a verified nonlethal incapacitating value, and waits for stock life-controller/EventBus delivery. The result must prove the real actor changes from conscious to unconscious, exactly one native `UnitIncapacitated` lifecycle record cleans Mounted to Unmounted, and presentation/relationship state restores without a direct life-state setter or direct handler invocation. Separate processes prevent an unconscious actor from contaminating another row. Historical direct lifecycle scenarios remain unchanged; fresh runtime A/B is still required.

Active-command direct combat-end behavior is now qualified RT/TB A/B on clean package `6145369509eaf69eae30e22a42f72de63f0d5ddae707a22625cd54a47a03a6d1`, suite snapshot `ca0b8b9c1e926e5ddf3d847ff74d9646c1e52416e931e2949892be00e5e8783a`. RT passes twice at schema v40 `52/0`; TB passes twice at schema v41 `56/0`. The handler cancels the exact active rider wrapper and Mammoth Move idempotently, records two exact mounted-state non-cleanup lifecycle deliveries, emits zero combat chains, retains the valid pair and presentation/UI, preserves RT-zero/TB-actual-rider-Move accounting, and restores all internal/external state. Restoration was independently proven before every evidence read.

Active-command combat-end diagnostics are now offline-green under schema v40/v41. They reuse the exact qualified rider wrapper/Mammoth Move termination machinery, deliver the direct party combat-end handler twice after measurable out-of-range approach, and require two exact mounted-state non-cleanup lifecycle records plus zero combat-rule emission, mode-correct costs, retained pair/presentation/UI, exact command restoration, and cleanup. Offline totals are `21/213/17/202/254`; no runtime credit is claimed until fresh clean-package RT/TB A/B passes.

Direct combat-lifecycle qualification now passes twice from clean package `7aef5b851a75d6ba18e8cd4602eff14cbaf2cd4fdb764ac61e1e7254380e3ad5` and suite snapshot `c070d0d6697e8a21c58bb8c8952ac2d810c703db28e3f03ea25717be5d114df8`. Runs `20260821T020000Z-combat-lifecycle-suite2-passA` and `20260821T020500Z-combat-lifecycle-suite2-passB` each produce nine-row `PASS 400/0` with restoration-before-read. This qualifies only the direct handler/service seams; native incapacitation delivery and active-command combat-end behavior remain open.

The combat-alpha lifecycle checkpoint adds an independent schema-v3 runtime suite without rewriting Phase 2A history. A valid pair is retained across combat start and end; combat end routes through the mounted-combat cancellation service before observation. Exact rider/Mammoth death, companion removal, and pair-view destruction handler paths cleanly dismount; rider/Mammoth incapacitation and exception rows exercise the exact fail-safe service with truthful zero-native-delivery claims. Each row retains the accepted Mammoth-specific pose and binds final relationship, attachment, selection, command, and cleanup state. Offline gates pass `21/213/17/199/254`; native EventBus delivery, real incapacitation, and active-command combat-end cancellation remain runtime-open rather than inferred.

First live suite process `20260821T014600Z-combat-lifecycle-passA` completed all nine rows at game `400/0` and restored external state exactly, but remains uncredited because the final validator required active pose identity in unmounted phases. The evidence establishes the correct shape: exact Mammoth profile while mounted; null/inactive pose before mount and after cleanup; restoration verification persists after the first cleanup. The repair changes only validator/test phase semantics and requires a fresh package/suite restart.

The user accepted the exact Phase 2A visual-review foundation, so the combat ordering gate is open. Version `0.1.0-phase2b-dev.1` now contains the scoped combat implementation, and stationary mounted rider melee is qualified twice in each of real-time and turn-based mode; the remaining Tranche C controls and all later combat rows remain open.

The rider owns initiative and the only pair resource ledger. One rider Standard wrapper manually drives exactly one native ignored-cooldown `UnitAttack` child, using either the rider or exact active Mammoth as the rule initiator. `IsSingleAttack=true` prevents full, off-hand, additional-limb, or combined sequences. The Mammoth remains the sole pathfinder; exact turn-based movement is charged through the current rider `TurnController`, and an exact separate Mammoth turn is ended. The rider pathfinder remains disabled.

The transient overlay exposes distinct `Rider melee` and `Mammoth primary` choices. One exact-pair enemy click is consumed; ranged rider weapons, spells, invalid targets, unavailable rider Standards, wrong turns, unsupported profiles, and concurrent pair commands fail closed with feedback. Rider reach is scoped to Mammoth corpulence plus target corpulence plus planned melee-weapon range through the exact native private range method. Mammoth mode retains the stock selection across click and child initialization, requiring the exact natural-melee primary hand chosen by native single-attack order.

The first movement-to-attack implementation preserves that rider-owned wrapper while assigning one exact delegated `UnitMoveTo` to the Mammoth's active Move slot, never its queue. This active representation is required by the installed stock lifecycle: `UnitActionController.TickOnUnit` calls `StopMoving` whenever the Mammoth's command container is empty, which caused immutable uncredited run `20260816T210000Z-rider-move-attack-rt-passA` to retain all ownership and pose invariants but make zero progress. Same-package repaired RT A/B then pass `55/0` each. TB attempt `20260816T221500Z-rider-move-attack-tb-passA` passes every mechanical gate except the slot projection: exact `UnitCommands.Move` filters the exact finished raw slot to null pending the stock sweep. Schema v32/v33 therefore reads exact raw `GetCommand(CommandType.Move)` and accepts only the exact command or a truly empty slot after finish. Missing unfinished commands and every replacement remain rejected. Real time uses the stock Mammoth command controller; turn based advances the same slot-owned command only on the exact rider turn and leaves stock rider Move accounting authoritative. Historical v28-v31 validation remains intact.

The same change introduces a guarded runtime-only target service using the stock generic Mammoth blueprint, a `HideAndDontSave` hostile faction, zero XP, disabled AI, native no-loot state, and exact destruction-controller drain, plus a rule probe for initiator/target/roll/damage counts. Exact inspection after failed probes established the native primary-hand/secondary-hand/additional-limb selection order and the `EmptyHandWeapon` fallback. The target path classifies the stock-initialized primary source as either an explicit hand item or that fallback, requires natural-melee identity and a positive primary attack count, and proves identical before/after hand item, fallback, and limb references. No weapon provisioning or body mutation occurs. These surfaces are inert unless a validated exact-Working runtime scenario invokes them. No save payload, blueprint, target body, active mount, player inventory, party, quest, dialogue, or persistent fact is changed.

Offline evidence at this implementation checkpoint: source `21/0`, Release build PASS, component `183/0`, visual/source-order `17/0`, harness/protocol `152/0`, and exact assembly contracts `153/0` (`142/0` Kingmaker, `11/0` Wrath). Live real-time/turn-based evidence, action-cost deltas, lifecycle qualification, and non-mounted controls remain required before any combat PASS claim; the failed target-contract probes prove cleanup, but receive no combat qualification credit.

Guarded Probe F `20260816T052000Z-rider-hit-rt-probeF` validates the repaired target contract in game: exact native primary-hand item `5d7d23f5e35254d4bb087f7476163509`, natural/melee true, primary/secondary attack counts `1/1`, zero limbs before/after, no target-body mutation, and every hostility/AI/loot/reward/cleanup gate PASS. Its sole failure precedes click and exposes a diagnostic-only contradiction: measured pair approach radius is `2.37020588`, while the first protocol guessed that it must exceed `3.02`. The repair places the target at measured radius minus `0.12`, bounds navmesh projection to `0.06`, requires positive separation above `0.05`, and independently validates that exact relationship. No gameplay reach threshold is reduced; Probe F remains uncredited `FAIL` `16/1` with exact external restoration.

Guarded Probe G `20260816T053000Z-rider-hit-rt-probeG` proves that repair in game at exact distance `2.25020623` inside radius `2.37020588`. The pair enters real-time combat with healthy accepted pose, Mammoth-only path authority, exact rider selection, an available rider Standard, and the RiderMelee action armed, but the real `ClickUnitHandler.OnClick` call returns false before any command or rule event. The old row did not retain whether Harmony declined or controller context rejected it, then waited to deadline. The next diagnostic path explicitly prepares only the transient target's fog/view visibility, independently checks exact click target, player visibility, attackability, and rider melee readiness, and immediately preserves controller feedback on rejection. This is target provisioning only; area/global fog and every non-target entity remain untouched. Probe G remains uncredited `FAIL` `25/3` with exact external restoration.

Probes H through J successively prove click acceptance, exact command ownership, the stock real-time pause/start gates, and finally the need for hostile group memory during native combat preparation. The schema-v3 repair queues only the exact rider/target memory pair, lets stock controllers join and prepare every unit, waits for zero rider initiative and positive game time, and removes only that memory lease during cleanup. It never calls `JoinCombat` or edits initiative/cooldowns. Each diagnostic remains an immutable uncredited `FAIL` with exact external restoration.

Guarded Probe K `20260816T070000Z-rider-hit-rt-probeK` proves the native-entry repair fully, accepts the real click, and isolates the remaining admission mismatch. Mammoth distance `2.25020623` is within the exact pair radius `2.37020588`, but stock `UnitCommand.IsUnitEnoughClose` measures from the elevated/offset rider executor. The Mammoth movement command is already complete, so the wrapper exhausts four bounded no-op repaths and interrupts. The repair preserves Mammoth-origin range as authoritative and adds only a measured child-native radius bridge after that gate passes: rider distance plus `0.001`, with an absolute expansion cap of `0.75`, reapplied through the exact child-only `UnitAttack.GetApproachRadius` postfix. Outside-pair range receives zero native admission; explicit Mammoth attacks retain stock range. New semantic evidence and mutation tests bind the pair gate, both distances, both radii, cap, and adjustment flag. Current offline totals are source `21/0`, Release build PASS, component `189/0`, visual/source-order `17/0`, harness/protocol `154/0`, and exact assemblies `166/0` (`155/0` Kingmaker, `11/0` Wrath). Probe K remains uncredited `FAIL` `39/7` with exact external restoration.

Guarded Probe L `20260816T072000Z-rider-hit-rt-probeL` proves that bridge in game: pair distance/radius `2.25020647 / 2.37020588`, native rider distance/admission `2.47861218 / 2.479612`, adjustment true, one child start, zero repaths, native rule object observed, and rider Standard charged. It then fails `42/5` because the next wrapper tick's generic liveness check rejects one or more predicates before the diagnostic rule handlers observe a completed attack. Bounded decompilation and exact token `0x0600834E` prove `UnitEntityData.CanAttack(UnitEntityData)` is neutral-or-group-hostility only, not an in-flight hands/cooldown check, so no predicate is removed speculatively. Schema v4 now publishes the exact transaction terminal reason from a pure ordered fifteen-gate liveness snapshot. Schema v1/v2/v3 remain valid as non-qualifying history. Current offline totals are source `21/0`, Release build PASS, component `191/0`, visual/source-order `17/0`, harness/protocol `155/0`, and exact assemblies `167/0` (`156/0` Kingmaker, `11/0` Wrath); behavior and qualification thresholds are unchanged.

Guarded Probe M `20260816T074000Z-rider-hit-rt-probeM` reproduces the same exact attack path and isolates only `target-conscious`; every other named liveness gate remains true. Native `UnitCommand.Tick` may call child `OnAction`, allow the attack to incapacitate its target, and set a result before waiting for animation completion to invoke `OnEnded` and mark the command finished. The wrapper's next pre-child guard prevented that exact started child from completing. The repair keeps unconscious targets invalid through admission, then permits only exact child-start count one to advance the native terminal path. Counts outside zero/one fail closed, while target in-state and not-finally-dead remain mandatory. Current offline totals are source `21/0`, Release build PASS, component `192/0`, visual/source-order `17/0`, harness/protocol `155/0`, and exact assemblies `167/0`; no action, range, persistence, or cleanup gate changes.

Guarded Probe N `20260816T075000Z-rider-hit-rt-probeN` proves that completion repair: the native rider child returns `Success`, the pair transaction reports terminal `completed`, one child starts, no repath occurs, and native `LastAttackRule` exists. Pair and native range remain exact at `2.25020647 / 2.37020588` and `2.47861171 / 2.47961164`. Its remaining `43/4` failure is instrumentation/action-transition specific: all Rulebook counters and the forced-d20 count are zero, while the rider Standard cooldown remains `0 -> 0` despite the wrapper's manually set acted flag. `EventBus.Subscribe(object)` `0x060074BB` forwards to `RulebookEventBus.Subscribe(object)` `0x06007540`, which requires a global/target/initiator Rulebook subscriber marker; plain `IRulebookHandler<T>` is inert. `UnitActionController.TickCommand` `0x0600911E` charges through `UpdateCooldowns` `0x06009120` only when `UnitCommand.Tick` `0x060027A7` changes `IsActed` from false to true. The repair uses `IGlobalRulebookHandler<T>`, exposes the wrapper's native base action only after exact child admission, holds child ticking until the acted transition, and removes manual `SetIsActed`. Pre-admission failures remain uncharged and no cooldown or rule evidence is synthesized. Offline totals are source `21/0`, Release build PASS, component `192/0`, visual/source-order `17/0`, harness/protocol `155/0`, and exact assemblies `173/0` (`162/0` Kingmaker, `11/0` Wrath).

Guarded Probe O `20260816T081000Z-rider-hit-rt-probeO` does not exercise that repair because native combat entry times out first. Its forward player-group memory is present while reverse target-group memory is absent; no click, command, attack, rule event, action charge, movement, or pose outcome occurs. Exact `UnitMemoryController.AddToMemory` is buffered and has separate target-faction and later stealth admission gates; the evidence cannot distinguish which rejected the reverse request. The diagnostic-only provisioner now captures the exact two groups and leases their public `UnitGroupMemory.Add` entries directly, refreshes only `LastDetectTime` while awaiting dispatch/outcome, and symmetrically removes those exact entries before target/group destruction. This makes controlled target provisioning deterministic without editing faction, stealth, production combat, initiative, cooldown, or save state. Probe O remains uncredited `FAIL` `20/1`; external state restored exactly. Offline totals are source `21/0`, Release build PASS, component `192/0`, visual/source-order `17/0`, harness/protocol `155/0`, and exact assemblies `176/0` (`165/0` Kingmaker, `11/0` Wrath).

Guarded Probe P `20260816T083000Z-rider-hit-rt-probeP` proves the exact direct memory lease and its symmetric cleanup, then remains uncredited `FAIL` `20/1` before click: both memory directions are true, but rider, Mammoth, target, and player group never enter combat and the rider never prepares. Exact installed `UnitMemoryController.Tick` calls `item.Observer.Wake()` immediately after committing the buffered memory entry. The direct lease omitted that stock side effect; native combat joining iterates awake units and its turn-based-aware enemy test also requires the enemy endpoint awake. The narrow repair conditionally calls exact `Wake` only when either captured lease endpoint reports `IsAwake=false`, while preserving the exact memory/timestamp/removal scope. Deadline evidence now includes target awake/fog/peaceful state. No combat membership, initiative, cooldown, faction, stealth, production attack, pathing, or save state is edited. External restoration remains exact. Offline totals are source `21/0`, Release build PASS, component `192/0`, visual/source-order `17/0`, harness/protocol `155/0`, and exact assemblies `178/0` (`167/0` Kingmaker, `11/0` Wrath).

That repair qualifies the stationary mounted rider hit in real time twice from one clean package. Runs `20260816T084500Z-rider-hit-rt-probeQ` and `20260816T090000Z-rider-hit-rt-passB` each pass `47/0` on commit `07d44eb89c6a533bc99e386bad6fd3dac4a9ac73`, package `af4855f93e31084b2406a2b9e527b5d8587c5a7b82a52d61bb3863d7af777459`, DLL `e3ca251a6a04da607f9fb6bc21605d32756e2648769fc341d6a6575b1616db09`, MVID `06e14d27-85a7-49f6-900c-94ca99f68f04`. Both record one exact rider child, one native attack rule, one roll, one damage rule, eight damage, critical result under forced d20 `20`, zero unexpected pair attacks, zero repaths, rider-only Standard charging, healthy accepted Mammoth pose, symmetric memory removal, and zero relationship/presentation/entity/group/faction residue. Independent audits after each run prove exact Mods/save/authority/pin-set/Baseline/Working restoration and no process, lock, sentinel, or live KMC deployment. This evidence qualifies only stationary mounted hit RT; it does not qualify miss, TB, invalid target, target death, non-mounted control, or any Mammoth attack.

The turn-based stationary-hit path is now a separate schema-v5 scenario. It uses the previously qualified in-memory native settings callback to enable turn-based mode before mounting, requires native controller initialization and exact rider/Mammoth/target roster membership, invokes public `CombatController.StartTurn` once for the exact rider, waits for `CurrentTurn.Unit == rider` and `IsActing`, and preserves that identity through the native attack outcome. Cleanup removes the relationship, combat memory, entity, group, and faction before restoring the callback value, raw settings cache, persisted-setting identity, original pause state, and real-time mode. It does not edit initiative, cooldowns, saved settings, or saves. Offline totals at this pre-runtime checkpoint are source `21/0`, Release build PASS, component `194/0`, visual/source-order `17/0`, harness/protocol `157/0`, and exact assembly contracts `186/0` (`175/0` Kingmaker, `11/0` Wrath). Live TB A/B remain mandatory.

First TB run `20260816T093000Z-rider-hit-tb-passA` remains uncredited `FAIL` `10/1`. The callback and exact restoration succeed, but the row times out before mounting because it waited for `IsInTurnBasedCombat`; exact installed code requires `Player.IsInCombat` in that predicate, creating a pre-combat cycle. The narrow repair observes the probe's exact temporary setting before mounting, while retaining `IsInTurnBasedCombat`, initialized controller, exact roster, and rider acting-turn gates after native combat entry. Schema v5 also now preserves null future-phase identities and round `-1` for truthful FAIL rows; PASS still requires exact rider identities and a nonnegative round. No combat or safety threshold changed. Repaired offline totals are `21/194/17/158/186`; fresh clean-package TB evidence remains required.

Second TB run `20260816T094500Z-rider-hit-tb-probeB` remains uncredited `FAIL` `22/1`. It proves the repaired pre-combat ordering, exact mount/combat/roster state, and public `StartTurn(rider)`, then reaches no click because a directly controllable Kingmaker turn begins in native `Preparing` and becomes `Acting` only after a command is queued. The follow-up admits the exact rider current turn only in `Preparing` or `Acting`, queues the unchanged native click path, and credits schema-v5 dispatch identity only after a later frame observes that rider turn in `Acting`; exact rider `Acting` remains required at outcome. Exact Status token/enum contracts and source ordering prevent recurrence. No turn state, initiative, cooldown, or save is edited. Repaired totals are `21/194/17/158/187`; fresh clean-package TB evidence remains required.

Probe C `20260816T100000Z-rider-hit-tb-probeC` remains uncredited `FAIL` `35/1`. Every native pre-click gate passes, but production `CaptureContext` retained a duplicate inline `IsActing` predicate and rejected the exact rider's legitimate `Preparing` window. One pure action-window policy now serves both diagnostic readiness and production context: real time is admitted; turn based requires the exact rider in only `Preparing` or `Acting`; wrong-unit and all other statuses reject. Post-dispatch/outcome `Acting` requirements are unchanged. Repaired totals are `21/195/17/158/187`; fresh clean-package TB evidence remains required.

Probe D `20260816T101500Z-rider-hit-tb-probeD` remains uncredited `FAIL` `50/1`. The shared policy admits the real click; the exact rider turn becomes and remains `Acting`; one native rider child produces one attack, one roll, one damage rule, nine damage, and `CriticalHit` under forced d20 `20`; rider Standard changes `0 -> 6`; Mammoth Standard/Move remain zero; and duplicates/repaths remain zero. The child then reports terminal `Interrupt` despite exposing its completed attack rule, so the wrapper correctly refuses `Success`. The diagnostic worktree captures only result, acted state, elapsed command time, attack-rule presence, animation state, and the first non-child terminal caller in the existing terminal reason. It does not reinterpret the result or change runtime behavior. Offline totals remain `21/195/17/158/187`; fresh Probe E must identify the caller before a narrow repair.

Probe E `20260816T103000Z-rider-hit-tb-probeE` remains uncredited `FAIL` `50/1`. It repeats the exact mechanical path with one attack/roll/damage rule, seven damage, forced-d20 `CriticalHit`, rider Standard `0 -> 6`, zero Mammoth resource use, and zero duplicates/repaths. The terminal snapshot is `Interrupt`, acted true, attack rule observed, animation present but unfinished at about one second; the first captured caller is only `Kingmaker.UnitLogic.Commands.Base.UnitCommand.Interrupt`, so it does not yet identify the external interrupter. Cleanup and independent external restoration are exact. The follow-up retains a bounded 12-frame caller chain in the same diagnostic field without changing behavior or accepting `Interrupt`; complete gates pass `21/195/17/158/187`, and fresh Probe F remains required.

Probe F `20260816T110000Z-rider-hit-tb-probeF` remains uncredited `FAIL` `50/1`, with the same exact one-child mechanical success and exact restoration. Its caller chain proves `UnitAttack.OnTick` itself invokes `Interrupt`: after the only attack's `OnAction` has produced native `Success`, advanced the attack index, and left the animation unfinished, the turn-based one-second iterative delay enters `TryStartNextAttack(false)` with null `PlannedAttack` and replaces Success with Interrupt. The narrow repair never reinterprets that Interrupt. Before native `UnitAttack.OnTick` can enter the nonexistent-next-attack branch, the mounted child alone retains the existing Success only when native turn-based mode, acted state, observed attack rule, exact one attack, completed index one, and no planned attack all agree; base `UnitCommand.Tick` then owns normal animation-finished completion. Pure mutation regressions reject real-time, unacted, non-success, missing-rule, incomplete, and multi-attack variants. Exact installed tokens for `AllAttacks`, `PlannedAttack`, `GetAttackIndex`, and `OnTick` are pinned. Complete gates pass `21/196/17/159/191`; fresh same-package TB A/B remain mandatory.

That repair qualifies stationary mounted rider melee hit in turn-based mode twice. Fresh same-package runs `20260816T111500Z-rider-hit-tb-passA` and `20260816T113000Z-rider-hit-tb-passB` each pass `51/0` on exact clean commit/package `943156a7f580abf15e56dfd70a0a2b23688d335c` / `a87330d32836f7b359106df767832c15fd46849d87c2e08b2fe156b79cb143d0`. Both retain exact rider `Acting` turn identity through one rider-owned native attack/roll/damage, native terminal `Success`/`completed`, forced-d20 critical hit, five/eight damage, rider Standard `0 -> 6`, zero Mammoth resource cost, zero duplicates/repaths, healthy accepted pose, and complete mode/settings/pause/combat/target/pair cleanup. Independent audits after both prove exact external restoration. Stationary rider hit is now qualified RT/TB; miss, invalid target, target death, cleanup stress, and non-mounted control remain open.

The isolated `mounted-rider-melee-miss-rt` row preserves the qualified real click/command/range/resource/pose/cleanup path and changes only the deterministic Rulebook value to natural `1`. Attempts `20260816T114500Z-rider-miss-rt-passA` and `20260816T120000Z-rider-miss-rt-passA` are each preserved uncredited final `FAIL` `46/1`: one rider attack/roll, `IsHit=false`, no damage, no duplicates/repaths, native Success, rider-only Standard cost, healthy pose, cleanup, and external restoration all pass. Their sole failed assertion expected one result category, but the processes report `ArmorAC` and `Miss`. Exact `ModifiableValueArmorClass.SelectMissReason(bool,bool)` token `0x06007F2D` intentionally uses Unity random selection and can return only `Miss`, `DodgeAC`, `ArmorAC`, or `ShieldAC`; `RuleAttackRoll.IsHit` token `0x0600716B` remains authoritative. Schema v6/v7 accepts only that exact four-value codomain alongside `IsHit=false` and zero damage; hit, mirror-image, concealment, and parry categories remain rejected, and historical schema-v4/v5 qualifying hit evidence remains valid.

Codomain-repaired run `20260816T121500Z-rider-miss-rt-passA` then passes `47/0` from clean commit/package `ce7c62fc19903ca905eeed6f91f5172d638d7132` / `76c85edc1252902f09b87d96086d010b466f4172c107753405f6d39747d36e07`. It proves natural `1`, `IsHit=false`, native `Miss`, one rider attack/roll, zero damage, native Success/completed, rider Standard `0 -> 4.300836`, zero Mammoth cost, zero duplicate/repath, healthy accepted pose, cleanup, and exact external restoration. Same-package run `20260816T123000Z-rider-miss-rt-passB` is preserved uncredited `FAIL` `20/1`: before click, the transient target falls out of `Game.State.AwakeUnits`, target/player combat collapses, and no attack outcome occurs. Exact installed `UnitEntityData.Wake(float)` token `0x0600836C` only extends `AwakeTimer`; `SleepingUnitsController.Tick` `0x060090B1` rebuilds the awake list and `ShouldBeSleeping` `0x060090B2` honors public per-unit `Sleepless` `0x040054C8`. The repair leases `Sleepless=true` only on the disposable target that began false, requires exact lease and awake membership through combat, and restores false before destruction. Schema v8/v9 binds acquisition/readiness/release while retaining schema v1-v7. Sequential complete gates pass `21/196/17/163/200`; two fresh same-repaired-package processes remain mandatory.

Fresh sleepless-repaired run `20260816T130000Z-rider-miss-rt-passA` passes `47/0` from clean commit/package `f267bdea5c474a6492f4e1a3cc648e72cb9c41d8` / `614ac8be2039199c1a6121da3601ff6ded25285f4e1da0b835d5b2bab86929ad`. It independently proves exact lease acquisition, target awake membership, one natural-1 miss, zero damage, rider-only cost, native Success, and exact lease/external restoration. Same-package Pass B `20260816T131500Z-rider-miss-rt-passB` is preserved uncredited `FAIL` `20/1`; unlike the earlier awake failure, target/rider awake and both memory memberships remain true, but rider/Mammoth/target/player combat never begins. Exact `UnitCombatJoinController.Tick/TickUnit/ShouldEngageEnemy` tokens `0x06009360/61/62` depend on additional live entity/group, consciousness, ignored-by-combat, enemy-list, fog, stealth/ambush, reverse-memory, and mode/awake predicates not fully retained by schema v8. The observation-only schema v10/v11 diagnostic records each such value, retains v1-v9, and changes no behavior. Complete gates pass `21/198/17/164/207`; one fresh diagnostic process must isolate the exact native predicate before repair.
## Current combat qualification checkpoint - 2026-08-16T13:44:00Z

Stationary mounted rider melee hit is repeatably qualified in real-time and turn-based modes. The real-time deterministic miss path has one exact-package PASS (`20260816T133000Z-rider-miss-rt-joinprobe`, `48/0`) and one same-package uncredited failure (`20260816T134500Z-rider-miss-rt-passB`, `20/1`). The failure occurs before native combat entry: all retained memory, group, enemy-list, awake/sleepless, fog, stealth, ignored-by-combat, and rider/Mammoth life gates pass, while the disposable target is non-conscious. Both processes restore Mods, all saves and protected pins, locks, sentinels, deployment, and process state exactly.

No behavioral workaround is implemented. Exact installed inspection shows native `UnitLifeController.TickOnUnit` derives life state from damage, nonlethal damage, HP, Constitution, and death flags. Schema v12/v13 now observes those raw inputs immediately after creation, at target activation, each engine frame, and at the first `IUnitLifeStateChanged` event. It does not heal, resurrect, edit stats, or alter combat. Historical combat evidence schemas v1-v11 remain backward compatible. Complete local gates pass source `21/0`, component `198/0`, visual/source-order `17/0`, harness/protocol `166/0`, and assembly `217/0`; a clean-package diagnostic run is next. Mammoth-primary and later combat tranches have not begun.

Clean-package life probe `20260816T140000Z-rider-miss-rt-lifeprobe` is preserved uncredited `FAIL` `20/1`. It proves the target is created and activated Conscious at zero damage with HP `1` and Constitution `13`, then receives exactly `15` damage and emits one native `Conscious -> Dead` transition before combat. Every previously retained native-join predicate except target consciousness remains exact, and no mounted click, attack, action cost, or movement occurs. The independent audit ran before evidence inspection and proved exact Mods, save inventory, authority, pin set, Baseline, Working, process, lock, sentinel, and deployment restoration.

The next diagnostic remains observation-only. Schema v14/v15 globally records the first incoming weapon attack and damage rule against the transient target, including exact initiator, blueprint, weapon or source, amount, attack/damage flags, and whether it preceded an explicit expected-rider-dispatch marker. Current PASS requires exactly one rider attack and zero pre-dispatch interference; miss PASS requires zero damage. Historical schemas v1-v13 remain valid. No heal, resurrection, damage/HP/stat write, combat-membership edit, or result reclassification is present. Local totals are source `21/0`, component `198/0`, visual `17/0`, harness `168/0`, and assembly `226/0`; one clean-package source probe must precede any repair.

Two schema-v14 processes from exact commit/package `dd35f0b693471eb704618c78333eba153966464e` / `a39479f60f0b01c61f7cbeb39332e37d5a1f1b808ba004c90abe3cca0c8c8db2` resolve that source boundary. A `20260816T143000Z-rider-miss-rt-ruleprobe` passes `49/0` with zero pre-dispatch rules, one rider natural-1 `DodgeAC` miss, zero damage, rider-only Standard cost, stable target, and exact restoration. B `20260816T150000Z-rider-miss-rt-ruleprobeB` is preserved uncredited `FAIL` `20/1`: a third player-faction actor UUID `d17c8fd0-6627-40c4-bf9a-024cf001c1f7`, blueprint `baaff53a675a84f4983f1e2113b24552`, weapon `03d706655c07d804cb9d5a5583f9aec5`, performs one ordinary pre-dispatch weapon attack for 17 real non-DOT damage and kills the target. Both independent restoration audits pass exactly.

No AI suppression is inferred from faction identity. Observation-only schema v16/v17 adds exact initiator group/player-party/pair-group/direct-control/effective-and-raw-AI/command-empty context and preserves historical schemas v1-v15. Current offline totals are source `21/0`, component `198/0`, visual `17/0`, harness `169/0`, and assembly `230/0`; a fresh exact-package actor-context probe must identify the safe scope before any reversible diagnostic lease.
