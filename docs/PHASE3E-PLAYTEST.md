# Phase 3E Paired-Scheduler Playtest

Status: TODO

Version `0.1.0-phase3e-dev.2` is offline-complete but has not been packaged or run in Kingmaker. No Phase 3E scheduler gameplay row is credited yet.

The immutable observation input remains `20260904T060000Z-phase3e-dev1-mammoth-tb-observation-passA`: gameplay `FAIL 49/1` by design, exact command encounters `2,485`, stock false `2,485`, stock true `0`, scheduler drives `0`, and immediate independent restoration audit PASS. It selects Option A but is not a vertical-slice success.

The first gameplay gate is limited to the existing focused `mounted-mammoth-primary-hit-tb` scenario, represented by matrix rows `shared-rider-turn-mount-primary-passA` and `shared-rider-turn-mount-primary-passB`. Both must use one immutable clean package and qualification-suite identity, separate fresh game processes and run IDs, and an audit-before-read after each process.

Required credit is one in-range mount-owned Standard command, first grant followed by native start within two actionable frames, one wrapper/child/animation/attack-roll chain, at most one damage event, one terminal result, one mount Standard charge, zero rider Standard cost, exact rider `CurrentTurn.Unit` throughout, zero native mount turn, zero duplicate-frame drive, zero foreign adoption, and exact slot/lease cleanup.

If either process fails, preserve it unchanged and attribute the exact native boundary before using either of the two authorized narrow repair cycles. Do not weaken schema 56, retry under a new package merely to obtain a PASS, broaden to a global controller, or begin later tranches first.
