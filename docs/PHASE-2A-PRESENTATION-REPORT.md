# Phase 2A presentation report

Status: IN PROGRESS

Authority: `planning/MOUNTED-COMBAT-PHASE-2-MASTER-MISSION.md`.

The Phase 2A checkpoint is not yet complete: persistence/uninstall and all seven individual presentation rows are technically green twice in fresh processes, including native UI ownership/repaint and camera follow/command routing. Dev.12 camera A/B close the dev.11 stationary terrain-snap defect without weakening a threshold. Two complete-suite repeats, final review framing/package and launcher, and manual visual acceptance remain open. Phase 1 established only a mechanically stable attachment with `MECHANICALLY VIABLE, NEW ANIMATION/POSE WORK REQUIRED` presentation; no subjective acceptance is inferred from the automated Phase 2A captures.

Tranche A lifecycle and transient-persistence safety are runtime-qualified at version `0.1.0-phase2a-dev.1`: the deterministic player-action controller passes availability twice at `29/0` and mount/dismount twice at `44/0`. Actual mounted IMGUI repaint plus native selection/portrait/circle/action-bar ownership later qualified twice at dev.11 `30/0`. Native save passes twice at `63/0`, proving exactly one real `SaveRoutine` prefix delivery, synchronous clean dismount, one bounded no-write suppression, unchanged Working identity, and exact restoration. It does not claim stock serialization or a save round trip.

Three earlier native-area processes remain preserved as engineering failures: the first exposed asynchronous unload delivery, the second exposed Unity end-of-frame destruction, and the third exposed premature publication of a private sticky-loading observation. Strict validators were not weakened. The final exact-phase implementation then passed area twice at `47/0`, with cleanup on frame 5, residue-free loading-start on frame 6, loading-stop on frame 65, fresh-world proof on frame 74, and nine ordered native deliveries. Native mode passed twice at `60/0` with an exact reversible in-memory callback/EventBus transition and unchanged persisted value. Disable/residue passed twice at `63/0` with synchronous cleanup, zero disabled-frame overlay/anchor/agent residue, and exact single-overlay re-enable.

The area, mode, and disable/residue runs all bind clean guarded-published commit `7ea7105b7100c7dbceedf606cfb59d0076432e2b`, package SHA-256 `2909011239621e840416905abcaeaa55cf47462eacca658c0a65721d8db15f22`, manifest SHA-256 `8ce34977cdeeb76ef3cd68379a26cc0aa78fc0993b9692a2d840efcaa73d2918`, and DLL SHA-256/MVID `894f419c7dbaa176eb07a4dccc2a13e1c8f623153a2d3d1fa8665203c9fcb2a9` / `784a9e68-9b04-4cc4-b598-91e25319acd7`. All six processes restored Working, protected-save inventory, and Mods exactly.

The `0.1.0-phase2a-dev.2` checkpoint implemented only the authorized presentation seam: one exact seven-bone Medium-humanoid profile, evaluated-frame restoration, analytical leg solving, UI ownership/repaint observation, camera-follower observation, bounded equipment-set leases, and seven exact runtime rows. Clean commit `bb974f3c764881cf828777d8bb32fd9be5c7cfe4` produced package SHA-256 `e7d71d7c52acaf3030851077e55eb483c211b14818be1e467f3e03e962c02a9f`, manifest SHA-256 `bca3b81ab88b2a9f7689816e1eea80e5a155473715adf29e2d87b48be5d3cc0e`, and DLL SHA-256/MVID `148d61729af42a0202babad403c228a346db3e74f1659f8308dc0a22021fae99` / `6eec176f-702f-4a64-a7e5-d08b8c11653f`.

The first guarded `pose-idle` calibration, `20260815T034700Z-pose-idle-calibrationA`, is a preserved `FAIL` at `24/2`, not Pass A. It nevertheless proved 209/209 healthy/applied pose observations, exact seven-bone ownership, maximum foot/knee/segment residuals below `0.000004`, idle local-frame deltas below `0.000528`, zero clamp, zero movement drift, verified pose/attachment restoration, and exact external restoration. Two diagnostic defects caused the failures: the row retained the maximum of a cumulative frame-cost average, permanently preserving the first warm-up sample, and final cleanup required an Update sample on an intentionally stationary row even though its disabled rider pathing correctly produced 209 LateUpdate samples with zero residual and no pending recovery. Version `0.1.0-phase2a-dev.3` fixes those evidence semantics without changing the `2000/500` microsecond limits or any synchronization residual threshold. Deterministic regressions bring the component suite to `141/0`; visual-capture contracts pass `12/0`, harness/protocol `139/0`, and exact assembly contracts `101/0`. No presentation row or subjective visual PASS is claimed from this failed calibration.

Dev.3 calibration `20260815T041000Z-pose-idle-dev3-calibrationA` is also preserved without Pass A credit. Its in-game row passed `26/0`, proving the repaired completed mean/max at `17.92/1496.8` microseconds, 207 healthy/applied frames, microunit solver residuals, zero drift/lag/recovery, final synchronization PASS, and next-frame component/baseline restoration. The strict external validator still rejected its transition-frame `cleanupAfter.poseComponentCount=1`: the disabled adapter was queued for Unity destruction and was proved absent on the following frame, but the producer had not refreshed the serialized cleanup snapshot. Dev.4 now recaptures `cleanupAfter` on that verified later frame before evidence publication; the validator and zero-component requirement remain unchanged. The complete dev.4 local gate passes source `21/0`, Release build, component `141/0`, visual `12/0`, harness `139/0`, and assembly `101/0`. Subjective visual acceptance remains open.

Dev.4 calibration `20260815T042100Z-pose-idle-dev4-passA` remains a preserved restored `FAIL`, not Pass A. Its in-game row passed `26/0` with 209 healthy/applied frames, completed mean/max cost `18.5919/1589.4` microseconds, microunit solver residuals, zero drift/lag/recovery, final synchronization PASS, and exact later-frame cleanup including `cleanupAfter.poseComponentCount=0`. The external PowerShell validator duplicated the obsolete unconditional Update-sample requirement and rejected this exact stationary row despite 209 LateUpdate samples. Dev.5 applies the same exact stationary-row policy at that independent boundary, retains both-phase coverage for every moving row, and adds acceptance/rejection regressions using JSON-shaped evidence. The preserved dev.4 game evidence passes the corrected request/game validators at `31/0` and `39/0`, but its immutable final result remains `FAIL` and receives no qualification credit. The complete dev.5 local gate passes source `21/0`, Release build, component `141/0`, visual `12/0`, harness `139/0`, and assembly `101/0`; no runtime presentation PASS or subjective visual PASS is yet claimed.

Dev.5 then qualified `pose-idle` twice from clean commit `a76a2d4f8787b5a9dd28924742706145e498a23c` and package SHA-256 `c0c036233a9ff744ef5ffbfd9750fa9b9ba1baec0ef9be0f7eb4c55f8377b5c5`. Runs `20260815T044100Z-pose-idle-dev5-passA` and `20260815T044700Z-pose-idle-dev5-passB` each passed `26/0`, with 209/207 healthy and applied frames, mean/max cost `18.1928/1478.4` and `18.4696/1536.7` microseconds, microunit solver residuals, zero drift, exact stationary final synchronization, zero cleanup residue, and exact external restoration. The mounted captures are consistent; the dismounted captures remain partly pillar-occluded and are automated evidence only, not subjective acceptance.

The first dev.5 `pose-walk-run` attempt, `20260815T045300Z-pose-walk-run-dev5-passA`, is preserved without Pass A credit. It passed in game at `68/0`, completed two endpoints, produced 241 walk and 80 run samples at `1.25` and `4.064` world units/second, kept 441/441 pose observations healthy and applied, and restored exactly. The external validator rejected its deliberate `86.85`-degree inter-leg direction change as turns/corners-only residue. Dev.6 recognizes that the declared walk/run route intentionally chooses a divergent second leg, requires a positive measured direction change there, preserves the dedicated turn row's `>=75`-degree gate, caps all turn metrics at 180 degrees, and still rejects stray turn evidence elsewhere. Its complete local gate passes `21/0`, build, `141/0`, `12/0`, `139/0`, and `101/0`. The failed terminal result remains immutable and uncredited.

Turn/stop attempts `20260815T055000Z-pose-turn-stop-dev7-passA` and `20260815T061000Z-pose-turn-stop-dev8-passA` are preserved final `FAIL` results with no Pass A credit even though their in-game rows passed `80/0`. Both proved the fixed movement, turn/reversal, pose-cost, synchronization, cleanup, and external-restoration gates, but both omitted the exact required `pose-stop-motion` screenshot. Dev.8 correctly introduced a one-render-boundary delay before authoritative stop, but the second run proved that sequencing was not sufficient: `CaptureMilestone` silently rejects names outside its static allowlist, and that allowlist omitted `pose-stop-motion`. Dev.9 adds exactly that already-required milestone and a declaration-scoped source regression while retaining the render-boundary fix. The complete dev.9 local gate passes source `21/0`, Release build, component `144/0`, visual `14/0`, harness/protocol `139/0`, and assembly-backed `101/0`; all screenshot counts/order and mechanics/performance thresholds remain unchanged. Fresh exact-package A/B runtime evidence is still required.

Dev.9 then qualified turn/stop twice from clean guarded-published commit `43eb67be89cd491f049cd395359b990d72090b4e`, package/manifest/DLL SHA-256 `908063740a0b9d2fda2f65e0ef70513b6b8a6d36637d4b6304be0799498118a3` / `c110984a82bc25143e673699a9f4559e230dba6268d78c1a30a4185714250de0` / `408fad45f8292102ca360cb81dc806ffcce7b4073a56702906047b32b2fc6d8e`, and MVID `d5fb83b9-acfd-444f-9e01-003a0a0d71dd`. Runs `20260815T062000Z-pose-turn-stop-dev9-passA` and `20260815T063000Z-pose-turn-stop-dev9-passB` each passed `80/0`, included the exact seven expected captures, issued one authoritative stop, completed 2/2 qualified route legs, and measured `167.4615/167.2724` degrees of turn/reversal. A/B recorded 383/402 healthy and applied pose frames with maximum/mean costs `1529.8/13.9830` and `1524.2/14.6726` microseconds, passed final synchronization, restored pose and attachment baselines with zero component residue, and restored Working/protected saves/Mods exactly. Automated frames are mechanically consistent across processes; later views retain pillar occlusion and selection highlighting and do not constitute subjective visual acceptance.

Doorway/formation attempt `20260815T064000Z-pose-doorway-formation-dev9-passA` is a preserved final `FAIL` and receives no Pass A credit despite its game row passing `88/0`. It selected the exact authored door, passed the unmounted control, completed 3/3 endpoint-qualified legs, normalized stock formation selection, preserved non-pair isolation and clearance, recorded 532 healthy/applied pose frames at maximum/mean `1704.8/13.8917` microseconds, passed final synchronization, cleaned to zero pose components, and restored all external state. Because this row mounts after its unmounted doorway control, its first emitted `InitialConfiguration` sample truthfully captured the one-time `10.26192`-unit and `169.67844`-degree pre-correction displacement; the same sample corrected to effectively zero, while all calibrated Update/LateUpdate position/yaw maxima remained within `0.081081/0.074646`. The external latest-sample validator nevertheless applied calibrated pre-correction bounds to that explicitly uncalibrated phase, then would have rejected the semantically valid null authority age for unmatched initial yaw. Dev.10 aligns the external rule with the existing producer and aggregate contract: initial pre-correction displacement is admitted only if the same sample's post-correction position/rotation is at most `0.10`; calibrated bounds remain fixed. Harness regressions accept corrected initial state and reject uncorrected or calibrated mutations. The complete dev.10 gate passes `21/0`, build, `144/0`, `14/0`, `140/0`, and `101/0`; fresh exact-package A/B remain required.

Dev.10 then qualifies doorway/formation in two fresh guarded processes from clean published commit `f343695fe0b09eec4b164e6c67b06e05af465ed6` and exact package SHA-256 `9f05a433f7bb3eb0d0e211a14b170d2e082dd48d21e97528a9a9eda7adcd93c7`. Runs `20260815T065000Z-pose-doorway-formation-dev10-passA` and `20260815T071000Z-pose-doorway-formation-dev10-passB` each pass row/game/final at `88/0`, exercise the same authored door and valid unmounted control, complete 3/3 endpoint-qualified legs, normalize formation selection, and keep non-pair interference at zero. A/B non-pair final distances are `0.04064/0.03895`; minimum pair/non-pair separation is `2.09395/2.08391` against required `1.56061`; one allowed unexpected repath occurs in each process. Pose coverage is 532/530 healthy and applied frames at maximum/mean `1685.4/13.5716` and `1634.7/13.6538` microseconds, with final synchronization and zero cleanup residue. Each six-frame set reconciles byte-for-byte. Repeated wall/door occlusion and native blue selection silhouettes remain explicit manual-review concerns, not automated visual acceptance.

Equipment runs `20260815T072000Z-pose-equipment-variants-dev10-passA` and `20260815T073000Z-pose-equipment-variants-dev10-passB` qualify twice at `28/0` from clean commit `b8db204b6bd58b26ac211909e18028bcd388b224` and exact package SHA-256 `8d25375a08299d26aa7efb72c5c37860f348fabbbdce0c046c3170b4f180b2ca`. The protected fixture repeatably exposes only its original one-handed weapon set (`5363519e36752d84698e03a86fb33afb`), with no secondary/shield and no safely selectable two-handed set. The scenario records that bounded availability without inventory mutation. A/B each deliver 99 healthy/applied pose observations, maximum/mean costs `1495.5/25.3939` and `1532.5/24.9182` microseconds, exact stationary synchronization, zero cleanup residue, three reconciled captures, and exact external restoration. This qualifies the available one-handed presentation and documents shield/two-handed unavailability; it does not claim those missing variants were visually tested.

First UI attempt `20260815T074500Z-ui-selection-portrait-actionbar-dev10-passA` remains final `FAIL` and uncredited although the game row passes `30/0`. It records exact rider-selected, mount-normalized, away, and back observations; every phase proves exact `SelectionManager` identity, active owned `ActionBarManager`, selected portrait controller, and selected native circle. IMGUI repaint rises from `23` to `229`, the mounted overlay qualifies in game, 206/206 pose frames remain healthy/applied at maximum/mean `1708.9/18.7796` microseconds, final synchronization and cleanup pass, all six captures reconcile, and every external authority restores. The external validator correctly rejects the published `Mount`/disabled overlay fields. Publication ran after cleanup and read mutable live overlay state instead of the mounted state already used by the game assertion. Dev.11 snapshots label/enabled/visible/repaint/activation/geometry before cleanup and publishes only that immutable observation. Two component regressions distinguish mounted and later dismounted states, while a source-order gate forbids live post-cleanup reads. Thresholds and required ownership phases are unchanged; fresh exact-package A/B are required.

Dev.11 UI runs `20260815T080000Z-ui-selection-portrait-actionbar-dev11-passA` and `20260815T081500Z-ui-selection-portrait-actionbar-dev11-passB` qualify twice at row/game/final `30/0` from clean guarded-published commit `46d6ed83e64708832c9f209ced86771df949536d` and package/manifest/DLL SHA-256 `5fdf4592c89d14526d9dd8974ced280fa7d04a524b9097e31946c17b84c31805` / `79b013c0f8b69404a9d8e90f509d744e1e55dc6627794bfe07d2b8d597b2d279` / `5fe4fe57178abcd9a4bbfe6438666e213f098cea6a2b5d37639dd1b70e238332`; MVID `552a753b-073a-4d9a-ae3d-c9ded28ff91b`. Both contain the exact four ownership phases with exact selected identity, active owned action bar, one selected portrait, and one selected native circle. Repaint rises `23->220` and `23->216`; the immutable mounted overlay is `Dismount`, enabled, visible, rendered, and unactivated. A/B record 197/193 healthy and applied pose frames at maximum/mean `1620.6/18.95685` and `1655.2/20.02642` microseconds, pass stationary final synchronization and zero-residue cleanup, reconcile all twelve captures, and restore Working/protected saves/Mods exactly. The gameplay-camera PNGs do not include full UI chrome, so direct native ownership evidence is authoritative; physical pointer behavior and subjective appearance remain manual-review claims.

Dev.6 run `20260815T050600Z-pose-walk-run-dev6-passA` is likewise preserved without Pass A credit. Mechanics remained healthy: 2/2 endpoints, `241/80` walk/run samples, `1.25/4.064` speeds, `86.9282` degrees of route direction change, 442/442 healthy/applied pose observations, `319/442` Update/LateUpdate coverage, final synchronization, zero cleanup residue, and exact external restoration. The row nevertheless failed `67/1` because one pose application measured `123729.0` microseconds, above the unchanged `2000`-microsecond maximum; the completed mean was `294.7244`, below the unchanged `500`-microsecond limit. The exact timed path allocated nine reference objects per evaluated frame—seven baseline snapshots plus two two-bone solutions—so a collection pause is the bounded working theory; the run itself does not prove causality.

Dev.7 removes that allocation source without changing a presentation, synchronization, or cost threshold. `Snapshot` and `TwoBonePoseSolution` are now immutable value types, the reusable snapshot list is pre-sized, and the successful restore/verify path no longer creates delegate closures or a failure list. A deterministic regression prevents either per-frame carrier from reverting to a reference type. The complete local gate is source `21/0`, Release build PASS, component `142/0`, visual `12/0`, harness/protocol `139/0`, and assembly-backed `101/0`; the dirty-build DLL SHA-256/MVID are `e5b8c348bda3c159e8801bcc7e49ba79be9988df291c64a936b3206dbc40f114` / `3bf65305-08a0-49af-acad-824e0b2e6b74` and are not yet package identity. Fresh exact-package A/B evidence is still required.

That repair qualified walk/run twice from clean guarded-published commit `ce4e4d46d07d12ca4418170412f75003fcaaa556`, package/manifest SHA-256 `54d33b52008694e15ab349fac3667f9a237893022ce27f5a651b533731744ca1` / `a2fcb136c12176ad6c1e57ce2ebf463d6f1919e500a802b2b00b3239e52b4b20`, and the same DLL/MVID. `20260815T053000Z-pose-walk-run-dev7-passA` and `20260815T054500Z-pose-walk-run-dev7-passB` each passed `68/0`. A/B completed 2/2 endpoints, recorded 448/449 healthy/applied pose frames, measured maximum/mean costs `1623.1/13.7308` and `1672.5/13.6069` microseconds, exercised walk/run at `1.25/4.064`, observed `86.8868/86.9158` degrees of route direction change, passed final synchronization, cleaned to zero pose components with exact baseline/attachment restoration, and restored Working/protected saves/Mods exactly. Automated walk/run/dismount captures are consistent and show no detached-body or cleanup regression; they do not confer subjective visual acceptance.

The first turn/stop attempt, `20260815T055000Z-pose-turn-stop-dev7-passA`, remains an uncredited restored `FAIL`. Its in-game row passed `80/0`: a `167.6377`-degree turn/reversal, one scoped authoritative stop, 388 healthy pose frames, `1533.6/14.1714`-microsecond maximum/mean cost, final synchronization, and exact cleanup. The external validator correctly rejected the missing `pose-stop-motion` screenshot. The producer waited for `IsReallyMoving` before queuing a moving frame, while the deterministic `0.75`-unit stop threshold could arrive first. Dev.8 introduces a pure capture-boundary state machine: it queues a missing moving milestone, retains one render boundary, and then admits Stop. Two component regressions plus a source-order visual regression preserve both missing/already-captured branches. Exact screenshot and mechanics validators are unchanged. Local gates pass `21/0`, build, `144/0`, `13/0`, `139/0`, and `101/0`; fresh A/B evidence remains required.

Dev.11 camera Pass A `20260815T083000Z-camera-follow-command-routing-dev11-passA` passed row/game/final `59/0` from clean commit `182e9b2fc58df9e89b80536d61071387e42010e9` and exact package SHA-256 `70b0918a34122d74c82d5d33bd2ad83715b3de5842d16e5ca06806ebb1620301`. It completed one 5-unit route, observed native camera follow through movement and away/back selection, retained zero command replacements, selection loss, and non-pair interference, recorded 244/244 healthy pose frames at maximum/mean `1638.2/18.45697` microseconds, and restored exactly. This remains a valid single-process result but cannot be paired with repaired dev.12.

Same-package dev.11 Pass B `20260815T084500Z-camera-follow-command-routing-dev11-passB` is a preserved final `FAIL` `28/2` and receives no Pass B credit. It recorded one stationary LateUpdate pre-correction rider view/entity displacement of `1.5942225456` units, position lag observed/permitted/violations `5/4/1`, and one stationary-position violation. Update residual, post-correction state, and the final stopped boundary were effectively zero; camera follow was accepted, pose remained 75/75 healthy/applied at maximum/mean `1600.2/33.07067` microseconds, cleanup had zero residue, and guarded restoration was exact. The strict gate correctly stopped before away/back and was not weakened.

Bounded exact-MVID inspection identifies the cause: Kingmaker's standing `UnitMoveController.Tick` path may call public parameterless `UnitEntityView.ForcePlaceAboveGround` token `0x06001848` after a small parent-follow delta. That method terrain-raycasts and writes both the view root and `EntityData.Position`, which is invalid for the rider parented to the Mammoth anchor. Dev.12 installs a ninth exact-token prefix that suppresses only the exact active rider while Mounted; all non-rider/unmounted calls remain stock and no global movement tick is patched. A pure identity policy regression covers mounted/nonmounted/nonpair/null cases, and the camera row deterministically calls the exact method and requires one interception with zero view/entity movement. Current local totals are source `21/0`, Release build, component `147/0`, visual `15/0`, harness/protocol `141/0`, and assembly `103/0` (`92/0` Kingmaker, `11/0` Wrath). The following exact-package A/B results supply the required runtime repeat.

The exact dev.12 repair and identity records are guarded-published through clean commit `e1dabf412a0c7a80924b305752f1f84b1f0334dd`. Its qualifying package ZIP/manifest/DLL SHA-256 are `a748b5f00afc69cd50e3224ea9f737bbc440cf1914839248d834c20f54be025a` / `be1b2d3f3effea5c20293dda06af0a529fcc12956d2a3497102ecc14847d4486` / `d6c1222f933a8864c75ff42bf172c0c0b7fa42a8a34bc5cfec749a1701a4db1a`; MVID is `cdf65654-464e-4dc9-87a9-a75fc0d2c4fb`.

Dev.12 camera runs `20260815T130000Z-camera-follow-command-routing-dev12-passA` and `20260815T131500Z-camera-follow-command-routing-dev12-passB` each pass the game row and final evidence with `61/0` assertions after a wholly fresh exact `-WhatIf` and process. A/B complete the same `5.0000003173`-unit path at final distances `1.2133210459/1.2232870951`, observe native camera follow for `247/250` samples through movement and away/back selection, return the target residual to zero, and retain zero command replacements, selection loss, non-pair interference, position-lag violations, or stationary-position violations. Raw position lag is exactly bounded (`3/3/0` and `2/2/0` observed/permitted/violations), and both stopped final boundaries have zero position residual and no pending recovery.

A/B record `248/248` and `251/251` healthy/applied pose observations with maximum/mean costs `1535.0/16.75685` and `1489.7/15.95498` microseconds. Each five-frame sequence contains mounted idle, moving camera, camera away, camera back, and dismounted milestones; every filesystem length/hash tuple reconciles with row and manifest evidence. The frames demonstrate technical repeatability and camera composition changes only. Rider posture, occlusion, native highlighting, and physical pointer feel remain manual-review decisions.

Both transactions restore the protected inventory digest to `973f203e16aad29d7e4865bae6f81149d959a4ac179953c87781a06d1c71898d` and Mods to `e62320bbe7d4b83edc128a62f9f5852b0669c5549859602c335c649318578d47`; Baseline is immutable, Working is restored, all restoration flags are true, and no Kingmaker process, active lock, sentinel, live KMC deployment, or source-tree change remains. This closes individual-row technical qualification only; the two complete presentation suites and exact manual-review checkpoint remain mandatory.

This report will bind the accepted pose strategy, exact supported rider/Mammoth profile, technical gate totals, two fresh-process results for every Phase 2A scenario, UI/camera observations, lifecycle claim limits, frame cost, known visual compromises, package/DLL hashes and MVID, protected-save/Mods restoration, and the exact manual-review checkpoint.
