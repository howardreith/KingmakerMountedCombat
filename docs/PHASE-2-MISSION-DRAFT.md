# Phase 2 mission draft

Status: BLOCKED — CRITICAL

This is a conditional draft, not authorization. The actual next mission must first be a Phase 1 resume that closes the missing-fixture gate.

## Admission gate: Phase 1 resume

1. Inventory only exact filename-prefiltered `KMC_AUTOMATION_BASELINE` and `KMC_AUTOMATION_WORKING` candidates.
2. Implement/test a KMC-owned descriptor guard proving exactly one distinct pair, matching intended campaign/area identity, immutable baseline, and writable working copy. Do not open any non-KMC archive.
3. Extend request/result schemas and the host for explicit save-backed scenarios; keep no-save smoke isolated.
4. Prove the exact Medium rider has the exact active Mammoth pet, current Mammoth size is larger, both views/stock agents are valid, mode is Default, and no combat is active.
5. Run all lifecycle, movement, doorway-control, selection, formation, pause/cancel, boundary, and visual steps twice in fresh processes with exact restoration.
6. Apply the existing kill criteria. Disable B on failure; rescore C/D. Publish a proceed, pivot, manual-review, or stop result before Phase 2.

## Conditional Phase 2 objective if B proceeds

Build the smallest combat-capable mount-authoritative pair without assuming any unproven persistence, action, targeting, animation, or mode contract.

Order of work:

1. explicit mounted-pair persistence policy and uninstall/repair behavior;
2. rider basic melee attack;
3. mount basic natural attack;
4. movement/action economy;
5. combat start/end behavior;
6. death, unconsciousness, prone, and forced movement;
7. turn-based and real-time parity;
8. charge;
9. reach and attacks of opportunity;
10. ranged/spellcasting restrictions;
11. mounted feats;
12. additional mounts/rider sizes;
13. UI/polish;
14. compatibility and hardening.

Every stage requires exact Kingmaker contract mapping, deterministic tests, two-pass runtime scenarios, save/uninstall safety, non-mounted isolation, and a narrow Harmony surface. Failure of movement authority, cleanup, save safety, presentation, or external-state restoration triggers a C/D pivot; no broader workaround is authorized implicitly.

If Phase 1 pivots to C or D, discard the B combat order and write a new mission for the actual simplified architecture. Do not execute any part of this draft under the current blocked status.
