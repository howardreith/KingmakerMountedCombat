# Phase 2 recommendation

Status: PHASE 1 COMPLETE — PROCEED RECOMMENDED

## Decision

Phase 1 is complete. The truthful final ledger is `25 PASS / 0 attributable FAIL / 0 DEFER`, no K1–K12 criterion fired, and Architecture B is the evidence-backed recommendation for a separately authorized Phase 2. This recommendation does not itself authorize Phase 2 execution or a public release.

Architecture B preserves Kingmaker's stock mount pathfinding and confines rider synchronization and ownership to the validated pair. Lifecycle and all eight movement rows now have repeatable A/B evidence; save safety, load safety, and area transition each retain one independently qualified boundary run. Final scores are A/B/C/D = `41/71/77/89`. D's higher structural-safety score reflects removal of the visible two-entity product, while B is the only live-qualified architecture that satisfies that product intent. A remains too cross-cutting; C and D remain bounded pivots if later combat, persistence, animation, or compatibility evidence fires an existing criterion.

No gate or threshold was weakened, and the earlier fixture-limited deferrals remain preserved as F0 history rather than being rewritten.

## Evidence supporting Architecture B

- Exact F1 fixture admission passed: Baseline=1, Working=1, near-match=0; exact internal/campaign/area identity; Baseline immutable; Working-only write authorization; exact protected Auto/Quick content and metadata pins.
- Lifecycle A/B each passed eight rows and `339/0` assertions under their stated direct-handler/service scope.
- Pair-only movement A/B passed open ground `47/0`, stop/start `61/0`, turns/corners `74/0`, pause/unpause `49/0`, and destination cancel `49/0` per run.
- F1 doorway A/B passed `60/0` each with the same-current-Mammoth unmounted matched control and mounted strict traversal; selection A/B passed `54/0` each with a real third-unit away/back switch; formation A/B passed `58/0` each using a meaningful stock group movement command. These six processes total `344/0` assertions.
- The mount remained the single pathfinding authority; rider entity/view/anchor synchronization remained within unchanged gates; cleanup left no KMC movement-agent, avoidance, attachment, rotation, command, or selection residue.
- Five independent boundary runs passed `259/0` aggregate assertions. Save did not invoke stock `SaveRoutine`; load used the real exact Working descriptor and native prefix; area used direct pre-clean before real `ReloadArea`.
- Every run passed strict result validation and restored exact Mods, Working, protected-save metadata, process, lock, and sentinel state.
- Usable visual evidence is `MECHANICALLY VIABLE, NEW ANIMATION/POSE WORK REQUIRED`; the rigid upright rider and lower-body intersection are known work, not a fired visual kill criterion.

F1 completion evidence is bound to commit `d5bd7fa9c434f04c6f8487b61ea49e3cf983c397`, package SHA-256 `5ce3bd7d98a090ee05405cc4b4725fa58f13f1926958a69905ba478374c75a4d`, manifest SHA-256 `0494f1892b58f00b4e9fa36a0e33a0b16a3030840e6232d0f52503d49e8fa90b`, DLL SHA-256 `202701ed9232e4f5a4d5ff65c468c684d1ed1dd53e4a8030be93b960a7cd202a`, and MVID `a702808c-e8a0-4755-bc24-5ed4e945866a`. Earlier F0 evidence remains bound to its original artifact.

## Claim boundaries that remain in force

- Turn-based: `Direct HandleTurnBasedModeStateChanged(true) invocation only; native mode-event delivery was not exercised.`
- Realtime: `Direct HandleTurnBasedModeStateChanged(false) invocation only; native mode-event delivery was not exercised.`
- Save: `Direct GuardBoundary(SaveRequested) service invocation only; stock SaveRoutine and serialization were not exercised.`
- Load: `Real Game.LoadGame of the exact Working descriptor exercised the native LoadRoutine prefix; no UI load request was exercised.`
- Area: `Direct OnAreaBeginUnloading cleanup was latched before real Game.ReloadArea; native area-event delivery was not independently observed or qualified.`
- Camera-only screenshots do not prove portrait state, UI selection, or camera-follow behavior. Some later-state frames are black/unusable.
- Phase 1 does not prove mounted attacks, targeting, reach, action economy, persistence, enemy riders, or production-quality animation.

## Required Phase 2 authorization boundary

Before any Phase 2 execution, a separate mission must explicitly authorize and bound:

1. an original pose/animation tranche for the Mammoth rider, without importing or redistributing proprietary assets;
2. direct UI/portrait/selection-circle/camera-follow observation;
3. persistence, uninstall, and orphan-repair policy before any relationship is serialized;
4. the first narrow combat contract, with new kill criteria and rollback gates appropriate to attacks, targeting, and action economy; and
5. guarded A/B runtime evidence and exact protected-save/Mods restoration for each added behavior.

## Phase 2 boundary

Architecture B is the final Phase 1 recommendation. The presentation limitation remains `MECHANICALLY VIABLE, NEW ANIMATION/POSE WORK REQUIRED`, and the bounded claim limits above remain in force.

This document is a recommendation record, not execution authority. Do not execute Phase 2 or create a public release without a separate user-approved mission.
