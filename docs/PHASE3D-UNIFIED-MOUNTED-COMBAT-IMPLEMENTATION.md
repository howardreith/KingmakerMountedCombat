# Phase 3D Unified Mounted Combat Implementation

Status: IN PROGRESS — production and runtime automation are offline-green; fresh guarded runtime qualification is pending

## Scope and identity

Phase 3D starts from accepted Phase 3C HEAD `63595b832f7f89c854edef5a9eb4d21dee026590` on `codex/mounted-combat-phase3d-unified-combat`. The production implementation checkpoint is `9bf83d21b5367e057130b41bf7379512a638c025`; the presentation checkpoint is `63d324d909ec333cd16cde5e942f419759faa664`. The runtime protocol/scenario checkpoint described here is not a runtime PASS until it is committed, guarded-published, packaged from clean HEAD, and exercised against the exact guarded Kingmaker fixture.

Current development version is `0.1.0-phase3d-dev.15`. The immutable dev.1 package failed before gameplay at nested registration admission. Dev.2 repaired that boundary and exposed the Horse presentation precondition. Dev.3 repaired the Horse coordinate boundary, then its first RT process admitted the exact native Rider Primary cast request while still on the first combat-entry frame and never reached ability delivery. Dev.4 repaired that native-shell boundary and proved exact delivery, but exposed an unrelated optional-telemetry reflection fault before the child attack could start. Dev.5 repaired that observer and proved one successful child attack with pair retention, then exposed an independent scalar-evidence serialization defect. Dev.6 completed the artifact and exposed four later diagnostic timing/contract mismatches. Dev.7 repaired those boundaries and passed every RT gameplay row through the adjacent ranged control, where a rider-cooldown-contaminated admission exposed one focused AoO-test uncertainty. Dev.8 proved that exact stock AoO chain and exposed a production point-move termination defect plus a ranged-variant diagnostic isolation seam. Dev.9 proved the production movement repair and exposed one later stock-melee target-reuse artifact. Dev.10 closed that isolated stock-melee boundary and exposed an earlier-than-execution Shortbow input-attribution gap. Dev.11 proved that exact native selection/target/request boundary and mounted Shortbow behavior. Dev.12 added exact post-cancel rule ownership but its fresh RT process stopped earlier at one nonterminal native stock-melee approach. Dev.13 proved that stall was not repeatable and passed every reached RT gameplay row through TB-to-RT, then exposed a native combat-Dismount delivery double gate. Dev.14 proved the Dismount repair and re-exposed the exact legal-range/native-move terminal mismatch on the first Rider Primary, plus an unready final unmounted Sling control. Dev.15 is the bounded terminal-boundary repair and readiness attribution checkpoint.

## Dev.10 RT melee proof and dev.11 Shortbow admission attribution

Clean guarded-published dev.10 commit `c3d0ad7bc26aedfed7e7ee00108a45d5dd61a804` produced package/manifest/DLL SHA-256 `2ccd8a29c982ae75f7849bbebbf172e2d554a6306fcc483d1d8d60911fb41933` / `cb53d8a2e68abe6d22ca087f9229df24ae73dc4a1f1309edca77c9a7dca780fb` / `e4500336170500f98cabedbd273969159d1618ec0757318975fe1eb9828d28ec`, MVID `2d1018d0-5fb1-4144-ac67-7c0f196a1a29`. Suite10 and its separate RT/TB/presentation WhatIf audits passed exact purity. RT A `20260901T214000Z-phase3d-dev10-rt-passA` is immutable aggregate `FAIL`; its mandatory postrun audit passed before inspection.

The isolated normal melee control passed: fresh target identity, readiness, native request/intent `1/1`, Horse approach, rider/Horse dispatch `2/1`, native attack/roll/damage `3/3/3`, zero duplicates, target survival, exact cancellation, and relationship retention. This closes the dev.9 target-exhaustion seam. The next long-range Shortbow click was reported handled but raised no current native attack request and created no mounted intent. Exact installed `ClickUnitHandler` uses the same handled return for nonattack selection and loot branches, so the old scenario could not distinguish a true attack admission from those paths after clicking the friendly Horse.

Dev.11 reasserts one exact selected rider and records the UI selection-manager instance, nearest selected rider, target visibility, direct-control, party, loot, hostility, combat-memory, weapon, command, animation, equipment, movement, intent, mode, relationship, and pose predicates for two stable frames. After the click it requires same-frame native request and intent deltas of exactly one. Failure is immediate and preserves input evidence; a later execution timeout preserves admission, movement, dispatch, and native-rule progress. The external validator and negative synthetic mutation reject principal/request ambiguity. Production `MountedCombatController`, child attacks, pathing, resources, initiative, lifecycle, pose, and assets are unchanged.

## Dev.11 ranged proof and dev.12 cancellation attribution

Clean guarded-published dev.11 commit `8bb663c241bac07815fa5b4583937a09ef31c958` produced ZIP/manifest/DLL SHA-256 `f3044a54b9c977fd1faf1937efab67da232f8eb298cba50c2da5a578c4fc2290` / `2510e68967faa33dd290d4ada0e8b0aec85fa0d3289c6f0cb4480016bf7f7f6d` / `92a3e357699b2eb14d2224a4e032fd1c0ca937125401e1335385961dda0f25f7`, MVID `15aadd32-5f61-46ac-9ae2-1815a22a515d`. Stable suite `20260901T234500Z-phase3d-dev11-unified-combat-suite11` and separate RT/TB/presentation WhatIf runs passed exact zero mutation. RT A `20260902T024000Z-phase3d-dev11-rt-passA` is immutable `FAIL` (`20/3` outer subscenarios, `64/4` assertions); mandatory audit `20260902T034000Z-phase3d-dev11-rt-passA-postrun-audit` passed before evidence inspection.

The dev.11 Shortbow gate succeeded exactly: one selected rider, exact selection manager and nearest selected identity, a visible hostile nonparty/nonloot/noncontrollable target at `19.0000019m`, one native request, one intent start, Horse-owned approach, two rider ranged attacks, zero Horse attacks, zero duplicates, native LOS/cover/concealment surfaces, exact cancellation, and one native hostile AoO rule/roll/damage control. Every mounted bow row passed. This closes the dev.10 admission uncertainty without a global click patch.

At melee cancellation the required two rider plus one Horse ordinary attacks were complete at frame `1691`. The ground command cleared intent exactly once, but by frame `1696` the probe had observed one more rider attack/roll/damage chain without another KMC dispatch. The old probe did not preserve `RuleAttackWithWeapon.IsAttackOfOpportunity`, so it cannot truthfully distinguish a late ordinary autoattack from a separately emitted native rider AoO. Dev.12 records every pair rule's frame, actor, weapon, iterative flags, and AoO identity; snapshots all rider/Horse raw and queued commands plus manual targets immediately before, immediately after, and five stable frames after ground admission; and accepts cancellation only when the non-AoO rule delta is zero. Any post-baseline rule not explicitly native-AoO remains a failure.

The later dev.11 exception is diagnostic and deterministic: ranged variants requested a `2.0m` target while `DiagnosticCombatTargetService` rejects placement below `3.0m`. Dev.12 uses a bounded `4.0m` request with the existing `0.5m` nav search, safely above the creation floor and still near enough to observe—but never require—one already-legal Horse primary. Crossbow/Sling no-forced-approach and rider-rule cardinality gates are unchanged.

## Dev.12 approach observation and dev.13 terminal attribution

Clean guarded-published dev.12 commit `58675c8bfaab3e6931ebc5aa5b6df26460fe1063` produced ZIP/manifest/DLL SHA-256 `097e7a1cf99e00255bb14ae7f1dd52e1ab404a9d28141d9280d27c837d8f1e49` / `68cd7edb652ec75acb6d25a7f81b54113b78a1b03bab967b642d08e08d1b72c4` / `2673cca3477174aaa520307995e5d9b0f077a16d8c203b29d97f61224001adf7`, MVID `51a20bee-1a0c-4af7-9135-501e4d856279`. Suite12 and separate RT/TB/presentation purity gates passed. RT A `20260902T072000Z-phase3d-dev12-rt-passA` is immutable uncredited `FAIL`; its mandatory postrun audit passed before inspection.

The native Rider Primary cancel, rejection, successful attack, post-movement retention, and explicit Horse Primary boundaries all passed. The isolated stock-melee target then passed stable readiness and an actual hostile click produced request/intent `1/1`. Its Horse-owned point move advanced `4.7231493m` but did not report terminal before `MountedPairAttackCommand` reached its unchanged `8.5s` execution bound. No attack rule fired. The target remained conscious and the rider was `1.70507455m` away when the outer leaf expired. Successful equivalent dev.10/dev.11 runs prevent attributing that single outcome to a production range or click defect.

Dev.13 serializes the full exact `LastOutcome` at the deadline and captures final rider/Horse/target positions, horizontal and mechanics distance, the outcome's pair radius and in-range comparison, both native LoS observations, mount movement-agent flags, and current raw/queued commands. Admission readiness also records the same distances and LoS without changing its existing gate. This is sufficient to distinguish a disconnected or LoS-invalid diagnostic placement from a pair-local native move that reached range without becoming terminal. No timeout, path, attack, resource, initiative, lifecycle, UI, pose, or asset behavior changes.

## Dev.13 RT proof and dev.14 native Move-shell repair

Clean guarded-published dev.13 commit `81be790e9dcc1a33e49caae5be6473a717ab3bba` produced ZIP/manifest/DLL SHA-256 `f9c8209b0752aa9ccf502fedd2075c081acc608dfbcd0fdf159bc6aa98a9b67a` / `8e9050e15ebe0490cbadf66ed40cf8c2ac6534735253b3a7db233e80cf13b5f6` / `7483c0ab30bb7b6d1c2e1669d966a70b84b9193b83b2786b3b3e4ebea410996f`, MVID `dceb55e1-f4ff-4bae-986b-2633a27252ff`. Suite13 and separate RT/TB/presentation purity checks passed. RT A `20260902T102000Z-phase3d-dev13-rt-passA` is immutable uncredited `FAIL` (`29/2` outer, `73/2` assertions, `28/1` Phase 3D rows); independent audit `20260902T113000Z-phase3d-dev13-rt-passA-postrun-audit` passed before evidence inspection.

The prior stock-melee stall did not reproduce. Rider Primary cancel, rejection, success, post-movement, and post-transition retention; explicit Horse Primary; normal melee click/approach/repeat/cancel; separate ledgers; invalid-target feedback; all mounted Shortbow controls; native adjacent ranged AoO; Light Crossbow; Sling; single rider tracker/initiative/portrait; and RT-to-TB-to-RT reconciliation all passed. The only Phase 3D failure was the later combat Dismount delivery.

Mount Companion and Dismount use exact native `CommandType.Move` shells. Installed `UnitActionController.TickCommand` calls `UpdateCooldowns` on the false-to-true acted transition, while KMC custom delivery may occur afterward. Dev.13 showed a Move-ready rider admit exactly one Dismount cast, then delivery rejected after the native shell committed its own resource. Dev.14 adds `NativeMoveActionShellAdmitted` only to the exact Mount/Dismount delivery context. It bypasses only that duplicate Move-resource recheck; pre-input Move availability and all non-resource gates remain unchanged. The native shell is still the sole cost owner. New evidence requires the real Move slot, `IgnoreCooldown=false`, exactly one accepted relationship-ending activation, rider Move cooldown `2.5..3.01`, and no residual intent or mounted command. Fresh clean-package qualification is pending.

The complete dev.14 pre-commit gate passes source `22/0`, clean Release, component `297/0`, visual/source-order `18/0`, harness/protocol `237/0`, exact assemblies `382/0` (`358` Kingmaker + `24` Wrath), PowerShell parser `26/0`, JSON parser `7/0`, diff, and prohibited-payload validation. Pre-commit DLL SHA-256/MVID are `2222f84a70ff7838fbf01cc625bbdae6ebb45a661e6908e28072653fe10e4930` / `56601a6d-dfbb-4969-959d-313cac661f70`; these are not package identity.

## Dev.14 Dismount proof and dev.15 exact legal-range terminal disposition

Clean guarded-published dev.14 commit `0c27307999703bb69aad6b3f0d5c5141d1f5b570` produced ZIP/manifest/DLL SHA-256 `9910db0341796e3008e8cc622c12d9c967bbc028c35e284c82a907ce3e10169c` / `0c7ce385dc90b5786e96a4ba756592d9ef9f0502be894f90926149d0a3aab2d4` / `2222f84a70ff7838fbf01cc625bbdae6ebb45a661e6908e28072653fe10e4930`, MVID `56601a6d-dfbb-4969-959d-313cac661f70`. Suite14 snapshot SHA-256 is `4ce7f8e57a768f9edf5617703aca9f053600cd6ee300b709107e7c1b055038fc`; separate RT/TB/presentation WhatIf checks passed exact zero mutation. RT A `20260902T153000Z-phase3d-dev14-rt-passA` is immutable uncredited `FAIL` (`28/4` outer, `72/6` assertions, `27/3` Phase 3D rows); its independent restoration audit passed before evidence inspection.

The native combat-Dismount repair succeeded. One selected mounted rider with Move available admitted a real `CommandType.Move` `UnitUseAbility`, delivered exactly once, changed the relationship to `Unmounted` with `CleanupTrigger.Manual`, and had Move cooldown `2.97256064` afterward. Rider Standard remained spent; no active mounted command, stock intent, raw pair slot, or queue remained. The diagnostic's `inMoveSlot=false` was not delivery failure: installed `UnitCommands.Move` returns raw slot 3 only when its active object casts to `UnitMoveTo`, whereas Dismount is `UnitUseAbility`. Dev.15 observes the authoritative `GetCommand(CommandType.Move)` raw slot. Production Dismount code is unchanged from the proved dev.14 repair.

The first Rider Primary retained `Mounted` and emitted no relationship-end or cleanup record, but ended at the unchanged bound before child start. Its Horse moved `6.125914m` from an initial pair distance of `6.47200775m`; the exact delegated move remained nonterminal, so rider/Horse native rule counts were zero. The same process later completed post-transition Rider Primary and passed ordinary stock melee, persistent cancellation, every mounted ranged control, and the shared initiative/portrait transition rows. This isolates command terminalization, not the reported dismount regression or hostile-click admission.

Installed `UnitMoveTo.OnAction` returns `Success` only once stock approach/start/action progression reaches that method. KMC's `MountedPairSingleAttack.IsPairEnoughClose` independently proves exact mount-origin reach plus rider LoS. Dev.15 makes that child boundary authoritative for ending only KMC's reference-identical delegated point move: if the move is unfinished after range/LoS becomes true, it captures the pre-stop result and mechanics distance, interrupts/removes that exact move, proves the Move slot and queue restored, and advances the unchanged transaction. If the native move finishes first, `Success` remains mandatory. Telemetry and schema `53/54` require exactly one of those terminal boundaries and reject an outside-range, duplicate, missing, wrong-result, or residual-field claim. No range/LoS relaxation, teleport, timeout extension, global movement patch, or action-resource rewrite is present.

The final dev.14 unmounted Sling leaf began directly after a weapon swap and timed out without a rule event. Because it captured neither stable equipment/Standard readiness nor the raw stock command boundary, it receives no regression credit. Dev.15 waits for two stable unmounted RT frames and records the actual click plus rider-owned `UnitAttack` in the Standard slot. This is diagnostic only. The complete dev.15 pre-commit gate passes source/prohibited payload `22/0`, clean Release, component `298/0`, visual/source-order `18/0`, harness/protocol `237/0`, exact assembly `382/0` (`358` Kingmaker + `24` Wrath), PowerShell parser `26/0`, JSON parser `7/0`, and diff. Pre-commit DLL SHA-256/MVID are `976685fbc45018477376b20f05904e9760114ab1766aae6d971d971d1238c5c8` / `24af2d25-258f-47c9-941a-7a195c2d6941`; clean-package runtime qualification remains mandatory.

## First runtime checkpoint

Dev.1 package/manifest/DLL/MVID were `28406ec450f60cd6e426be7b87f9146a7f0609df9dc85f014b584ba7754752d9` / `3c262539e6c5276bfa1821c15132d7c1c7ecdc91f6eec963b6eec78f54345919` / `38df7b7b177589d6ea8fcdae9b47e59a885979790cb164a5fd2c10866bd84126` / `8dbda12f-ce20-40b2-88e9-49d63e8fd208`. Suite `20260830T230000Z-phase3d-dev1-unified-combat-suite1` passed RT/TB WhatIf and independent audits. RT A `20260830T233000Z-phase3d-dev1-rt-passA` is immutable uncredited `FAIL 3/1` at scenario start: the registration prerequisite rejected the Phase 3D name, so no Phase 3D artifact or gameplay row was produced. Mandatory restoration audit passed before evidence read. This result provides diagnostic attribution only.

## Dev.3 RT attribution and dev.4 bounded repair

The clean guarded-published dev.3 package has ZIP/manifest/DLL SHA-256 `70d6e373464ba805c31d9e08db6dd9f241918fdacd3ecfed296915167981020a` / `8fcf83cc33ceed6c2f4e9e87f5c86fe69bb451e03903cc52ed52efc0283640a4` / `80b20bf02c836946dbd24a44890a91a1e970ff0811d00080b42fbac8e29a0d77`, MVID `3b980b66-be62-4397-9a1a-b1ce5292d9e8`. Separate RT/TB WhatIf and independent audit passed. Its RT A run admitted one exact hostile Rider Primary selection and cast request with zero native refusal, while cancel and invalid-target controls retained the pair. No dispatch, child command, attack, or cleanup followed; the pair remained mounted until the exact leaf deadline. The run and its postrun restoration are immutable, but its gameplay result is FAIL and uncredited.

Exact installed assembly inspection attributes two contributing boundaries. The diagnostic clicked before it had proved all native RT `ShouldStartCommand` gates (`0x0600911F`). Independently, `UnitUseAbility.Init` (`0x06002728`) initialized the custom intent shell with `NeedLoS=true`, permitting the shell to seek pre-dispatch approach through the disabled rider mover even though KMC's child transaction owns mount movement and final attack LoS. Exact `UnitActionController.UpdateCooldowns` (`0x06009120`) also maps an acted RTWP `Free` shell to rider Move cooldown; leaving that stock asymmetry in place would make one explicit primary consume both the shell's rider Move and the child actor's Standard.

Dev.4 therefore waits for two stable frames of exact rider/Horse action, initiative, command, hands, equipment, selection, target, and transaction readiness. An exact-token `UnitUseAbility.Init` postfix then sets `NeedLoS=false` and `IgnoreCooldown=true` only for the reference-identical KMC Rider Primary/Mount Primary shell cast by the exact active rider in the exact mounted relationship. Mount, Dismount, foreign abilities, wrong casters, and unmounted state are untouched. The shell remains a real native Free input command but owns no action resource; the actor-owned child `UnitAttack` is the sole Standard owner and still applies native range/LoS/weapon/rule behavior. Fresh clean-package runtime evidence is mandatory before this repair receives qualification credit.

## Dev.4 delivery proof and dev.5 observer repair

Clean guarded-published dev.4 commit `9a26f5103c0e9a1692af819e93640c91c806b5dc` produced package/manifest/DLL SHA-256 `ce29d2a3c15f6a640642893d47e4dabcbfe16388b2dbcdcc7b182dc81a5ed695` / `f26b31f2735ba0d6ce464f38cd060fd8c48dbb800df8b8155edb8d7b123f164d` / `49e0d0e22a9cccd2747d2764451b4a2d9ac128cb43dddde91a5c1e7bed9c14a1`, MVID `5c5d523a-3c48-4129-9cd0-0cff0d9c53e8`. Stable suite `20260831T132000Z-phase3d-dev4-unified-combat-suite4`, snapshot SHA-256 `cd4edee8a810a891d3d917b2a43f11723a9baf937106a374899b53b83d3d8636`, passed distinct RT/TB WhatIf and an independent presentation-path zero-mutation audit.

RT A `20260831T161500Z-phase3d-dev4-rt-passA` is immutable uncredited `FAIL`. Its mandatory independent postrun audit passed before evidence inspection. Exact native evidence proves the dev.4 repair succeeded at its boundary: the hostile Rider Primary click produced one selection start/end, one cast request, one shell preparation, no refusal, a rider-owned Free-slot shell with `NeedLoS=false`, `IgnoreCooldown=true`, no cooldown, `CanStart=true`, and terminal `started=true`, `acted=true`, `finished=true`, `Result=Success`. The service then recorded `DispatchStarted`, primary delivery, an accepted KMC RiderMelee command, and `DispatchCompleted`; the relationship remained mounted through that activation.

The child failed in `MountedPairAttackCommand.CreateAndValidateChildAttack` before native attack start. Optional ammunition/reload description called `Type.GetProperty(name)` on Kingmaker's inherited weapon surface; the runtime threw `AmbiguousMatchException` before the method's getter-only catch. This observation-only fault then triggered the legitimate exception cleanup path. Dev.5 replaces that ambiguous lookup with `OptionalPublicPropertyReader`: it walks public declarations from the most-derived type to its base types, ignores indexers, catches both discovery and getter failures, and truthfully returns unavailable when foreign/dynamic state cannot be read. It cannot select a weapon, admit a command, alter range/LoS, consume an action, or change a rule event. A deterministic regression reproduces hidden inherited property names, requires the most-derived readable value, proves fallback after a throwing getter, and proves absent values remain absent.

Clean guarded-published dev.5 commit `10d1d3ef17ed032d35dabed51cb28d63076de304` produced package/manifest/DLL SHA-256 `cffd518a9c81386631024e3065067a0e0df376945cb5895f2c5041990ad970b4` / `dcdbf69ed7fb212fc3b5da56b99c23a4f6177d187974fa5fc32c1296badc0bfc` / `7e3d6f5ba3cf445a3f809e15fb57e6927c0d8d2f913de8149e1f7b5e6ef2cd8c`, MVID `b22d25e6-1197-4a87-a1e7-b26e8f82fca5`. Stable suite `20260831T181500Z-phase3d-dev5-unified-combat-suite5`, snapshot SHA-256 `8dee138522f9d4a6463f4f9afd88d0f6906a486b074c66aa4868db83439314ab`, passed separate RT/TB WhatIf and an independent presentation-path zero-mutation audit.

RT A `20260831T212000Z-phase3d-dev5-rt-passA` is immutable uncredited `FAIL`; mandatory independent postrun audit `20260831T220000Z-phase3d-dev5-rt-passA-postrun-audit` passed before evidence inspection. The live command prepared/delivered one exact native Rider Primary, accepted RiderMelee once, completed `Success` with one child attack and zero repaths, emitted no relationship-end activation record, and remained `Mounted` through command terminal. The subsequent diagnostic `CaptureOutcome` called `JObject.FromObject` on `CapturePresentationObservation()`, whose declared and actual contract is scalar `string`; Newtonsoft correctly rejected a scalar where `JObject` was required. Dev.6 preserves that scalar directly as a `JValue` and wraps it only where row evidence requires an object. Source regression forbids all three erroneous scalar-to-`JObject` sites. No gameplay, action, turn, movement, rule, lifecycle, or presentation behavior changes.

The complete dev.6 offline gate passes source `22/0`, Release, component `294/0`, visual/source-order `18/0`, harness/protocol `237/0`, assembly contracts `380/0` (`356` Kingmaker + `24` exact Wrath), PowerShell parser `26/0`, JSON parser `7/0`, diff, and prohibited-payload validation. Guarded-published commit `e9db5db07c9bcd33904a485bdf076eb87c22784b` produced package/manifest/DLL SHA-256 `6ef6cb633df1f932ef2f0a9969df551e383025b4c9305dad4ea3dae5ff026582` / `daf1e0892158c572409c7cb8e7dbbd16e503887c635b8d59efc33d1d05cd1848` / `115b6e9f88bb0a7a12be6bfb11a6e1f111131771f7eff1395262e9bf29ee266f`, MVID `aaee9cb1-59f9-4a6b-999c-43424dbf2a2e`. Stable suite `20260831T231000Z-phase3d-dev6-unified-combat-suite6`, separate RT/TB WhatIf, and independent presentation audit passed exact zero mutation.

Dev.6 RT A `20260901T015000Z-phase3d-dev6-rt-passA` is immutable uncredited `FAIL` (`18/11` outer subscenarios; `17/10` Phase 3D rows). Mandatory postrun audit `20260901T022500Z-phase3d-dev6-rt-passA-postrun-audit` passed exact restoration before evidence inspection. The run proves native Rider Primary retention, explicit Horse Primary, stock melee approach, two rider plus one Horse melee dispatch with zero duplicates, long-range Shortbow approach/autofire/cancel, no long-range Horse melee, Shortbow/Light Crossbow/Sling native attacks, rider initiative authority, and exact relationship retention. Its remaining failures are attributable to the diagnostic: it waited until the repeated melee target died before attempting explicit cancellation; it contradicted the user contract by rejecting one Horse primary already in legal melee; it inspected ranged AoO before native engagement/tick completion; and it constructed `InitiativeTrackerVM` before a current turn existed, then requested the rider turn while Kingmaker still held its native next-unit handoff.

Dev.7 observes the second repeated melee dispatch while intent is still active and cancels that command with ordinary ground input. It requires exact adjacent-hostile engagement, opportunity count, threat hand, memory, and action readiness before ranged fire, then waits for exactly one native AoO attack/roll/damage chain. Long-range ranged intent still requires zero Horse dispatch; adjacent ranged variants permit at most one Horse primary only when native melee readiness was true and Horse post-admission movement remained at most `0.25m`. RT-to-TB evidence now waits for a real native current turn, makes at most one public rider-turn request, requires a stable rider turn, and only then constructs the exact tracker VM and activates Rider Primary. The complete dev.7 pre-commit gate passes source `22/0`, Release, component `294/0`, visual/source-order `18/0`, harness/protocol `237/0`, assembly contracts `382/0` (`358` Kingmaker + `24` exact Wrath), PowerShell parser `26/0`, JSON parser `7/0`, diff, and prohibited-payload validation. Dirty-build DLL SHA-256/MVID are `d16e472c56f59532362ce1822688fef5f232f5a979823d713840b1c363408d13` / `f9f2aa4a-d38e-4b90-b049-a3383a332cb2`. Fresh clean dev.7 evidence remains mandatory.

Dev.7 clean package identity is ZIP/manifest/DLL `d2eaf37cfff0ebc49b34e854b2c8e26979e2aa9f276334b5f0bd278cd3d7a9a5` / `14be90cdc13dd37037aedec69cba66a8a22057d7aa17d7aba18c5dc2870c6bbc` / `d16e472c56f59532362ce1822688fef5f232f5a979823d713840b1c363408d13`, MVID `f9f2aa4a-d38e-4b90-b049-a3383a332cb2`, commit `c1f8e460cc5f4766b41eb5860eed2df181e20327`. Audited RT A `20260901T064000Z-phase3d-dev7-rt-passA` passed `18/19` Phase 3D rows; its only leaf deadline followed repeated legal adjacent Horse/rider dispatches. Exact Kingmaker code establishes that ranged `RuleAttackRoll` queues provocation for a later engagement tick. Dev.8 waits for an idle rider-first admission with native non-mutating AoO simulation success, proves the roll is `Ranged` with `DoNotProvokeAttacksOfOpportunity=false`, requires exact stock counter consumption and `1/1/1` hostile rule cardinality, and cancels before Horse dispatch. This is the only repair cycle for that newly exposed control; failure under those exact gates becomes an architecture disposition.

## Dev.8 RT attribution and dev.9 bounded repair

Clean guarded-published dev.8 commit `ec1282042a5fbe60e990ffbdaefbbf9b28e7cc21` produced ZIP/manifest/DLL SHA-256 `0d1c2bae5adccc3426e0b35d8cfe2040d91c8cf514d31b112f8b163b49f9e238` / `9a19609c6031c9b776248762543b97657cdd24b5e959fc4b9fb91139f59646d7` / `b39d67199be4aa773c25d02778c07f726e9e9457879dba65ca77923f88614afa`, MVID `36b8c5f0-7e7f-46f4-8091-a1c8b8e0550b`. Stable suite `20260901T085000Z-phase3d-dev8-unified-combat-suite8` and its separate RT/TB/presentation WhatIf audits passed exact purity. RT A `20260901T120000Z-phase3d-dev8-rt-passA` is immutable uncredited `FAIL`; mandatory postrun audit `20260901T130000Z-phase3d-dev8-rt-passA-postrun-audit` passed before evidence inspection.

The focused adjacent-ranged row passed the complete native contract: one real request and intent, one rider ranged dispatch, no Horse dispatch or movement, zero duplicates, `DoNotProvokeAttacksOfOpportunity=false`, one hostile attack/roll/damage chain, exact hostile-to-rider identities, and opportunity-count consumption. This establishes that mounted rider ranged attacks retain stock AoO behavior without a KMC global rewrite.

Rider Primary also established that the reported relationship regression is repaired: native ability delivery and KMC RiderMelee admission occurred, every non-lifecycle activation retained the exact mounted pair, and no cleanup trigger or relationship-end event fired. The row nevertheless timed out before child attack because the Horse's delegated point-target `UnitMoveTo` used `NeedLoS=true`. Exact installed `UnitCommand.IsUnitEnoughClose` then evaluated LoS against object ID zero; the hostile at the destination remained an unignored blocker even after `6.1207m` of valid mount-owned approach. Dev.9 makes only the delegated point move LoS-free. `MountedPairSingleAttack.IsPairEnoughClose` continues to require native rider-to-hostile `HasLOS`, so attack admission, range, cover, concealment, weapon, and rule ownership remain unchanged.

The dev.8 Light Crossbow control admitted one native request/intent and one rider child, spent rider Standard, allowed one already-legal Horse primary, and moved the Horse zero distance, but its reused target/rule window observed extra matching events. Sling followed an immediate equipment swap and no exact current native request was observed. Dev.9 destroys and verifies the preceding target, equips the exact weapon, creates a fresh target, and waits for two stable frames of exact weapon, relationship, selection, combat-memory, command, animation, equipment, ledger, and intent readiness for each variant. Evidence must bind distinct target IDs, one native request, one intent, one child, and exact rider/Horse rule cardinality. These are diagnostic isolation changes; production ranged routing is unchanged.

The complete dev.9 offline gate passes source `22/0`, Release, component `295/0`, visual/source-order `18/0`, harness/protocol `237/0`, and exact assembly contracts `382/0` (`358` Kingmaker + `24` Wrath). Dirty-build DLL SHA-256/MVID are `54e0aa8210ee8ec020a7c025b099e126ac6f61ec84f60cf7aafe8d049befc0f2` / `85f6b0c2-0b98-4a1a-b7dc-d17c8a9d9699`. Fresh clean-package runtime evidence remains mandatory.

## Dev.9 RT attribution and dev.10 diagnostic isolation

Clean guarded-published dev.9 commit `a6b28f35eac67ccdbfd7f412ab9e09b216eb5afe` produced ZIP/manifest/DLL SHA-256 `c01510db56b22c558cf3113b07ee2a221c5dae07890cc7cd3487e544247d4678` / `ebdf8a43b497189809fe6c5157a47384355cf56937925ae556c674eafe8ee331` / `54e0aa8210ee8ec020a7c025b099e126ac6f61ec84f60cf7aafe8d049befc0f2`, MVID `85f6b0c2-0b98-4a1a-b7dc-d17c8a9d9699`. Suite `20260901T133000Z-phase3d-dev9-unified-combat-suite9` and its separate RT/TB/presentation WhatIf audits passed exact purity. RT A `20260901T163000Z-phase3d-dev9-rt-passA` is immutable uncredited `FAIL`; mandatory postrun audit `20260901T173000Z-phase3d-dev9-rt-passA-postrun-audit` passed before evidence inspection.

The dev.9 production repair succeeded exactly. Rider Primary retained the pair, completed one native rider attack after one terminal Horse-owned point move, charged only rider Standard, and emitted one rider attack/roll/damage chain. The Horse moved `6.673599m`, reduced pair-target distance from `6.467118m` to `1.61881852m`, and reported no queue replacement or pose defect. Explicit Mount Primary subsequently completed one native Horse Bite on the Horse Standard ledger with no rider attack and no dismount. These results close the point-move and Rider Primary production uncertainties at their exercised boundary.

The next normal hostile click also crossed its real native admission boundary and completed a rider attack, but the diagnostic had retained the exact target already struck by the forced-natural-20 Rider and Horse explicit controls. That target has a bounded 128-temporary-HP lease; after prior damage `38 + 15`, the stock child exhausted the remaining observation window, target invalidation ended party combat, and persistent dispatch could not reach the two-rider/one-Horse predicate. Dev.10 therefore changes only the runtime scenario and strict evidence validator: it destroys/verifies the explicit target, spawns a distinct target six metres away, waits for two stable frames of exact selection/weapon/hostility/memory/ledger/command/animation/equipment/pose readiness, and binds one click/request/intent, positive Horse approach, matching dispatch and attack/roll/damage counts, zero duplicates, retained relationship, and distinct target IDs. A validator mutation proves reused or uncleared target evidence is rejected.

The complete dev.10 offline gate passes source `22/0`, Release, component `295/0`, visual/source-order `18/0`, harness/protocol `237/0`, exact assemblies `382/0` (`358` Kingmaker + `24` Wrath), PowerShell/JSON parsers `26/0` / `7/0`, and diff. Dirty-build DLL SHA-256/MVID are `e4500336170500f98cabedbd273969159d1618ec0757318975fe1eb9828d28ec` / `2d1018d0-5fb1-4144-ac67-7c0f196a1a29`. Fresh clean-package dev.10 runtime evidence remains mandatory.

## Implemented architecture

The implementation keeps the established boundaries rather than adding a synthetic actor:

- `UnifiedMountedTurnCoordinator` retains the rider as the real Kingmaker turn controller and initiative principal, skips only the exact active mount candidate, projects only the rider into `InitiativeTrackerVM`, and defers dismount split participation to a safe later round boundary.
- Rider and mount retain their own native cooldown/action ledgers. A natural rider-turn start prepares the mount ledger once; a mid-turn combat Mount merges the pair without clearing either ledger.
- `MountedCombatController` admits an exact player-created stock rider `UnitAttack` only after the native attack-request event, creates one bounded pair intent, gives physical approach to the mount, and dispatches native rider/mount attacks without a shared Standard pool.
- RTWP keeps the exact target intent through ordinary cooldown waits and successful attacks. Stop, ground click, a new target, target invalidation, lifecycle cleanup, or mode/turn termination cancels it, and duplicate-dispatch telemetry must remain zero.
- TB uses the same hostile-click path during the rider-led turn. It deterministically dispatches rider first and then the mount when each actor independently has a legal Standard action. Rider Primary and Mount Primary remain explicit single-actor alternatives.
- Ranged routing detects the exact native weapon blueprint rather than a bow allowlist. The mount approaches only to native rider range and line of sight and never closes farther merely to use its natural weapon. A Horse primary may occur only if the Horse is already in exact legal melee, or through explicit Mount Primary; the outside-range control requires zero Horse dispatch. Native `UnitAttack` remains responsible for ammunition, reload, attack rules, range, cover, concealment, and ranged AoO behavior.
- Combat Mount/Dismount requires the exact owned eligible adjacent pair, correct active rider turn, and available rider Move action. Its exact native Move shell charges only rider Move and preserves every already-spent rider/mount resource. Delivery recognizes that this same admitted shell may already have committed the cost; it never performs a second charge, refund, or refresh.
- Mounted five-foot movement uses Kingmaker's native five-foot mode and distance, transfers physical movement to the mount without charging ordinary Move, and suppresses disengage AoO only for the exact active mounted step. Ordinary mounted movement and every unmounted unit remain on the stock AoO path.
- The Phase 3C separate-turn behavior remains available behind `EnableUnifiedMountedTurn=false` as the fail-closed fallback required by the mission.

## Rider Primary P0

Phase 3C's `UnitMoveContiniously.Init` prefix treated a pair-member continuous-movement command as `UnexpectedCommand`, which could end the relationship while Rider Primary was selecting, approaching, or resolving. Phase 3D delegates rider continuous movement to the mount and cancels only a superseded stock-attack intent; it does not invoke relationship cleanup.

The activation ledger correlates a monotonic activation sequence with ability owner, selected unit, target mode, relationship state, pair/view identities, lifecycle deliveries, combat/mode transition, cleanup trigger, and terminal result. Fresh RT/TB hit, rejection, target-cancel, post-movement, and post-mode-transition scenarios must all retain the same relationship. Automatic remount is not used.

## Original presentation assets

The exact Kingmaker bundle inventory did not expose a safely reusable saddle/tack icon, so Phase 3D uses original KMC-owned artwork:

- `MountSaddleIcon.png`, 128x128, is used only for Mount/Dismount and communicates tack rather than Horse identity;
- `HorsePortraitSmall.png`, 185x242, is a tighter original close-up derived from the KMC-owned Horse master; large and medium Horse portraits remain unchanged;
- the accepted Horse procedural pelvis/leg pose remains local `Y=-0.17`; the final lowering is exact Horse mount-root-local `Y=-0.08`, applied after source-anchor resolution so animation-driven pelvis axes cannot turn a vertical product decision into a diagonal displacement;
- all Mammoth profile constants and resources remain unchanged.

The image-generation skill supplied the original KMC saddle master and the KMC-master-derived small portrait crop. No Wrath, Kingmaker, YouTube, screenshot, or third-party artwork is embedded or redistributed. Exact prompts, dimensions, rights boundary, and hashes are recorded in `planning/MOUNT-ICON-ASSET-AUDIT.md`.

Dev.2 runtime attribution is exact: `Pelvis.localPosition Y=-0.29` produced left/right stirrup distances `0.491643876/0.5325693` despite zero clamps and microunit solver residuals. The second value truthfully failed the existing `0.5` gate. Dev.3 restores the Phase 3C procedural pose and moves the owned Horse rider root by `(0,-0.08,0)` in the stable mount-root frame. Mammoth root offset is `(0,0,0)`. The gate is unchanged, and actual saddle contact/clipping remains manual review.

## Strict runtime tranche

`Phase3dHorseScenarioTranche` adds three guarded outer scenarios and one immutable `phase3d-horse-scenario-evidence.json` artifact:

- `phase3d-horse-presentation-suite` binds the exact asset dimensions, rider principal, and Horse pelvis profile;
- `phase3d-unified-combat-rt-suite` exercises real native Rider Primary target selection/cancel/rejection, persistent melee intent, ranged Shortbow approach/autofire/cancel, adjacent native ranged AoO, Light Crossbow and Sling observations, RT-to-TB-to-RT reconciliation, and unmounted melee/ranged controls;
- `phase3d-unified-combat-tb-suite` begins unmounted in combat, proves three combat-Mount resource states, exact tracker projection, explicit and stock pair attacks, ranged rider-only dispatch, mounted five-foot and ordinary-move AoO controls, combat Dismount, and an unmounted five-foot control.

The external validator requires the exact row set for each suite, reconciles every row with the final protocol result, rejects unknown/duplicate rows, rechecks immutable artifact bytes, and applies semantic ownership/resource/cardinality predicates. Synthetic protocol tests construct valid evidence for all three suites and prove one targeted mutation per suite is rejected.

## Qualification boundary

Offline compilation or synthetic protocol evidence does not prove Kingmaker control usability. Runtime credit requires a clean guarded package, stable-suite admission, separate mode `WhatIf` checks, independent zero-mutation audits, actual native input admission, physical movement/attack outcome, exact rule-event cardinality, and exact cleanup. Pointer feel, tracker rendering, portrait/icon readability, and final Horse seat/clipping remain focused human gates.

No Paladin Divine Steed, mounted spellcasting, mounted feats/charge, persistent relationship, automatic remount, additional mounts, Small riders, enemy riders, public release, or `main` merge is implemented or authorized.
