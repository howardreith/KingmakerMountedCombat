# Phase 2 qualification

Status: IN PROGRESS - COMBAT QUALIFICATION AUTHORIZED

Current combat credit: stationary mounted rider melee hit in real time is PASS A/B. Fresh same-package runs `20260816T084500Z-rider-hit-rt-probeQ` and `20260816T090000Z-rider-hit-rt-passB` each pass `47/0` from guarded-published commit `07d44eb89c6a533bc99e386bad6fd3dac4a9ac73`, package/manifest/DLL SHA-256 `af4855f93e31084b2406a2b9e527b5d8587c5a7b82a52d61bb3863d7af777459` / `10b8bae35848e919259839c4af9aaa265a6762b247b7b3a781a1f836ea42076a` / `e3ca251a6a04da607f9fb6bc21605d32756e2648769fc341d6a6575b1616db09`, MVID `06e14d27-85a7-49f6-900c-94ca99f68f04`. Each independently proves one rider-owned native attack/roll/damage, eight damage, one rider Standard charge, zero duplicate pair attacks, zero repaths, exact transient-memory/target cleanup, accepted Mammoth-profile health, and exact external restoration. No turn-based, miss, invalid-target, target-death, non-mounted-control, or mount-attack credit is inferred.

Turn-based stationary rider melee hit is now qualified. Probes D-F remain preserved uncredited `FAIL` `50/1`; the exact repair they motivated is validated by fresh same-package runs `20260816T111500Z-rider-hit-tb-passA` and `20260816T113000Z-rider-hit-tb-passB`, each `PASS` `51/0` from clean commit/package `943156a7f580abf15e56dfd70a0a2b23688d335c` / `a87330d32836f7b359106df767832c15fd46849d87c2e08b2fe156b79cb143d0`. Both prove one rider-owned native attack/roll/damage, forced critical hit, correct rider-only Standard cost, zero duplicates/repaths, exact native turn identity, healthy pose, terminal `Success`, and exact external restoration. This closes stationary rider hit RT/TB only; the remaining Tranche C controls are still unqualified.

Stationary rider miss investigation preserved the actor-context package `40b6dad2e5cfb62d84b53c0297d73909ec00ddd4`, two `49/0` diagnostic passes (`20260816T151500Z-rider-miss-rt-contextprobe`, `20260816T153000Z-rider-miss-rt-contextprobeB`), and one uncredited `20/1` failure (`20260816T154500Z-rider-miss-rt-contextprobeC`). The failure proved the fixture's non-pair actor was in the exact rider/Mammoth player-party group, directly controllable, raw/effective AI enabled, and command-active when it performed two pre-dispatch attacks for total damage `45`. That attributable defect led to the diagnostic-only exact-member AI lease now qualified below under schema v18, with command-preserving acquisition, continuous validation, and exact restoration.

Stationary rider miss is now qualified by fresh same-package schema-v18 runs `20260816T152500Z-rider-miss-rt-ailease-passA` and `20260816T154000Z-rider-miss-rt-ailease-passB`, each `49/0` from clean commit/package `a552c64cea3cfe29706c1bac1b19acd37ef87704` / `9fa52901e14f3284260d66509094ed57de9e0dfffb3066826af09c48e95b09dd`. Both prove exact one-member non-pair command purity and raw/effective AI restoration, zero pre-dispatch interference, one rider natural-1 native miss, zero damage/duplicates/repaths, rider-only Standard cost, stable target, healthy accepted Mammoth pose, and complete cleanup. Independent audits before evidence inspection prove exact external restoration after both. This closes stationary rider hit RT/TB and miss RT only; Mammoth primary, movement-to-attack, cancellation, lifecycle/invalidation, reach, AoO, and charge remain open.

Mammoth-primary implementation is offline-green but has no runtime credit yet. The exact Mammoth owns its Standard wrapper, command queue, native child, rule initiator, weapon, target, and resource delta; the rider remains selection principal and must retain unchanged Standard/Move state. An explicitly armed Mammoth native turn is admitted only for that command and then ended without unused Move/Swift charging. Schema v20/v21 and deterministic RT/TB fixtures bind exact command/resource/weapon/turn identity while retaining historical schema-v1-v19 rider validation. Current offline totals are source `21/0`, Release build PASS, component `203/0`, visual/source-order `17/0`, harness/protocol `173/0`, and assembly contracts `232/0`. Fresh same-package RT A/B and TB A/B plus independent restoration audits remain mandatory.

First Mammoth RT attempt `20260816T164500Z-mammoth-primary-rt-passA` is preserved uncredited `FAIL` `33/1`. Before click or any combat action, the final range check observed exact Mammoth natural radius `3.589406` but target distance `3.06238842`, outside the required near-boundary inset. Restoration audited exact before evidence inspection. The repair revalidates at final dispatch and moves only the disposable diagnostic target when that exact actor-specific drift is proven; the same strict range assertion remains. Deterministic regression covers the failed values. Repaired offline totals are `21/204/17/173/232`; fresh clean-package RT A/B and TB A/B remain required.

Second Mammoth RT attempt `20260816T170000Z-mammoth-primary-rt-passA` is preserved uncredited `FAIL` `48/3` from clean guarded-published commit `622fe5fd9353ca881207cc8d18b0937c3f7921c3` and exact package `0603768e00d0f84e522fbb6e00a7c2e8c261f7f7552cce2d495bbd777a009301`. It proves repaired placement at radius/distance `3.589406 / 3.46940613`, accepted click, exact Mammoth actor and target, exactly one attack/roll/damage chain, natural `20`, native `CriticalHit`, damage `29`, no duplicate/AoO/charge/repath, Mammoth Standard `0 -> 5.10285139`, unchanged Mammoth Move and rider Standard/Move, and healthy accepted pose. Its stock one-HP target became `FinallyDead` before terminal animation completion, native teardown cleared the late planned-weapon reference, and death made the old combined stationarity assertion ambiguous. Independent restoration audited exact before evidence inspection.

The resulting narrow repair does not relax final-death or consciousness validation. Mammoth-primary diagnostic rows alone lease exactly 128 temporary HP on a target proven to begin at zero, then remove the exact modifier and re-prove zero before destruction. The command retains its validated planned-weapon identity through native teardown. Schema v22/v23 binds lease acquisition/release, a fresh Conscious/zero-transition outcome life snapshot, and separate rider/Mammoth/target displacement values each at most `0.05`; rider rows prove no durability lease, and schemas v20/v21 remain valid immutable history. Complete post-refresh offline gates pass `21/204/17/174/236`; dirty-build DLL SHA-256/MVID are `f46da7ff839c8ff35dd3f0cf8311b073e0f3c5a2649290db06208f6838b3d285` / `5398d6e5-d091-4dfe-9726-afc2bd5f348a`. No runtime credit is inferred.

Third Mammoth RT attempt `20260816T174500Z-mammoth-primary-rt-passA` is preserved uncredited `FAIL` `50/2` from clean commit/package `f7c41361473a9c1265e9c98046bc33ad94f8a4f1` / `e9944ba19ee4d3c6a3b56d1317ae6d54b67b02b17b3d29592f50b240c803641c`. The durability and terminal-identity repair works: the exact Mammoth makes one primary natural critical attack for 25 damage, the target remains Conscious with no life transition, only Mammoth Standard advances, rider resources remain zero, rider/Mammoth displacements are `0.03717125 / 0`, and all ownership, rule, pose, duplicate, AoO, charge, repath, lease-release, and external-restoration evidence is exact. The target alone moved `0.675754547`; cleanup cleared entity/group/faction/memory/durability/sleepless/non-pair-AI residue but left domain state `DestroyRequested`.

The installed movement contract makes both causes exact. `UnitMovementAgent.Stop` clears active and requested paths, destination, speed, and velocity; target provisioning previously interrupted commands after translocation but never stopped the stock agent. The repaired click gate requires target commands empty, agent enabled, no wanted/real movement, and exact zero speed/velocity after a target-only Stop. Cleanup now retries `ConfirmRemoved` only on a later call where every concrete residue flag is already true. No displacement, cleanup, pathfinding, action, or restoration gate is weakened. Offline totals are source `21/0`, Release build PASS, component `204/0`, visual/source-order `17/0`, harness/protocol `174/0`, and assembly contracts `240/0` (`229/0` Kingmaker, `11/0` Wrath). Fresh clean-package RT A/B and TB A/B remain required.

The exact Phase 2A build accepted by the user is commit `09a63729e0847c540ae7e79e9e3876d005ee9afe`, package/manifest/DLL SHA-256 `80b0498cd9d1e6d8dd518ffd8d343af56bcc711306492548c221b9559c98cef7` / `6ba6a3da57355d61e7d181d1a43738530646ebc31db7c8e7c890a3310d28ca00` / `68e71ff952b8ea48aa4c77479d968680837c54b975f94ae977cc872be485b097`, and MVID `f4347f61-7a6b-43c0-8c4c-ccacb0002944`. Human acceptance closes the presentation gate with the exact known limitations recorded in the Phase 2A report. Combat rows remain unqualified until their own deterministic and fresh-process evidence passes.

Phase 2 intake, transient persistence/native lifecycle, player action, all seven individual presentation rows, and the complete seven-row presentation suite are qualified twice in fresh processes. The exact review.2 package was created from clean guarded-published commit `9686105e6ebc7531764b2f614e9d323b6d427410`, passed package validation `10/0`, and passed the full-pin manual launcher `-WhatIf` with no mutation. Fresh same-package runs `20260815T143000Z-presentation-suite-review2-passA` and `20260815T142900Z-presentation-suite-review2-passB` each pass all seven rows at aggregate `381/0`. The earlier dev.12 `380/1` cold first-row failure and review.1 pre-mutation launcher-WhatIf failure remain preserved and uncredited. Only explicit human visual acceptance is open; no combat implementation or runtime row is claimed.

After those qualifying runs, explicit external user activity changed exactly `Quick_3.zks` and `Quick_438.zks`. This is now represented without save-write authority by append-only schema-v2 epoch `20260816T0115379973604Z-user-attested-quicksave-continuity`, SHA-256 `111c794baf8bd1062ef7ecf8c307f4e3badb7a519cf3e66ae1f8f2442b770701`, chained to immutable schema-v1 SHA-256 `77fd415d177423dd7da58e3685e418f38cf954dead75068424a717e2be6b3ca9`. The current inventory digest is `03a91757453a8e875e376f31f1664decb0a6e60dcf6433f2348795a90be634c9`; the current protected-pin-set digest is `6f137d57e3a519b3a315a4c654b5feee51c8495f7104c26bfd4da097930c9ff4`. `Quick_3.zks` uses only the exact authorized metadata-only prior marker; `Quick_438.zks` retains its known superseded hash and exact replacement hash. Baseline and `Auto_1120.zks` remain unchanged, both quicksaves remain non-writable, and `KMC_AUTOMATION_WORKING` remains the sole transaction write target.

The qualified review artifact is version `0.1.0-phase2a-review.2`, ZIP/manifest/DLL SHA-256 `266b007892ebe6e09ecd09612fd6fa5e8ce63e533da719cbeebdb2e1eec81604` / `b799af6d687047ec7da664a05b503eb229e7ae8e843e18d97371119a6427edec` / `68e71ff952b8ea48aa4c77479d968680837c54b975f94ae977cc872be485b097`, and MVID `f4347f61-7a6b-43c0-8c4c-ccacb0002944`. Each process reconciles 37/37 artifacts and 35/35 unique screenshots, reports zero position/yaw lag violations, zero stationary correction violations, exact final synchronization for every row, and zero pose/relationship/movement/selection cleanup residue. Pass A/B save-transaction state SHA-256 values are `59ce3a8e832b8726b468548e754566a175cacf14c8a920ab6e5603cda7ff3f5c` / `7f0fb58c5f365a0f7d497a5d130e105dde748df65e02b7eb7183207a29f63c7e`; exact combined state SHA-256 values are `7b1bc7468d1a3c3e127dcefcd4f20566a43eea5e00466f60ba9d60ae6edd71f8` / `b718902b9467bd6beb4f884bacf887e2a70c3e4904c13b49d64ab7d8dcdd3caa`. Both restore Mods `e62320bbe7d4b83edc128a62f9f5852b0669c5549859602c335c649318578d47` and protected saves `973f203e16aad29d7e4865bae6f81149d959a4ac179953c87781a06d1c71898d` exactly, with Baseline immutable and Working restored after its expected transient runtime modification.

The prior review.2 offline totals were source `21/0`, Release build PASS, component `152/0`, visual/source-order `17/0`, harness/protocol `144/0`, and exact assembly contracts `103/0` (`92/0` Kingmaker, `11/0` Wrath). The complete post-schema-v2 gate passes source `21/0`, Release build PASS, component `152/0`, visual/source-order `17/0`, harness/protocol `147/0`, and exact assembly contracts `103/0` (`92/0` Kingmaker, `11/0` Wrath); the rebound package identities are reported by the exact stop checkpoint. No gameplay validator, presentation threshold, runtime restoration gate, package allowlist, or save-write rule was weakened. Review.2 remains ready only for the guarded human checkpoint. The final Phase 2 ledger remains open for manual acceptance, combat qualification after acceptance, stretch disposition, and the final truthful private-alpha status.
