## Active Chunk 4 native boundaries — 2026-09-09

Exact Kingmaker SHA3b6450ffec440e296e586f71c711b195aed144b28d53e1cbb29406d18fef5afb / MVID07fa1e4d-8618-41b3-9b8d-faa17d3b26f7. Native H observes UnitAttack.OnTick06002680 -> UpdateTarget06002683 false -> Interrupt060027AC after three ranged deliveries when the remaining natural weapon is out of reach. Native full plan remains4, delivered3; source7 clears every nonSuccess stock intent. Preview.8's domain decision permits later RT ranged repetition only for this synchronous native range terminal; native weapon plan, individual reach, costs, animation lifecycle and released effects remain authoritative. Native control/rerun still required.

New actual incoming probes use the fixture's prepared CureLightWoundsCast5590652e1c2225c4ca30c4a699ab3649 and Entangle0fd00984a2c0e0a429cf1a911b4ec5ca, identified by G's native inventory. SpellSlot.Spell04001544/Available04001545 and Spellbook.GetAllMemorizedSpells06001E79 retain existing slots; no grants/resets. AbilityEffectStickyTouch.TouchDeliveryAbility04001CE4 and ContextActionSpawnAreaEffect.AreaEffect040018B9 identify native follow-on logic. PersistentState.AreaEffects04005339 and exact area Blueprint/UnitsInside/Context getters060082AB/AC/AE observe actual area inclusion, with no forced update calls. RuleHealDamage.OnTrigger0600740B, RuleSavingThrow.OnTrigger0600731C and RuleAttackWithWeaponResolve.OnTrigger060071A4 provide actual heal/save/delivery boundaries. The optional third native trace actor observes caster costs without changing rules. These are original adapters/observations, not mounted casting content.

UnitLifeController.TickOnUnit06009162 and OnUnitDeath06009168 own real incapacitation/death after labelled native RuleDealDamage; no final flag assignment or resurrection. Probes refuse essential/immortal/main-character subjects and retain actual death-policy settings, visual attachment cleanup, actor budgets, unrelated native successors and native callbacks. Current exact contract checks469/0; new incoming/life native qualification remains open. Local bounded IL remains analysis-cache only, outside Git and packages. Existing Wrath/Phase1 evidence is unchanged.
## Chunk 2 current contract - 2026-09-07 09:08 UTC

Exact Kingmaker assembly MVID07fa1e4d-8618-41b3-9b8d-faa17d3b26f7 remains authoritative. Prepare06000C3C clears/reapplies native acting-command costs, then OnNewRound0600939D precedes round/AI/fact/readiness callbacks. The narrow pair/owed-record correction at that round boundary replaces the too-late postfix; no transpiler or replay. Native7 R/S/T/U prove36 representative preparations with effects/order and correct movement floors. Pacing uses exact CanMoveInTurnBased060018A9 and fields04001191/040011AC/040011AE/040011B6/040011CB, preserving native restoration. Actor destruction/settled encounter/replacement session retire obsolete references; same-campaign debt persistence remains unqualified.

GetTimeToNextTurn0600837C equals initiative plus max(Standard,Move), used by native next-unit ordering/time. A04's old Standard before Horse preparation remains unresolved; global-round identity is not a grant and early preparation requires exactly-once continuation/participation. ASSEMBLY CONTRACT461/0; final A10 sourcec804ba0/preview10 PASS32/0 native cases. Native allocation proof remains frozen7; later changes concern diagnostic target durability/cleanup. [Concise contract and evidence](../docs/CHUNK2-ACTOR-ALLOCATIONS.md). Historical Phase1 contracts below remain unchanged.

# Assembly contract matrix

Round-2 follow-up pins the exact path-reuse boundary that the public graph queue does not expose. Public instance `Kingmaker.View.UnitMovementAgent.PathTo(UnitCommand,Vector3,Single,Single,UnitMovementAgentBase)` is token `0x060018A3`. Public static `Pathfinding.Util.TileHandler.LastUpdateFrame` is type token `0x020006C4`, property token `0x170005E6`, and getter token `0x060036E8`. Exact installed code suppresses a same-destination path request only when the agent's private request frame is strictly newer than `LastUpdateFrame` and `RepathNeeded` is false. `TileHandler.EndBatchLoad` advances `LastUpdateFrame` in an Astar work item after cut notification, while `AstarPath.IsAnyGraphUpdatesQueued` observes only its graph-update queue. KMC may observe Unity frame and `LastUpdateFrame`; it may not infer work-item completion from cut readiness or the graph queue, invoke `ForceUpdate`, write pathfinding internals, or relax path-quality gates.

Status: PASS

Round-2 distance-door qualification pins the exact installed post-open pathfinding boundary. `Kingmaker.View.MapObjects.StandardDoor.Open()` toggles state and, when `DisableNavmeshCutWhenOpen` is true, disables its child `Pathfinding.NavmeshCut`. `NavmeshCut.RequiresUpdate()` remains true until stock `Pathfinding.TileHandlerHelper.Update()` reloads the dirty bounds and calls `NotifyUpdated()`. The pinned first-pass assembly also exposes public static `AstarPath.active` field token `0x0400089B` and public instance Boolean `IsAnyGraphUpdatesQueued` getter token `0x06000517`. The diagnostic may observe those public readiness surfaces within a separate bounded deadline; it must not call `ForceUpdate`, relax endpoint/door-plane/path-replacement checks, or infer that synchronous door state implies completed tile-graph work.

Status: PASS

The Phase 1 responsibility map is complete. The settled forensics below are unchanged; the final runtime overlay records which bounded seams qualified and which mandatory fixture-dependent rows remain deferred.

Authority fingerprints are in `planning/ENVIRONMENT-FINGERPRINT.json`. Bounded decompilation is retained only under the ignored lab `analysis-cache`; this document records original summaries, signatures, and evidence.

## Classification

`DIRECT EQUIVALENT`, `OLDER EQUIVALENT`, `USABLE CONTROL POINT`, `PARTIAL PRIMITIVE`, `ABSENT — REIMPLEMENT`, `ABSENT — SIMPLIFY`, `UNSAFE GLOBAL SEAM`, and `UNKNOWN — MORE EVIDENCE REQUIRED`.

## Responsibility matrix

| Responsibility | Exact Wrath contract | Exact Kingmaker contract / absence | Classification | Proposed Phase 1 seam | Confidence |
|---|---|---|---|---|---|
| Relationship state | `UnitPartRider.m_SaddledUnitRef` field token `0x04008376` and `UnitPartSaddled.m_RiderRef` `0x04008382` are reciprocal `[JsonProperty] EntityPartRef` values. `Mount` `0x0600C263`; dismount path `0x0600C264`–`0x0600C268`. | Exact type inventory contains no rider, saddled, or mounted controller type. | ABSENT — REIMPLEMENT | Original runtime-only domain object, at most one pair, no entity part and no JSON annotation. | high |
| Pair validation | `AbilityTargetIsSuitableMount.CanMount` `0x0600D7FF` requires exact master/pet, consciousness, and no polymorph. Size checker `0x0600D803` requires mount size greater than rider. Controller revalidates both. | `UnitDescriptor.Master` and `Pet` are serialized reciprocal relations; `SetMaster` `0x06001F17`; `OriginalSize` `0x06001ED1`; current `UnitState.Size`; `IsDirectlyControllable` `0x06008328`; life/combat state. | USABLE CONTROL POINT | Adapter accepts only distinct, directly controllable, conscious, out-of-combat entities for which `mount.Descriptor.Master.Value == rider`, `rider.Descriptor.Pet == mount`, and `mount.State.Size > rider.State.Size`. | high |
| Player intent / command ownership | Rider retains the player command. `SaddledUnitController.TickDelegateRiderToMount` `0x0600AB06` creates linked mount commands; `UnitCommand.RiderCommand` / `MountCommand`; `AddRiderCommand` `0x0600CA6B`; `AddMountCommand` `0x0600CA6C`. | `UnitCommands.Run(UnitCommand)` `0x060026B2` initializes/owns commands and raises `IUnitRunCommandHandler`. `UnitMoveTo` is public. There are no paired-command fields. | USABLE CONTROL POINT | In Phase 1, intercept only ground/group movement before command creation and route one original `UnitMoveTo` to the active mount. Do not synthesize combat/action pairs. | medium-high |
| Ground destination fan-out | Wrath ground movement removes saddled mounts from the effective formation/command set and issues rider intent; controller delegates to mount. | `ClickGroundHandler.MoveSelectedUnitsToPoint` `0x060093DB` calculates formation, then private `RunCommand` `0x060093DC` constructs the exact `UnitMoveTo`. The public callback overload is not the ordinary click call site. | USABLE CONTROL POINT | Prefix only private `RunCommand`: replace an active-rider `unit` argument with the active mount; skip an active-mount duplicate row; return unchanged for every other unit. This preserves native speed, orientation, delay, marker, and `CreatedByPlayer`. | medium-high |
| Rider path suppression | `UnitCommand.TickApproaching` `0x0600CA7E` detects `Executor.RiderPart`, marks movement, and returns before `View.MoveTo`. | `UnitCommand.TickApproaching` `0x060027A6` always calls `View.MoveTo` for an approaching command. No mounted condition exists. | ABSENT — REIMPLEMENT | Do not run a rider `UnitMoveTo` at all in the movement-only slice; route to mount at command origin and interrupt any pre-existing rider move at transition. | high |
| Authoritative mover | Mount's ordinary `UnitMovementAgent` receives the delegated command. | `UnitMoveController.Tick` `0x06009183` uses `UnitEntityView.MovementAgent`; `MovementAgent` `0x1700051A` returns `AgentOverride` `0x17000519` or stock `AgentASP` `0x17000518`. A moving view agent advances, then its entity position follows its view. | USABLE CONTROL POINT | Mount remains an ordinary mover. Install one project-owned rider override that mirrors mount motion and lets Kingmaker's normal per-unit commit update rider entity position. | high for seam; tested-pair runtime PASS |
| Avoidance ownership | `UnitPartRider.SetAvoidanceEnabled` `0x0600C269` disables rider avoidance. `UnitMovementAgent.UpdateAvoidance` skips its own work when disabled; `UpdateUnitAvoidance` `0x06001EEF` also ignores avoidance-disabled other agents. | `UnitMovementAgent.AvoidanceDisabled` tokens `0x0600189B/9C` uses a `CountingGuard`; `UpdateUnitAvoidance` `0x060018B2` ignores disabled agents. Setter symmetry is reference-counted. | DIRECT EQUIVALENT | Set rider agent avoidance disabled once after snapshot; restore once during every cleanup path. Never disable mount avoidance. Track ownership so unrelated guards are preserved. | high |
| Navmesh clearance / footprint | Mount path uses mount corpulence; rider does not path. | `UnitMovementAgent.PathTo` uses `UnitEntityView.Corpulence * 2` as seeker clearance; ordinary `PartyFormationHelper` uses each view's corpulence. | DIRECT EQUIVALENT | Route only mount command; never reduce mount clearance. | high |
| Entity position semantics | `UnitMoveController.TickUnit` `0x0600ABAA` copies mount position/orientation to rider entity and rider view every tick, even stationary. | Kingmaker `UnitMoveController.Tick` `0x06009183` has no pair-sync block. It commits a moving view to `UnitEntityData.Position` `0x1700169F`, but snaps a standing view to the entity. Awake-unit ordering is not guaranteed. | ABSENT — REIMPLEMENT | Rider override mirrors mount movement; a pair-local `LateUpdate` enforces the proven anchor local transform and commits final rider entity position/orientation. This isolated per-pair diagnostic sync is measured and reversible; no global tick patch. | high for semantics; tested-pair runtime PASS |
| View position / presentation | Rider view shares entity origin with mount; `IKController` moves rider skeleton root to `MountOffsets.Root`/Battle and drives pelvis/feet/knees/hands. | `UnitEntityView` exposes root transform, animator, animation manager, pelvis fallback, and recursive transforms, but no `MountOffsets` or mounted IK type. | PARTIAL PRIMITIVE | Keep rider view root synchronized to entity; attach only an owned presentation child/anchor strategy if exact native transforms support it. Snapshot parent/local/world transform and restore. | high for absence, low for candidate |
| Movement completion/cancel | Mount agent propagates interruption/completion to rider and stops rider agent. Rider Stop/Hold/LookAt delegate to mount. | `UnitEntityView.OnMovementInterrupted` interrupts move and group command; `StopMoving` `0x06001851` stops an agent. `SelectionManagerBase.Stop` `0x060000B9` and `Hold` `0x060000BA` cannot reach a mount-owned command. | PARTIAL PRIMITIVE | Pair-scoped forwarding calls mount `Commands.InterruptMove` `0x060026C2` plus `View.StopMoving`; telemetry asserts both commands/agents are idle. | medium-high |
| Selection | Direct click on a controllable mount redirects to rider; box selection excludes mounts. | `SelectionManager.SelectUnit` `0x060034F0`, `ReplaceSelection` `0x060034F4`, and `MultiSelect` `0x060034F5` are public; no mounted redirection. | USABLE CONTROL POINT | Snapshot selected stable IDs; keep rider selected; remove mount from selection while active; restore exact still-valid selection on cleanup. Avoid a global selection replacement unless runtime proves necessary. | medium-high |
| Formation | Wrath excludes mounts from auto/multi-ground formation; mount/dismount dirty formation; mount receives rider offset. | `ClickGroundHandler` builds `allUnits` from controllable characters; `PartyFormationHelper.FillFormationPositions` `0x06007E1A` indexes all units and uses their corpulence. | USABLE CONTROL POINT | Build an effective-unit view where active mount replaces rider and rider is omitted, then map resulting destination to mount. Non-pair units retain their ordinary order and targets. Runtime must prove no interference. | medium |
| Combat boundary | Wrath remains mounted and integrates combat/turn state deeply. | `IUnitCombatHandler.HandleUnitJoinCombat` `0x06007613`; `IPartyCombatHandler.HandlePartyCombatStateChanged` `0x0600750F`; `UnitEntityData.JoinCombat` `0x06008355` raises the event after combat state changes. | USABLE CONTROL POINT | Phase 1 immediately triggers best-effort cleanup when either entity joins or party combat becomes true; no combat behavior is enabled. | high |
| Death/incapacitation | Controller dismounts for unconscious/prone/swallowed/pit states and invalid relations; dead-unit ticking remains enabled. | `IUnitLifeStateChanged.HandleUnitLifeStateChanged` `0x06007620`; `IUnitFinallyDeadHandler` `0x060074CD`; current `UnitState`; no mounted controller. | USABLE CONTROL POINT | Event-driven cleanup plus defensive per-frame invariant validation. | high |
| Area/game-mode boundary | Wrath has special global-map temporary unmount/remount; ordinary view detach preserves relation. | `ISceneHandler.OnAreaBeginUnloading` is raised before mode/entity teardown; `IPartyLeaveAreaHandler.HandlePartyLeaveArea` `0x06007516`; `IGameModeHandler`; area loading/activation interfaces. | USABLE CONTROL POINT | Cleanup at area-begin-unloading and any party/game-mode transition. No automatic remount. | high |
| View lifecycle | Wrath part only toggles avoidance on detach/attach; relation persists. | Kingmaker exposes unit view-attach event `0x06007519`; entity `Dispose` clears the view but no public detach event was found. | PARTIAL PRIMITIVE | Defensive service tick detects null/replaced views and cleans up before attempting sync; view-attach event never reconstructs relation. | medium |
| Mod disable / exception / process exit | Wrath subsystem is engine-owned. | Installed UMM 0.28.2 exposes `OnToggle`, `OnUnload`, and `OnSessionStop`; process exit beyond managed callbacks is not guaranteed. | USABLE CONTROL POINT | Composition root owns coordinator; disable/unload/session-stop call cleanup in `finally`; every mutation has a restoration record. | high |
| Save/load | Valid Wrath reciprocal parts persist. `UnitEntityData` post-load token `0x0600A107` repairs orphan parts/stale mount ability. Commands and turn state also serialize mounted fields. | `UnitEntityData.Position` and descriptor relationships serialize. `SaveManager.SaveRoutine` `0x06008029` calls state/player `PreSave`; `LoadRoutine` `0x0600802C` disposes current state. | ABSENT — SIMPLIFY | KMC state is ordinary in-memory code only, never an entity part. Harness scenarios force cleanup before save/load; no relationship reconstruction. Position mutation still requires cleanup before save. | high |
| Targeting/action economy | Wrath retargets ordinary hostile rider targets to mount and treats pair as one turn with coupled commands/resources. | No mounted target/turn concepts. | ABSENT — SIMPLIFY | Explicitly excluded from Phase 1. Combat start cleans up. | high |
| Animation / native anchor | `MountOffsets`, rider-race/size configs, mounted clip alternatives, and full-body IK are dedicated systems. Missing offsets can leave logical mount without presentation. | No mounted offset/IK/clip types. Exact native metadata identifies the active-companion Mammoth, idle/walk/run clips, and a bounded `Spine` position-anchor hypothesis; no saddle/rider transform or mounted pose exists. | ABSENT — SIMPLIFY | One crude Kingmaker-native Mammoth position-anchor experiment; no Wrath assets or animation code. Attachment stability qualified for the tested pair, but presentation requires new original animation/pose work. | high for absence/metadata; mechanically viable attachment, low pose quality |

## Wrath state, entry, exit, and failure summary

- Relationship state is reciprocal and serialized. Mounting validates the master/pet relationship, assigns both halves, disables rider avoidance, toggles the system ability, ensures rider IK, then raises the mount event.
- Dismount clears both halves, restores avoidance, calculates a navmesh-aware side position, disables the ability, and removes the rider part.
- No transaction or `try/finally` spans Wrath's multi-step transition. Invalid direct `Ensure<UnitPartRider>().Mount(...)` can leave an empty part; exceptions can leave partial state until controller or post-load repair.
- Controller invalidation covers consciousness, prone, swallowed, pit, hostility, master/pet/polymorph, and size. It does not use combat start as a cleanup boundary.
- View detach alone does not dismount. Serialization intentionally preserves valid relationships.

## Open-source observations

No source is reused. AutoMount exercises the public Wrath parts but omits comprehensive cleanup. TabletopTweaks and DragonFixes demonstrate future combat/action coupling. The unlicensed multiplayer snapshot contains a whole-method copy of decompiled click routing and is factual evidence only. All target Wrath/modern Harmony and are incompatible with the Kingmaker net47/C# 7.3/Harmony12 production surface.

## Pre-code gate

The contract portion of the gate is PASS for a bounded, diagnostic movement slice:

- one exact rider/pet pair can be validated from existing reciprocal descriptor state;
- rider command creation can be suppressed/rerouted at a scoped origin;
- the mount can remain the sole ordinary pathing mover;
- rider avoidance can be disabled/restored through the existing reference-counted property;
- the missing Wrath movement-controller sync can be reproduced in an isolated active-pair service;
- selection/formation can be projected through an effective-unit mapping rather than changing global data;
- all lifecycle and save boundaries have observable cleanup hooks or defensive invariant checks;
- no custom relationship serialization is required.

The asset/presentation metadata gate is PASS for one bounded experiment: only the exact active `AnimalCompanionUnitMammoth`, currently larger than a Medium rider (normally rank 7+), with a `Spine` position-anchor hypothesis. Runtime attachment is mechanically viable for the tested pair; the final visual classification is `MECHANICALLY VIABLE, NEW ANIMATION/POSE WORK REQUIRED`. Any ordinary-click Harmony hook must validate exact signatures/tokens and be inert when no active pair exists.

## Kingmaker movement caveats that constrain implementation

Phase 2 dev.12 adds one bounded contract discovered by strict camera repeat evidence. Public parameterless `Kingmaker.View.UnitEntityView.ForcePlaceAboveGround` is exact token `0x06001848` under the pinned Kingmaker MVID. Kingmaker's standing movement path can call it after a small parent-follow delta; the method terrain-raycasts and writes the result to both the view transform and `EntityData.Position`. That operation is invalid only for the parented active rider. The ninth exact-token Harmony prefix therefore suppresses only the exact active rider while Mounted, leaving every non-rider and unmounted invocation stock. No global movement tick is patched. The updated offline contract total is `92/0` Kingmaker plus `11/0` Wrath.

- `UnitEntityView.MoveTo` `0x0600184D` and turn-based precomputed-path calls address stock `AgentASP` directly, bypassing `AgentOverride`. An override alone does not stop rider pathing.
- `UnitMovementAgent.UpdateUnitAvoidance` also inspects another unit's stock `AgentASP`. The transition must call rider `AgentASP.Stop()` `0x060018C2`, acquire exactly one `AvoidanceDisabled` lease, then disable it before installing the owned override.
- `AvoidanceDisabled` is a `CountingGuard`, not an idempotent Boolean. Cleanup releases only the lease KMC acquired. Its effective getter is the counted/serialized guard OR native `!UnitState.IsConscious`, so post-release validation requires the captured effective state plus current native consciousness; stock unconsciousness must not be mislabeled as retained KMC ownership.
- `UnitMoveContiniously.Init` `0x060026F0` installs its agent override after receiving the executor. Phase 3D rewrites only the exact mounted rider executor to the mount before that install, cancels any superseded pair combat intent, and retains the relationship; a continuous-control request is not a lifecycle invalidation.
- Exact Kingmaker `UnitAttack.OnTick` `0x06002680` uses the turn-based one-second iterative delay after `OnAction` has already advanced its final attack index. For a still-animating exact single attack, `PlannedAttack` `0x06002670` is null and native `TryStartNextAttack(false)` calls `Interrupt`, replacing the existing `Success`. The mounted child alone bypasses that nonexistent-next-attack branch only while `IsInTurnBasedCombat`, acted `Success`, observed `LastAttackRule` `0x06002666`, `AllAttacks` `0x0600266F` count one, `GetAttackIndex` `0x06002675` count one, and no planned attack are all exact; base `UnitCommand.Tick` remains responsible for animation-ended completion. Real-time and multi-attack behavior are unchanged.
- Exact Kingmaker `UnitCombatState.Disengage` `0x0600939B` synchronously calls `ShouldAttackOnDisengage` `0x060093A2` and public instance Boolean `AttackOfOpportunity(UnitEntityData,bool)` `0x060093A1`. The latter evaluates the owning unit's `CanAttackOfOpportunity` `0x06009390`, counter, threat hand, memory, motion, and conditions, then enqueues separate free `UnitAttackOfOpportunity` constructor `0x06002696`; its `OnAction` `0x06002699` marks the independent weapon rule through `RuleAttackWithWeapon.set_IsAttackOfOpportunity` `0x06007182`. Runtime run `20260816T230000Z-rider-move-attack-rt-rawslot-passA` proved an Architecture B synchronization boundary can therefore create a rider duplicate during an explicit approach. The fourteenth token-pinned Harmony guard suppresses only exact rider/Mammoth opportunity emission while one mounted command is active; it is inert outside that temporal/pair scope and patches no engagement lifecycle. Exact pair-wide trigger/counter arbitration would require a broad layer, so the explicit mounted-AoO stretch is `DEFER — EVIDENCED` and default-off.
- Exact Kingmaker `AbilityCustomCharge.Deliver` `0x06002BB6`, `TurnBasesRoutine` `0x06002BB7`, `RuntimeRoutine` `0x06002BB8`, `Cleanup` `0x06002BB9`, and `CanTarget` `0x06002BBD` bind charge to one caster. State-machine IL proves caster-owned `UnitAttack` construction, `AgentASP.IsCharging`, unit charging state, `MaxSpeedOverride`, `ObstacleAnalyzer.TraceAlongNavmesh` `0x060017AD`, `UnitAttack.set_IsCharge` `0x06002663`, and caster command-queue ownership. Rider-caster use would violate Mammoth-authoritative movement; Mammoth-caster use would select the wrong attacker and resource ledger. No narrow stock mover/attacker split exists, so basic mounted charge is `DEFER — EVIDENCED` and default-off while ordinary non-mounted charge remains stock.
- Exact Kingmaker `RuleAttackRoll.IsHit` getter token `0x0600716B` is the authoritative hit Boolean: only `AttackResult.Hit` and `AttackResult.CriticalHit` return true. `RuleAttackRoll.Result` getter `0x06007131` retains a miss category selected by `ModifiableValueArmorClass.SelectMissReason(bool,bool)` token `0x06007F2D`. That selector uses `UnityEngine.Random.Range` and returns exactly `Miss`, `DodgeAC`, `ArmorAC`, or `ShieldAC`; two fresh natural-1 processes produced `ArmorAC` and `Miss`. Schema v6/v7 records both facts, accepts only that exact selector codomain for this row, and retains schema-v4/v5 validation for historical hit evidence.
- Assigning null to `AgentOverride` destroys the current override. Cleanup clears it only when reference equality proves KMC owns it.
- `UnitEntityData.Translocate` `0x06008345` is the supported entity/view relocation primitive for a validated nearby dismount location. Phase 1 does not write private movement caches.

- Exact Kingmaker `UnitCombatEngagementController.OnEventDidTrigger(RuleAttackRoll)` token `0x06009352` queues a ranged or ranged-touch initiator only when `RuleAttackRoll.DoNotProvokeAttacksOfOpportunity` is false, then calls `ProvokeAttackOfOpportunity` token `0x06009353`. `Tick` token `0x0600934C` later enumerates the queued unit's then-current `CombatState.EngagedBy` set and calls each hostile's stock `UnitCombatState.AttackOfOpportunity`; ranged provocation is therefore asynchronous rather than inline with `UnitAttack.TriggerAttackRule`. A focused control must preserve the native roll, later tick, hostile eligibility/counter, and separate opportunity command. It cannot infer suppression from a same-frame sample or synthesize the chain downstream.

## Final Phase 1 runtime qualification overlay

- Lifecycle qualifies twice at 8/0 rows and 339/0 assertions per process in direct service/handler scope. Open ground, stop/start, turns/corners, pause/unpause, and destination cancellation each qualify A/B with one mount-authoritative mover, bounded synchronization, exact cleanup, and exact external restoration.
- Boundary cleanup qualifies in five fresh processes: turn-based 56/0, realtime 56/0, save 59/0, load 44/0, and area 44/0, totaling 259/0 assertions. Turn-based/realtime are direct-handler claims; save is direct cleanup without stock `SaveRoutine` or serialization; load is a real exact-Working `Game.LoadGame` with native `LoadRoutine` prefix but no UI request; area is direct pre-cleanup plus real `ReloadArea`, without independently qualified native area-event delivery.
- The revised F1 fixture closes the prior runtime qualification gaps without changing any assembly contract: doorway matched control, away/back selection, and stock party formation each pass twice in fresh processes under unchanged gates.
- The final named-scenario ledger is 25 PASS, 0 attributable FAIL, and 0 DEFER. No K1–K12 kill criterion fired, and no K13 exists. Final A/B/C/D scores are 41/71/77/89; Architecture B is selected. The top-level status is `PHASE 1 COMPLETE — PROCEED RECOMMENDED`; Phase 2 remains separately unauthorized.

## Phase 2 native settings and persistence addendum

Phase 2 authority does not alter the settled Phase 1 architecture. Bounded inspection of the same exact Kingmaker MVID pins `SettingsEntityBool.m_Cached` `04002275`, `SettingsEntityBase.OnOptionUpdatedCallback` `04002269`, `SettingsEntityBase.OnInvokeUpdateCallback` `06003359`, `SettingsEntityBase.GetSavedValueString` `0600335C`, `SettingsRoot.SettingsListScreen.EnableTurnBasedMode` `04007C9F`, and `OnlyOneSave` `04007CBC`. `GameSettingsController.Initialize` installs the turn-based callback that raises `ITurnBasedModeEnabledHandler`. This supports a no-persistence diagnostic mode transition by temporary in-memory cache substitution, callback invocation, reverse callback, exact raw-cache restoration, and persisted-string equality verification.

The Phase 2B transient-target repeatability repair additionally pins `UnitGroup.get_Count` `06002412`, `UnitGroup.get_Item` `06002413`, `UnitEntityData.m_AiEnabled` `040054BA`, `get_IsDirectlyControllable` `06008328`, `get_IsAIEnabled` `06008329`, `set_IsAIEnabled` `0600832A`, and `UnitCommands.get_Empty` `060026A6`. Three fresh schema-v16 processes prove that a non-pair member of the exact rider/Mammoth player-party group can autonomously attack the transient hostile before expected dispatch. The permitted seam is diagnostic-only: capture the exact ordered non-pair set before target creation, reject any existing command or membership/context drift, lease raw/effective AI disabled, and restore exact prior state after target/group/faction removal. No command interruption/clearing, party mutation, save authority, or production mounted-combat behavior follows from this contract.

`Game.SaveGame` token `06000CE4` performs admission checks before passing `SaveManager.SaveRoutine` to `LoadingProcess`; the existing exact-token Harmony prefix is therefore a valid native-delivery observation seam. Phase 2 permits only one armed exact-Working suppression before the stock iterator body. It does not qualify stock serialization and does not broaden the write allowlist. `Game.ReloadArea` token `06000CD6` calls `LoadArea` with `AutoSaveMode.None`; bounded decompilation establishes the native area-stage order used by the Phase 2 validator. Current offline exact-contract total is `92/0` Kingmaker plus `11/0` Wrath; runtime claims remain bounded to their recorded rows.

## Phase 2A manual-review contract addendum

No new game-assembly seam is required for the mandatory human checkpoint. The review session reuses the already-pinned native Working-load, exact pair, selection, player-action overlay, pose, lifecycle, and process-teardown seams. Its protocol narrows authority further to `read-only`: one exact Working load is required and every write remains fatal. READY requires exact Default-mode/noncombat/area/campaign identity, one selected rider, the active Mammoth pair, healthy applied `medium-humanoid-mammoth-v1` pose, and a visible/enabled `Dismount` action for ten stable frames. The offline exact-contract total remains `92/0` Kingmaker plus `11/0` Wrath.
# Private-alpha stabilization contracts — 2026-08-21

- Exact installed `GameModeType` identities are `Default=1`, `Pause=4`, `FullScreenUi=5`, `EscMode=6`, and `Cutscene=7`. Pause/character/inventory/spellbook/map/menu surfaces use non-world interaction or UI identities and do not prove area unload, scene detach, or relationship invalidation. KMC retains the pair through `Pause`, `FullScreenUi`, and `EscMode`, while blocking new movement/combat admission until `Default` world interaction resumes.
- Installed `Polymorph.TryReplaceView` token `0x06002A08` instantiates the replacement, parents it to the old view's current parent, copies pose, and calls `EntityDataBase.AttachToViewOnLoad` token `0x06007E9D`. `Polymorph.RestoreView` token `0x06002A09` uses the same attachment seam for the stock body. `UnitEntityData.OnViewAttached` token `0x0600835C` then raises both native view-attached handlers. Because KMC temporarily owns the old rider root parent, an exact replacement can inherit the KMC anchor; KMC may release only that replacement root to the lease's captured stock parent before destroying the owned anchor. It may not enable renderers or retain the old humanoid.
- Public `UnitCommands.Run(UnitCommand)` token `0x060026B2` remains the exact stock command-admission seam. The stabilization prefix rejects only a stock `UnitAttack` submitted through the exact mounted rider's own `UnitCommands`; KMC's pair wrapper, Mammoth commands, unmounted rider, non-pair units, abilities, and all non-attack commands remain unchanged.
- Installed `ClickUnitHandler.OnClick(GameObject,Vector3,int,bool,bool)` token `0x060093ED` resolves the exact root `UnitEntityView`, selects the nearest selected actor, and runs the resulting stock attack through that actor's `UnitCommands`. It has no knowledge of KMC's IMGUI overlay. The private-alpha screenshot preserved `Mounted combat cancelled: ground command.` after overlay combat input; production review proved that the overlay previously armed without a bounded same-activation input lease while KMC's ground prefix unconditionally cancelled the action. Stabilization therefore consumes at most one unit/ground world click within two frames of the exact overlay activation, retains the armed action, and leaves later pointer routing unchanged. No global `PointerController` or renderer patch is introduced.
- The previously used `CombatController.StartTurn` token `0x06000BDA` remains an evidence surface but is no longer patched to force-end an unarmed Mammoth turn. Exact `SortedUnits`, `CurrentTurn`, `TurnController.Unit/Status/IsActing`, and `TickMovement` contracts remain authoritative. Installed `TurnController.Tick` changes a directly controlled unit from `Preparing` to `Acting` only after that unit owns a command or is already acted/unable. Both native pair actors therefore stay represented; ordinary mounted ground movement advances the sole exact Mammoth Move-slot command under the exact rider `Preparing` or `Acting` window through the rider controller's `TickMovement`, then the rider attack wrapper supplies the native rider command that advances the turn to `Acting`. KMC writes neither turn status nor a synthetic rider movement command.

## Chunk 4 Charge path — 2026-09-08 (ASSEMBLY CONTRACT)

Exact installed Kingmaker Assembly-CSharp SHA256 `3b6450ffec440e296e586f71c711b195aed144b28d53e1cbb29406d18fef5afb`, MVID `07fa1e4d-8618-41b3-9b8d-faa17d3b26f7`. `AbilityCustomCharge.CanTarget` token `06002BBD`; native ability target click `ClickWithSelectedAbilityHandler.OnClick` `060093F6` creates a cast command and calls UnitCommands.Run. `UnitUseAbility.Init` `06002728` initializes targeting/approach; its OnAction `06002737` begins native execution. UnitCommand.TickApproaching `060027A6` precedes ordinary movement; Tick `060027A7` drives action delivery. Charge custom delivery owns a forced path, charging state/buff and a queued native attack. This distinct path is not ordinary mounted attack routing. Exact local IL inventory: `analysis-cache/chunk4-native/charge-contracts.txt`; proprietary decompilation remains outside Git/packages. No Wrath seam or new dependency is needed. Charge blueprint identity and mounted behavior still require the new native fixture. No production hook is added at this checkpoint.
### Chunk 4 exact Charge authority and early guard - preview.4

NEW NATIVE INTEGRATION A/RT and B/TB identify ChargeAbility c78506dd0e14f7c45a599990e4e65038 with exact AbilityCustomCharge. Both accepted mounted input and spent native rider resources without observed motion. Local ASSEMBLY CONTRACT confirms private UnitCommands.Run060026B3(UnitCommand cmd,bool fromQueue,bool doNotClearQueue) initializes/assigns slots; AddToQueueInternal060026B8(UnitCommand cmd,bool first) initializes/enqueues. Queue promotion removes its first node before the private Run call. UnitCommand.Start060027A5 precedes OnStart; Tick060027A7 calls OnAction, then marks acted and raises the action/cost event, including failed action results. An OnAction-only rejection is therefore too late. Get_IsAvailableForCast06002B49, CanTarget06002B63 and GetUnavailableReason06002B66 support pure query feedback. Exact native click060093F6 parameter names gameObject/worldPosition/button/simulate/muteEvents are pinned for Harmony12. All nine new hook signatures pass446/0 Kingmaker contracts with the retained checks. Original policy+adapter owns rejection; patches only delegate. Native guard/recovery qualification remains pending.

ObstacleAnalyzer.TraceAlongNavmesh060017AD first compares origin against GetNearestNode060017B4; horizontal squared distance>0.0001 returns the clamped origin rather than the requested endpoint. The unmounted control's origin projection is a new observation, not permission to change native collision, range, or Charge implementation. Bounded original factual inventory/IL remains under analysis-cache/chunk4-native and is not shipped.
Chunk 4 C query compatibility: installed CallOfTheWild DLL SHA256 4ebf8e1ed3e66ffed72ea33ea325595629423dacd5bffa23e3c9109144b26915 has CanTarget replacement prefix060009C9. Original factual local inspection: it computes query result and returns false, bypassing later prefixes. KMC applies exact mounted Charge policy in a read-only final-result postfix; no foreign assembly dependency or mutation. New native C (source91be8d1) provides query-mismatch evidence; subsequent native qualification remains open. Bounded local IL retained only in analysis-cache/chunk4-native/Inspect-ChargeForeignPatch.ps1 output, not source/package.

Chunk 4 D NEW NATIVE INTEGRATION: private queue Run promoted an unacted Charge on frame9457, followed by its mounted safety Interrupt in TickApproaching frame9458 before native start/movement/expenditure. This refutes a fixture assertion that rejection must finish inside private Run. Relationship at earlier admission was not captured; pending Mount at admission remains an inference. Preview.6 observes exact relationship and both actors' costs/positions before/after the existing TickApproaching060027A6 seam, requiring mounted, unstarted/unacted pure rejection. No new gameplay hook, timing reset or foreign mutation; native requalification pending.
Chunk 4 E/F NEW NATIVE INTEGRATION on sourcea2f89b3/preview6 resolves queued timing: private Run before/after Unmounted (Eframe9579,F9126) leaves native shellunstarted/unacted; nextframeTickApproaching before/after Mounted changes only terminalstate tofinished/Interrupt, with no start/acted/charging, identical actorpositions andbothactorStandard/Move0. No native approach occurs for this rejected shell. Bothmodes fiveCharge rows/50assertionsPASS plusstrictvalidator. HistoricalD remainsfailed underits schema19 timingassumption. Mounted queryrestriction/nativewarning andunmounted/unrelated nativechargecontrolsPASS. Exactdata inCHUNK4report andimmutable E/Fartifacts.
# Chunk 4 sustained observations - 2026-09-09T01:35Z

ASSEMBLY CONTRACT: installed Kingmaker SHA256 `3b6450ffec440e296e586f71c711b195aed144b28d53e1cbb29406d18fef5afb`, MVID `07fa1e4d-8618-41b3-9b8d-faa17d3b26f7`. Read-only subscription observations use EventBus.GlobalSubscribers04004C2F, SubscriptionManager.m_Listeners04004C39, PooledDictionary.m_Dictionary04004C34 and SubscribersList.Executing04004C3B/List04004C3C. The pooled wrapper implements IEnumerable but not IDictionary; its exact backing dictionary implements IDictionary. A new failed contract caught the original reader assumption before native use; repaired contracts451/0. No subscriber collection mutation or cleanup call is authorized by this observation. Native settled counts remain unqualified until measured.

The sustained fixture observes existing UnitAttack.OnStart, InitAttacks, OnAction and native cost callbacks, retains actual command references and per-case event snapshots, and compares entire native plans/recovery rather than one rule per command. No new production hook or native mode/cost override is introduced. Available fact/spellbook identities and real difficulty/death flags are inventoried without granting facts, changing slots or applying effects; inventory alone cannot qualify incoming targeting or native death.

## Chunk 4 visibility and native UI/delivery contracts — 2026-09-09

ASSEMBLY CONTRACT, exact installed Kingmaker SHA3b6450ffec440e296e586f71c711b195aed144b28d53e1cbb29406d18fef5afb / MVID07fa1e4d-8618-41b3-9b8d-faa17d3b26f7. UnitCommand.IsUnitEnoughClose06002784 combines native distance with NeedLoS0600276C, ApproachPoint06002781, Executor.EyePosition06008302 and virtual GetTargetLOSObjectId060027A9 through LineOfSightGeometry.Instance04000C43.HasObstacle0600121A. UnitEntityData.HasLOS0600834F reads a different SightCache contract; it is not the native command visibility predicate. New K runtime evidence records cachefalse with native enoughClosetrue after the three native ranged deliveries. Preview9 uses the geometry authority only for the scoped RT ranged-tail continuation classification; native reach, obstruction, sequence, timing and costs remain authoritative. UnitCommand.Interrupt060027AC calls OnEnded060027B2 synchronously; no deferred terminal latch is justified.

Native character inspection: ServiceWindowController.OnHotKeyShowChracterScreen06004768 opens the actual character sheet; GroupController.SelectUnit06003F29 / GetCurrentCharacter06003F22 drives its existing binding. CharacterScreenController.m_CurrentCharacter04003140 is observation only. ServiceWindowTabs.Hide0600477A closes only the owned window; FullScreenTabsWindow.ScreenIndex getter060046D0 is observed. Native UI binding is not human visual approval.

Native projectile lifecycle: RuleAttackWithWeapon.LaunchProjectile0600719F installs RuleAttackWithWeaponResolve into Projectile.OnHitTrigger04005E0F; actual OnHit06009270 runs native rule delivery and possible native deflection. ProjectileController.Projectiles06009275, Projectile IsHit06009258/HitWall0600925A/Cleared0600925C/Destroyed06009256 and Target04005E11/View04005E1C provide read-only flight observations. Interruption fixtures never destroy or synthesize projectile completion. Local bounded analysis files native-attack-visibility-contract.txt, character-ui2..5-contract.txt and native-projectile-life2-contract.txt remain outside Git. No proprietary source is redistributed.

## Chunk 4 native area entry and round observations — 2026-09-09

ASSEMBLY CONTRACT, exact installed Kingmaker SHA256 `3b6450ffec440e296e586f71c711b195aed144b28d53e1cbb29406d18fef5afb`, MVID `07fa1e4d-8618-41b3-9b8d-faa17d3b26f7`. Bounded local inspections remain outside Git in `analysis-cache/chunk4-native/native-area-{callback,unit-entry,single-unit}-contract*.txt`.

`AbilityAreaEffectRunAction.OnUnitEnter(MechanicsContext, AreaEffectEntityData, UnitEntityData)` is token `06002CCD`; `OnRound(MechanicsContext, AreaEffectEntityData)` is `06002CD0`. They run separate `UnitEnter`/`Round` ActionLists (`04001D53`/`04001D56`) inside real AreaEffectContextData and actor target scopes. The round callback enumerates the area's actual `UnitsInside`. `AreaEffectContextData.Entity` is `04001D52`; observe it through native `ElementsContext.GetData<T>()` (`060083E5`), not an assumed `Current` property.

The area execution iterator `0600C070` calls `UpdateUnits` (`060082BC`), then blueprint `HandleTick` (`06002D08`), then decrements its native round timer and may call `HandleRound` (`06002D07`) in that same update. `UpdateSingleUnit` (`060082BD`) adds a new UnitInfo before `HandleUnitEnter` (`06002D04`). The `UnitsInside` iterator (`0600C067`) yields real in-game/in-state unit references; it does not wait for `InsideThisTick` assignment. These facts support observing membership during entry and distinguish native entry from round callbacks. They do not establish P's runtime callback attribution.

P's mounted Entangle attempt (`0fd00984a2c0e0a429cf1a911b4ec5ca`, area `bcb6329cefc66da41b011299a43cc681`) recorded one rider and two Horse save events at the same game time. The additional Horse save could belong to the first native round; this remains an inference until fresh mounted and unmounted traces identify each event's area instance, actor, exact callback token and MVID. The next observer records these facts, native source/reason metadata, bounded action/condition metadata, separate actor Reflex stats, and dropped-event counts. No native area logic or saving throw is patched. Duplicate events within one actor/callback remain failures.

O's incapacitation trace reached real Unconscious state and removed attachment/commands/private partner context, but its fixture incorrectly waited for the retained split activation identity to become null during the encounter. The next diagnostic observes split/finalized/ended state without changing it, checks unrelated native turns and unchanged grants, and requires actual native encounter exit and record retirement before fixture teardown. Rider death itself still requires fresh native evidence.
