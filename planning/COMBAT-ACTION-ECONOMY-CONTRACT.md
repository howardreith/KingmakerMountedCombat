# Combat action economy contract

Status: IN PROGRESS

Authority: the user accepted exact presentation commit `09a63729e0847c540ae7e79e9e3876d005ee9afe` and opened Tranches C-F of `planning/MOUNTED-COMBAT-PHASE-2-MASTER-MISSION.md`. This contract is scoped to one transient Medium-humanoid/`AnimalCompanionUnitMammoth` pair and ordinary melee only.

## Exact Kingmaker evidence

The authority assembly is Kingmaker `Assembly-CSharp.dll` SHA-256 `3b6450ffec440e296e586f71c711b195aed144b28d53e1cbb29406d18fef5afb`, MVID `07fa1e4d-8618-41b3-9b8d-faa17d3b26f7`.

| Concern | Exact local contract | Consequence |
|---|---|---|
| Attack construction | `UnitAttack(UnitEntityData)` token `0x06002678`; `IsSingleAttack` accessors `0x0600265C/5D`; `Init` `0x06002679`; `LastAttackRule` `0x06002666` | A single stock attack preserves native animation/rule execution. `IsSingleAttack=true` prevents iterative, off-hand, and additional-limb expansion. |
| Attack identity | `UnitAttack.TriggerAttackRule` token `0x06002686` creates `RuleAttackWithWeapon` using `Executor`, `Target`, and the planned weapon. Rule constructor token `0x0600719C` | The child attack executor, not the UI or pair coordinator, is the combat-log/rule initiator. |
| Command ownership | `UnitCommands.Run` token `0x060026B2`; `UnitActionController` ticks commands only for their executor. In turn-based mode a command other than `UnitAttackOfOpportunity` advances only when its executor is `CurrentTurn.Unit`. | Rider melee runs in the rider Standard slot on the rider turn. Mammoth primary runs in the Mammoth Standard slot on an explicitly armed Mammoth turn. Actor, command owner, rule initiator, and resource owner must be identical; cross-actor execution is rejected. |
| Real-time start readiness | `UnitActionController.ShouldStartCommand` token `0x0600911F` waits for idle hands, no scheduled hand-equipment update, `State.CanAct`, `UnitCombatState.CanActInCombat` token `0x0600938F`, and no command cooldown. `UnitEntityData.AreHandsBusyWithAnimation` is token `0x06008330`; `UnitHandEquipmentController.IsUpdateScheduledFor` is `0x06009154`; initiative cooldown getter is `0x0600C3B4`. | A command issued during Kingmaker's combat-entry auto-pause must remain admitted and wait. Real-time qualification explicitly leases pause state, unpauses after combat entry, waits for every stock start gate, and restores the exact prior pause state. Production does not bypass hands, equipment, initiative, or cooldown. |
| Actor-specific combat initiative | `UnitCombatPrepareController.Tick` token `0x0600936F` clears and assigns each joining unit its own initiative across the exact native 0-6 second window through setter `0x0600C3B5`. `UnitCombatState.CanActInCombat` token `0x0600938F` reads its own `IsWaitingInitiative` token `0x0600938E`. | Readiness belongs to the action actor, not invariably to the rider. RT requires the exact action actor at zero initiative; TB requires exact current-turn ownership and a bounded prepared value. The rider's independent initiative remains bounded and separately evidenced, and its Standard/Move ledger must remain unchanged for Mammoth primary. |
| Diagnostic combat entry | `UnitMemoryController.AddToMemory` token `0x0600916F` is buffered, rejects a peaceful target faction before enqueue, and later skips a hidden queued target. `UnitEntityData.Memory` `0x060082F8` is exact group memory; public `UnitGroupMemory.Enemies/Add/Remove/Contains` are `0x06001F2B/2C/2D/31`. `UnitEntityData.Wake` `0x0600836C` only extends `AwakeTimer`; `SleepingUnitsController.Tick/ShouldBeSleeping` `0x060090B1/B2` rebuilds `AwakeUnits` and honors per-unit `Sleepless` `0x040054C8`. `UnitCombatJoinController.Tick/TickUnit/ShouldEngageEnemy` `0x06009360/61/62` additionally gate on exact live groups, consciousness, `IsIgnoredByCombat`, materialized enemy lists, fog, stealth/ambush, reverse player memory, and turn-mode awake state. | Controlled provisioning leases one memory entry per direction and `Sleepless=true` only on a disposable target proven to begin false, then restores everything before destruction. Schema v8/v9 binds awake acquisition/release; observation-only v10/v11 adds every known native join predicate while preserving v1-v9. KMC never calls `JoinCombat`, edits player/pair sleepless state, clears native flags, or changes initiative, cooldown, pathing, or a save. |
| Diagnostic target durability | `ModifiableValue.AddModifier(Int32, Fact, String, ModifierDescriptor)` token `0x06007F08`, `ModifiableValue.Modifier.Remove()` token `0x0600BE68`, and `ModifiableValueTemporaryHitPoints.HandleDamage(Int32)` token `0x06007F74` are exact public instance contracts. Native `RuleDealDamage` computes and retains its positive damage result before temporary hit points absorb that amount. | Only Mammoth-primary diagnostic rows may lease exactly 128 untyped-stackable temporary HP on the disposable target, which must begin at exactly zero. The modifier is removed and the exact prior value re-proved before target destruction. Rider rows acquire no durability lease. This changes no rider, Mammoth, production attack, save, initiative, cooldown, or pathing state, and a cleanup proof failure prevents target destruction rather than hiding residue. |
| Diagnostic target brain | `UnitEntityData.IsAIEnabled` token `0x06008329` returns true for every non-direct unit before consulting raw `m_AiEnabled`. `AiBrainController.TickBrain` token `0x0600940A` instead selects commands when the unit is in combat, has a brain, and public `IsBrainActive` getter token `0x06008319` is true; pinned IL contains that getter and not `IsAIEnabled`. Public setter token `0x0600831A` is the reversible per-unit seam. | Raw AI state alone cannot make the hostile transient target stationary. Diagnostics lease only that target's exact brain state `true -> false -> true`, validate it continuously from activation through outcome, then restore it after final Stop/drain immediately before destruction. No direct-control, pair/player AI, production behavior, pathfinding, resource, persistence, or external-state authority changes. |
| Mammoth-origin native admission | `UnitCommand.IsUnitEnoughClose` getter `0x06002784` compares the command executor position to `ApproachPoint`; `GeometryUtils.MechanicsDistance` is `0x06001C68`. Probe K proves that setting only the Mammoth-derived stopping radius leaves the offset rider executor outside stock admission even when the Mammoth is in range. | The exact Mammoth-origin range gate remains authoritative. Only after that gate passes may the scoped child radius bridge the measured rider-executor offset by `0.001`, capped at `0.75`; outside-pair range returns zero native admission. The bridge is re-applied through the existing exact child-only `UnitAttack.GetApproachRadius` postfix so stock `Start` and `UpdateTarget` agree. Delegated pathfinding continues to use the unexpanded Mammoth stopping radius. |
| In-flight liveness | `UnitEntityData.CanAttack(UnitEntityData)` token `0x0600834E` is neutral-or-`Group.IsEnemy`; it is not a hands, cooldown, animation, or action-readiness gate. Probe L reaches one child start and then exposes a generic combined liveness rejection before completed rule evidence. | Keep every existing relationship, identity, in-state, consciousness, final-death, hostility, and attackability predicate fail-closed. Schema v4 records their exact ordered failure set and transaction terminal reason; no predicate is removed until the exact live transition is measured. |
| Native child completion after target incapacitation | Probe M's schema-v4 terminal reason names only `target-conscious`. Exact `UnitCommand.Tick` order sets the result from child `OnAction` before waiting for animation completion to call `OnEnded`; therefore target incapacitation can precede `IsFinished`. | Target consciousness remains mandatory at admission. After exactly one child start, unconsciousness alone cannot prevent that same child from publishing its native result. Target removal/final death, pair identity, hostility, and duplicate/impossible child counts remain fail-closed. |
| Rulebook observation | `EventBus.Subscribe(object)` token `0x060074BB` delegates to `RulebookEventBus.Subscribe(object)` token `0x06007540`. A global rule observer must implement `IGlobalRulebookSubscriber`; `IGlobalRulebookHandler<T>` token `0x02000DC3` supplies that marker plus `IRulebookHandler<T>`. | Diagnostic forced-roll and attack/roll/damage evidence uses exact global handlers. Plain unmarked handlers are rejected by regression coverage; production attack execution remains native and independent of the observer. |
| Cooldown charging | `UnitEntityData.UpdateCooldowns` token `0x0600838F` adds six seconds for Standard and three seconds for Move. `UnitActionController.TickCommand` token `0x0600911E` snapshots `IsActed`, invokes `UnitCommand.Tick` token `0x060027A7`, then calls `UpdateCooldowns` token `0x06009120` only on a false-to-true transition. | The pair exposes one actor-executed Standard wrapper. It keeps animation gating until one child is admitted, then lets the base wrapper publish the native acted transition while manually advancing the ignored-cooldown child. It never calls `SetIsActed` or edits cooldowns. The wrapper charges its exact executor once; the child cannot double-charge. |
| Turn accounting | `CombatController.CurrentTurn` `0x06000BBE`, `StartTurn` `0x06000BDA`, and `CurrentUnit` `0x06000BFA`; `TurnController.Status` `0x06000C0E` (`Preparing=2`, `Acting=3`); `TurnController.ForceToEnd` `0x06000C47` | Rider melee uses the exact native rider turn. Unarmed Mammoth turns remain immediately suppressed. Only an explicitly armed Mammoth-primary action admits the exact native Mammoth turn; after its one terminal command KMC invokes `ForceToEnd(false)`, preserving the already charged Mammoth Standard delta without manufacturing Move/Swift costs. KMC never writes turn status. |
| Movement accounting | `UnitMovementAgent.CanMoveInTurnBased(ref float)` token `0x060018A9` calls `CurrentTurn.TickMovement`; stock admission requires the moving unit to be current. | One exact scoped prefix may admit only the active Mammoth while the active rider owns the current turn. The stock `TickMovement` call remains the source of rider movement expenditure. |
| Movement command | `UnitMoveTo(Vector3,float)` is already `IgnoreCooldown`; `UnitCommand.TickApproaching` otherwise moves its executor. | A pair coordinator manually drives one Mammoth `UnitMoveTo`. The rider stock agent remains disabled and never paths. |
| Cancellation | `UnitCommand.Interrupt` `0x060027AC`; `UnitCommands.InterruptAll` `0x060026C1`; `InterruptMove` `0x060026C2` | Cancelling the rider wrapper interrupts its child attack, interrupts the delegated Mammoth move, and stops the Mammoth agent. |
| Native attack availability | `HasStandardAction` `0x0600837E`, `HasMoveAction` `0x0600837F`, and `HasCooldownForCommand` enforce current resource state. | KMC validates the exact action actor's Standard resource before accepting a pair command. It never edits cooldowns to create availability. |
| AoO | `UnitCombatState.AttackOfOpportunity` `0x060093A1` owns a per-unit count and creates `UnitAttackOfOpportunity`; `ShouldAttackOnDisengage` is `0x060093A2`. | Rider and Mammoth have independent stock engagement/AoO state. Combining it safely is stretch work; core does not synthesize or duplicate AoOs. |

Real-time stationary rider result: fresh same-package hit runs `20260816T084500Z-rider-hit-rt-probeQ` and `20260816T090000Z-rider-hit-rt-passB` each pass `47/0`. The rider wrapper starts exactly one rider child and stock false-to-true acted accounting charges only the rider Standard; the Mammoth Standard/Move remain unchanged, exactly one attack/roll/damage chain occurs, and no unexpected pair attack occurs. Fresh same-package schema-v18 miss runs `20260816T152500Z-rider-miss-rt-ailease-passA` and `20260816T154000Z-rider-miss-rt-ailease-passB` each pass `49/0`, including exact diagnostic-only non-pair AI lease restoration. Rider hit RT/TB and rider miss RT are qualified. Mammoth-primary stationary RT/TB is also qualified below; movement accounting still requires separate evidence.

Turn-based actor diagnostic contract: begin only from a verified real-time baseline; use the existing non-persisting `NativeModeTransitionProbe` to deliver the exact registered false-to-true callback before mounting; require that exact temporary setting value and an available native controller before mounting; then require the full `CombatController.IsInTurnBasedCombat()` predicate, controller initialization, and the exact rider, Mammoth, and transient-target membership in public `SortedUnits` after native combat entry. Call public native `CombatController.StartTurn(actionActor)` once, require `CurrentTurn.Unit` to be that exact actor and native status to be exactly `Preparing` or `Acting`, and queue only the normal click-path command. Production and diagnostics call the same pure `MountedPairTurnPolicy.CanIssueAction`; no duplicate inline status predicate is permitted. This is required because exact installed `TurnController.Start/Tick` places a directly controllable unit in `Preparing` and changes it to `Acting` only after a command is queued. A later frame must observe the exact action-actor turn in `Acting`, and the same identity/state must survive through outcome. Rider melee uses the rider actor; Mammoth primary is armed before starting the Mammoth actor turn so the normal unarmed-Mammoth suppression remains fail-closed. This ordering avoids the proven circular gates while retaining the full combat and turn predicates before qualification credit. It is bounded deterministic turn provisioning, not status, initiative, or cooldown editing. Schema v5 originally recorded rider controller/roster/current-turn/round identities, schema v9 added bounded target-awake fields, and schema v21 binds the exact Mammoth actor/turn/terminal-end identities. Fresh same-package rider runs `20260816T111500Z-rider-hit-tb-passA` and `20260816T113000Z-rider-hit-tb-passB` each pass `51/0`; fresh same-package Mammoth runs `20260816T195000Z-mammoth-primary-tb-passA` and `20260816T195500Z-mammoth-primary-tb-passB` each pass schema v27 at `59/0`. Both stationary turn-based actors are qualified under their exact ownership contracts.

## Selected rider movement-to-attack implementation

The first movement-to-target path is rider melee only. The rider's actor-owned Standard wrapper remains in the rider command container for the complete transaction. One `UnitMoveTo` uses the exact Mammoth as executor and owns only the Mammoth's active Move slot through stock `UnitCommands.Run`; the Mammoth queue must remain exactly empty. Exact installed `UnitActionController.TickOnUnit` token `0x0600911C` calls `UnitEntityView.StopMoving` token `0x06001851` whenever `UnitCommands.Empty` token `0x060026A6` is true, so a detached move cannot make progress. The slot-owned command must never be replaced, must finish successfully, and must be removed through the stock finished-command sweep before rider child admission. The Mammoth stock agent remains enabled with avoidance; the rider stock agent remains disabled/avoidance-suppressed and the existing `RiderMovementAgent` keeps logical/view synchronization and the accepted Mammoth pose bounded.

The deterministic open-ground probe begins at exact Mammoth-origin pair radius plus `2.0` world units, allowing at most `0.06` navmesh projection drift. It must begin outside radius plus `0.05`, reach radius plus `0.05` before native attack admission, move both rider and Mammoth by at least `0.5`, move the held brain-suppressed target by at most `0.05`, use exactly one delegated move with zero repaths, retain rider selection and combat UI coherence, and then start exactly one rider child attack. Real-time `UnitMoveTo` remains ignored-cooldown and changes neither Move ledger. Turn-based Mammoth movement is admitted only while the exact rider is current and Acting, and stock `TurnController.TickMovement` must advance the rider Move ledger while Mammoth Move remains unchanged. No cooldown or turn state is written by KMC.

Schema v32/v33 is additive only for the raw-slot repair. Schema v30/v31 first bound exact Mammoth Move-slot ownership, an empty queue, successful completion/restoration, progress, and exclusive stock-RT/rider-turn-TB control; same-package RT A/B pass those gates. TB attempt `20260816T221500Z-rider-move-attack-tb-passA` exposed that `UnitCommands.Move` token `0x0600269F` returns null for an exact finished command still present in the raw slot because it reads `IsFinished` token `0x0600275E`. The authoritative lifecycle check now uses raw `GetCommand(CommandType.Move)` token `0x060026A9`: the raw slot must be the exact delegated command, or must be truly empty only after that command is finished. A missing unfinished command or any replacement remains a failure; queue, success, final restoration, actor, resource, geometry, pose, selection, UI, and cleanup gates are unchanged. Historical schema v28-v31 shapes remain valid and are never reinterpreted. Repaired offline qualification is `21/211/17/180/253`; fresh same-package schema-v32 RT A/B and schema-v33 TB A/B are required.

## Bounded Wrath reference

The exact installed Wrath reference assembly remains read-only. Its `SaddledUnitController.TickDelegateRiderToMount` links a rider command to a mount command, its `UnitCommand.RiderCommand` propagates interruption, and its turn-based controller treats a rider and mount as one turn while excluding a saddled mount from the ordinary next-unit choice. Those responsibilities are evidence for the required behavior only. Kingmaker has none of those types or members, and no Wrath code, assembly, unit part, asset, or serialization model is imported.

## Selected pair policy

| Resource/behavior | Private-alpha owner and rule |
|---|---|
| Initiative and turn | Rider remains selection/initiative principal for rider actions. Unarmed Mammoth turns are suppressed; one explicitly armed Mammoth primary may use its exact native turn, which is bounded to that command and then ended with `ForceToEnd(false)`. |
| Standard action | Exact action actor. Rider melee consumes one rider Standard; Mammoth primary consumes one Mammoth Standard and leaves the rider unchanged. |
| Move actions | Rider. Mammoth moves as the sole pathfinding authority; TB distance/time is charged through the rider's current `TurnController`. |
| Swift action | Rider stock state, untouched. KMC exposes no mounted swift action. |
| Full-round/full attack | Unsupported while mounted in this alpha. Every KMC attack sets `IsSingleAttack=true`; no automatic rider-plus-mount sequence exists. |
| Five-foot step | No custom mounted five-foot-step surface. Movement-to-attack uses the current native rider movement state; KMC never grants a step or AoO immunity. A native state that cannot safely admit the Mammoth is rejected. |
| Rider attack | One rider Standard wrapper manually drives one rider-owned stock single attack. |
| Mammoth attack | One Mammoth Standard wrapper manually drives one Mammoth-owned stock single primary natural attack with ignored child cooldown. Rider does not attack and its resources remain unchanged. |
| AI | Mammoth companion AI receives a scoped effective-state lease while mounted. An unarmed native Mammoth TB turn is suppressed; an explicitly armed Mammoth-primary turn admits only the owned command and ends immediately afterward. The lease restores exactly on dismount. |
| Turn end | Stock rider turn completion for rider melee. Mammoth-primary completion/interruption ends the exact Mammoth turn without adding unused-action cooldowns. |
| Cancellation/interruption | One transaction owns wrapper, delegated move, and child attack. Any invalid target, death, unconsciousness, dismount, combat/mode boundary, new pair command, or manual cancellation interrupts all owned representations once. |

## Invariants and rejection rules

- Every command has exactly one actor-owned turn and one matching resource ledger. No command transfers or duplicates a cost across the pair.
- The Mammoth remains the only stock pathfinding agent. The rider agent is never enabled to approach.
- The attack wrapper is inert unless its executor, command owner, resource owner, and selected action actor are the same exact active rider or Mammoth.
- A child attack is stock `UnitAttack`, single only, and ignored for child cooldown. Its exact initiator/target/weapon/rule identities are recorded.
- A command cannot contain both rider and Mammoth child attacks.
- Ranged weapons, auto-use abilities, spells, full attacks, combined attacks, and unsupported profiles are rejected with visible feedback.
- Every scoped AI, command, movement, and turn lease restores on cleanup. Non-pair commands and non-mounted behavior remain stock.

Stationary rider and Mammoth attack accounting is qualified under the exact evidence above. Overall action-economy qualification remains `IN PROGRESS` for movement-to-attack, cancellation/interruption, lifecycle invalidation, AI suppression outside the stationary rows, and non-mounted isolation.

## Mammoth-primary offline checkpoint - 2026-08-16

Schema v20 real-time and v21 turn-based evidence established the exact action actor, command owner, resource owner, selected weapon blueprint/natural/ranged/slot identity, target, rule initiator, damage actor, resource deltas, movement, pose, turn, cleanup, and restoration. Historical schemas remain accepted under their original contracts. Deterministic fixtures reject rider substitution, cross-owned command/resource state, rider resource consumption, missing Mammoth Standard cost, Move cost, duplicate pair attack, weapon/slot mismatch, and wrong turn identity. The final schema-v26/v27 clean-package RT A/B and TB A/B runs qualify this stationary Mammoth-primary contract.

## Mammoth-primary terminal durability repair - 2026-08-16

Fresh repaired-placement run `20260816T170000Z-mammoth-primary-rt-passA` is preserved uncredited `FAIL` `48/3`. It proves the final range refresh (`3.589406 / 3.46940613`), accepted click, exact Mammoth actor and target, exactly one attack/roll/damage chain, forced natural 20, native `CriticalHit`, 29 damage, no duplicate/AoO/charge/repath, Mammoth-only Standard charging, unchanged rider resources, and healthy accepted pose. The one-HP target became `FinallyDead` before the native child completed its terminal animation, so the unchanged final-death liveness gate correctly rejected completion. Terminal `PlannedAttack` was also cleared by native command teardown, making a late weapon lookup unavailable; target death made the old position-only stationarity check ambiguous.

The narrow repair does not relax final-death or consciousness rules. The Mammoth-only diagnostic target now leases 128 temporary HP and must restore the exact zero prior value before destruction; schema v22/v23 binds acquisition/release, a conscious zero-transition life snapshot captured at the exact outcome, and independent rider/Mammoth/target displacement values at or below `0.05`. The command retains exact planned weapon identity at child validation and publishes that immutable identity after native teardown. Schema v20/v21 remains valid as immutable history. Deterministic mutations reject incorrect lease values, acquisition/release failure, target life transition, actor displacement, and weapon/ownership substitutions. Complete repaired gates pass source `21/0`, build, component `204/0`, visual/source-order `17/0`, harness/protocol `174/0`, and exact assembly `236/0` (`225/0` Kingmaker, `11/0` Wrath). Fresh same-package RT A/B and TB A/B remain mandatory.

## Mammoth-primary hostile-brain repair - 2026-08-16

Fresh run `20260816T183000Z-mammoth-primary-rt-passA` is preserved uncredited `FAIL` `51/1`. Every intended Mammoth command/rule/resource/weapon/life/pose/cleanup/restoration gate passes, rider/Mammoth displacement are `0.0362853371 / 0`, and only the target moves `0.6614979`. This occurs after the exact command drain and Stop gate, proving a new post-click path rather than retained pre-click motion.

The installed controller contract above makes the cause exact. Schema v24/v25 records the target's exact prior brain state, acquisition, effective-AI observation, sticky continuous-validation count/violation state, click and outcome suppression, exact prior-state restoration, and cleanup. Historical v22/v23 and all earlier schemas retain their original shapes and validation. Deterministic mutations reject each false or missing brain claim. The complete repaired gate passes `21/204/17/175/245`; fresh clean-package RT A/B and TB A/B remain required.

Actor-readiness update: `20260816T190500Z-mammoth-primary-rt-passA` reaches an exact game/schema-v24 `PASS` `53/0`, including all three displacement values zero, but remains uncredited final `FAIL` because the external validator required the rider's independent initiative to be zero while the exact Mammoth was ready. The observed rider value `4.99591351` is within the native per-unit preparation window and consumes no rider Standard or Move resource. Schema v26/v27 binds action-actor ID, preparation, eligibility, and initiative, keeps strict RT-zero/TB-turn gates, retains bounded rider evidence, and preserves schema v1-v25 unchanged. Offline gates pass `21/206/17/176/247`; fresh package RT/TB A/B remain required.

## Mammoth-primary runtime qualification - 2026-08-16

Exact package `8adb516b6970b07d714dcb46e3418d2fc0fc68a92af218b6170a3427598bab2d` from clean commit `defbbd0516ec1ad16eac7e83dd69ae1fc41fc8c6` passed separate RT/TB WhatIf gates and then fresh RT A/B at `54/0` each plus TB A/B at `59/0` each. The four credited runs prove one exact Mammoth primary natural attack, coherent attack/roll/damage actor and weapon identity, Mammoth-only Standard accounting, unchanged rider and Move resources, exact active Mammoth turn in TB, no rider duplicate, no repath/AoO/charge, zero rider/Mammoth/target displacement, continuously valid target-brain suppression/restoration, accepted profile health, complete cleanup, coherent manifests, and independently exact external restoration. This closes only stationary Mammoth primary; movement-to-attack and broader cancellation/lifecycle accounting remain open.
## Transient target life-state observation contract - 2026-08-16

Fresh-process miss evidence isolated an intermittent pre-combat target consciousness failure while every retained group, hostility, memory, awake, fog, stealth, and ignored-by-combat gate remained exact. Installed Kingmaker authority is `UnitLifeController.TickOnUnit` token `0x06009162`; raw reads are `UnitState.LifeState` `0x06001F95`, `IsFinallyDead` `0x06001F9B`, `MarkedForDeath` `0x06001FA1`, `ForceKill` `0x06001FA3`, `IsConscious` `0x06001FA7`, `IsDead` `0x06001FA9`, and `UnitEntityData` damage/nonlethal/stats tokens `0x060082E6/0x060082E8/0x060082EC`. Native transition delivery remains `IUnitLifeStateChanged.HandleUnitLifeStateChanged` `0x06007620`.

Schema v12 real-time and v13 turn-based evidence are observation-only. They capture exact target life projections immediately after native creation, at activation, at the last pre-cleanup observation, and on the first native transition. No life state, damage, HP, Constitution, death flag, combat membership, or save is written. A PASS requires a clean conscious creation and activation; a miss PASS additionally requires no life transition and a conscious last observation. Historical schemas v1-v11 remain valid under their original contracts.

Life probe `20260816T140000Z-rider-miss-rt-lifeprobe` proves the target begins Conscious with zero damage and then receives exact damage `15` before combat, producing one `Conscious -> Dead` transition. Schema v14 real-time and v15 turn-based therefore add observation-only global incoming-rule identity. Exact installed authority includes `RuleAttackWithWeapon.Weapon/IsAttackOfOpportunity/IsCharge` tokens `0x04004A8F/0x06007181/0x06007185` and `RuleDealDamage.AttackRoll/Damage/IsFake/IsDot/SourceAbility/SourceArea` tokens `0x060073D5/E5/ED/EF/F9/FB`. Evidence binds the first target-directed attack and damage initiator, blueprint, weapon/source, amount, and whether it preceded the explicit expected-rider-dispatch marker. A current PASS rejects every pre-dispatch incoming rule, requires exactly one non-charge/non-AoO rider attack, and requires zero damage on the miss row. Historical schemas v1-v13 retain their original meanings. The observer changes no Rulebook event, target statistic, command, combat state, or save.

Same-package schema-v14 probes separate the stable and interfering cases. A `20260816T143000Z-rider-miss-rt-ruleprobe` passes `49/0` with zero pre-dispatch rules and one exact rider miss. B `20260816T150000Z-rider-miss-rt-ruleprobeB` is uncredited `FAIL` `20/1` and proves one third player-faction actor attacks before the marker and deals 17 real non-DOT damage. Because faction identity does not prove party group, direct control, or raw AI state, schema v16 real-time / v17 turn-based adds only those observations: exact group ID, player-party membership, equality with rider and Mammoth groups, direct controllability, effective and raw AI enablement, and command-queue emptiness. Current PASS requires the intended rider attack actor to be the exact player-party pair-group direct-control actor. The diagnostic changes no actor AI or commands; schemas v14/v15 and all earlier history remain valid.

Mammoth-primary run `20260816T174500Z-mammoth-primary-rt-passA` isolates diagnostic-target movement from pair movement: rider/Mammoth displacement is `0.03717125 / 0`, but the target moves `0.675754547` with no KMC repath. Exact installed `UnitMovementAgent.Stop` `0x060018C2` clears `Path`, `RequestedPath`, destination, speed, and velocity; getters `WantsToMove`, `IsReallyMoving`, `Velocity`, and `Speed` are `0x060018BE`, `0x060018C0`, `0x060018E2`, and `0x060018E6`. Target provisioning had interrupted commands without clearing that residual agent state. The diagnostic click seam now stops only the disposable target and requires commands empty, agent enabled, both movement predicates false, and exact zero speed/velocity. Rider synchronization and Mammoth pathfinding authority are untouched.

Follow-up run `20260816T181500Z-mammoth-primary-rt-passA` reaches that seam but exposes exact stock command-container semantics. `UnitCommands.InterruptAll` token `0x060026C1` immediately removes interruptible active commands and clears its queue, yet skips already-finished slots; `Empty` token `0x060026A6` still sees those non-null slots until stock `RemoveFinishedAndUpdateQueue` token `0x060026BD` runs. The target-only click preparation now performs that stock sweep between InterruptAll and agent Stop. Since the queue was cleared first, no queued work can start. A live noninterruptible slot still remains nonempty and rejects dispatch.
