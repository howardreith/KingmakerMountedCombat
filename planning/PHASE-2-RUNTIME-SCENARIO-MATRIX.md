# Phase 2 runtime scenario matrix

## Private-alpha stabilization automated qualification - 2026-08-23

Status: `PASS` for the automated boundary. Exact package/DLL `b16535f94ee914c4953b40fd19e88f97dfa117f984f8134855b1eae79fa6eaaf` / `d01a75c173c692198963e4c72acb20e1c5891d89a95916a4f649ab7d1d417777` and suite snapshot `04cc10c11ff78a1899405c7387d6c990bc035e756545bff9f639694c57af62bd` produced RT A/B `81/0` each, TB A/B `105/0` each, and doorway `62/0`, aggregate `434/0`. Every process received an independent exact restoration audit before evidence inspection.

Credited runs are `20260822T222335Z-private-alpha-stabilization-v47-suite1-rt-passA`, `20260822T224523Z-private-alpha-stabilization-v47-suite1-rt-passB`, `20260822T230812Z-private-alpha-stabilization-v47-suite1-tb-passA`, `20260822T233145Z-private-alpha-stabilization-v47-suite1-tb-passB`, and `20260822T235432Z-private-alpha-stabilization-v47-suite1-doorway`. RT proves ordinary player-surface melee admission and the exact single rider chain. TB additionally proves explicit native roster/turn ownership, Mammoth-routed rider-turn movement with rider-only Move accounting, RT-to-TB and TB-to-RT pair/presentation coherence, one owned Mammoth AI reassertion, and one exact rider UI-principal restoration. Doorway retains mounting while opening and traversing the selected authored door.

Manual-only UI character/map/combat-menu, polymorph/revert visibility, mounted ranged rejection, and unmounted ranged control remain `IN PROGRESS` until the guarded human regression. No horse, persistence, mounted ranged implementation, spellcasting rule, feat, AoO, charge, Phase 3, public-release, or `main` claim follows.

## Stabilization native TB-exit rider UI-lease repair pending fresh evidence (2026-08-22)

Published suite-2 RT A/B each pass schema v44 `81/0`. TB `20260822T201000Z-private-alpha-stabilization-v46-suite2-tb-passA` is immutable uncredited `FAIL 103/1` after exact audit-before-read restoration. It passed ground movement, player-click rider melee, rule/resource ownership, pair/view/pose/roster retention, native mode restoration, and exact Mammoth AI reassertion; only final exact selection/action-bar ownership was absent while portrait and camera owner remained the rider. Forward-only schema v47 requires one event-armed, post-AI-boundary, exact-Default reselect of the captured rider plus complete following-frame UI coherence. Fresh clean-package full RT/TB A/B and doorway regression remain mandatory.

## Stabilization native TB-exit AI-lease repair pending fresh evidence (2026-08-22)

Live schema-v46 attempt `20260822T173100Z-private-alpha-stabilization-v46-tb-passA` is immutable uncredited. It proved the exact one-shot Mammoth AI-lease reassertion, then the forced critical killed the sole target and collapsed native combat/turn/resource state before transition capture; exact Pause persisted after TB false/controller clear while player combat had already ended. The superseding diagnostic repair uses the existing reversible durability lease only for this exact TB human row, recognizes that exact Pause without the transient combat flag, and preserves complete failed-evidence shape. Fresh full A/B remains required.

Status: `IN PROGRESS`. Same-package schema-v44 RT A/B on `a57b84c...` pass `81/0` each, but TB `20260822T150000Z-private-alpha-stabilization-native-exit-tb-passA` is immutable uncredited `FAIL 100/3` after exact restoration. The TB row proved ground movement, player-path rider melee, roster/turn/action ownership, and zero duplicate/AoO before stock `CombatController.HandleCombatEnd` enabled the controllable Mammoth and invalidated KMC's still-owned AI-disable lease. Forward-only schema v46 requires one exact event-armed, post-controller, owned-lease reassertion and correct rider-only Move accounting through Standard completion. Offline Release/component/harness/assembly pass `Release/231/219/282`. A fresh clean package/suite and complete RT A/B, TB A/B, and doorway restart remain mandatory.

## Stabilization human-path runtime checkpoint (2026-08-22)

Status: `IN PROGRESS`. Package `e8d507...` schema-v44 RT A/B (`...061700Z...rt-passA`, `...063700Z...rt-passB`) pass `81/0` each and qualify the exact ordinary rider-melee path only for that superseded package. Schema-v45 TB Pass A `20260822T065700Z-private-alpha-stabilization-native-mode-tb-passA` is uncredited `FAIL 28/5` despite exact restoration. The pair/view/pose/selection/action-bar/portrait/roster/turn remained valid, but the diagnostic skipped the required observable RT interval between its inner TB restore and outer ambient-setting restore, then cleaned combat under TB; it also lacked a camera-follow precondition. The narrow staged-mode and reversible camera-lease repair is offline-green `21/230/17/219/277`. Fresh clean-package RT A/B and TB A/B must restart under one new suite snapshot; no cross-package A/B credit is permitted.

## Stabilization RT/TB mode admission repair pending fresh evidence (2026-08-22)

Status: `IN PROGRESS`. Uncredited observation row `20260822T043600Z-private-alpha-stabilization-initiative-observation-rt-probeA` proves exact rider cooldown callbacks were present but the RT request entered native TB `Preparing`, so installed token `0x06009349` returned before initiative decrement. Each v44/v45 combat row now establishes an exact non-persistent real-time setting baseline before mounting or combat; v45 then stacks the native TB transition after mounting and unwinds to baseline before restoring the user's original setting. Schema shape and validators remain unchanged. Offline totals pass `21/230/17/219/277`; fresh clean-package v44 RT A/B and v45 TB A/B remain required, and no overlapping preflight/run receives credit.

## Private-alpha stabilization pending fresh evidence (2026-08-21)

Status: `IN PROGRESS`. Human play against exact historical handoff `ff4bae7ca5dc145373771b718574d15f7babdd5e` found non-world UI dismount/invisibility, polymorph replacement-view loss, ordinary rider-melee failure without rejection evidence, and broken turn-based roster/ground movement. The historical package and all prior qualifications remain immutable; they do not qualify the stabilization repair.

New forward-only combat schemas v44/v47 cover `mounted-rider-melee-human-play-path-rt` and `mounted-rider-melee-human-play-path-tb`. Both bind player-facing controller arming, exact native direct-view click admission, zero rejection codes, the unchanged bounded Mammoth-origin reach contract, one rider attack chain, rider-only Standard ownership, no incidental opportunity/duplicate chain, pose/selection coherence, and cleanup. Schema v47 supersedes historical v45/v46 for the TB human path: it additionally mounts before RT-to-TB transition, requires rider/Mammoth/target native roster membership, executes exact Mammoth Move-slot ground movement under the rider turn with rider-only Move accounting and slot restoration, proves the pair survives TB-to-RT restoration, binds the exact rider UI/camera identity and accepted presentation after both transitions, proves one exact native TB-exit reassertion of KMC's already-owned Mammoth AI-disable lease, and proves one exact post-boundary rider UI-principal restoration.

UI character/map/combat-menu and polymorph visibility cannot be safely proven through synthetic UI input. Their exact installed seams, deterministic policies, source order, and structured presentation telemetry are offline-gated; final credit requires the guarded manual regression package. Mounted ranged rejection is exact-pair-local and deterministic in source/unit coverage, with manual RT/TB confirmation required. Fresh RT A/B, TB A/B, and doorway stabilization rows now have the exact live credit recorded above.

## Bounded mounted reach and same-package controls qualified (2026-08-21)

Status: `PASS` for the exact Medium-humanoid/Mammoth/one-handed private-alpha profile. Clean published commit `1fa5c2e6e841f4025f36283fd5bba7ae94c23ba5`, package SHA-256 `a448bf8e1aa36163c6a30b3c0c116b0b5fce36eb84123d6ae3756be9537932be`, DLL SHA-256/MVID `9f715f4ddd4e086cee8f2aa3aaa4e746401cec113fc529809d1376eab1caf6c1` / `292982ba-6486-4807-b4fa-c15dc0558266`, and suite `20260821T181000Z-mounted-reach-suite1` / snapshot SHA-256 `3aea4cdf34a706ca207886d97fb2724be7f574732421edb33d7a8a229950a3e1` bind the complete set.

Rider RT A/B (`20260821T183500Z-mounted-reach-rider-rt-passA`, `20260821T184000Z-mounted-reach-rider-rt-passB`) pass `60/0` each; rider TB A/B (`20260821T190000Z-mounted-reach-rider-tb-passA`, `20260821T191500Z-mounted-reach-rider-tb-passB`) pass `64/0` each. Mammoth RT A/B (`20260821T193000Z-mounted-reach-mammoth-rt-passA`, `20260821T194800Z-mounted-reach-mammoth-rt-passB`) pass `60/0` each; Mammoth TB A/B (`20260821T201500Z-mounted-reach-mammoth-tb-passA`, `20260821T203500Z-mounted-reach-mammoth-tb-passB`) pass `65/0` each. Reach credit is `498/0`. Same-package controls A/B (`20260821T211400Z-mounted-reach-controls-passA`, `20260821T224000Z-mounted-reach-controls-passB`) pass `80/0` each, adding `160/0`; total qualification is `658/0`.

Every process received an immediate independent restoration audit before evidence inspection. The exact suite save/Mods inventories, immutable Baseline, restored Working, and zero process/lock/sentinel/deployment residue passed after all ten credited processes. The target begins outside both independently derived radii; only the requested actor dispatches inside its own unchanged boundary; exact weapon/corpulence inputs and four targetability directions remain unchanged; each attack row emits one intended actor chain and zero unexpected pair/AoO/duplicate chain. Controls preserve invalid-target, target-death, cleanup, and ordinary non-mounted behavior. This does not generalize reach to other profiles and does not qualify explicit mounted AoO or charge.

## Bounded mounted-reach proof pending fresh runtime A/B (2026-08-21)

Stationary rider and Mammoth-primary combat rows now emit strict schema-v42/v43 evidence for independent Mammoth-origin rider and stock Mammoth radii, a real initial position outside both, actor-specific inside-boundary dispatch, immutable weapon/corpulence inputs, and bidirectional hostile targetability of both pair members. Deterministic validators reject every material field mutation and preserve all historical combat schemas. Production range behavior is unchanged. Runtime credit requires fresh same-package RT/TB A/B for both stationary actors plus fresh non-mounted control A/B, with one suite snapshot and restoration-before-read. Status remains `IN PROGRESS`.

## Opportunity-isolation requalification complete after launcher repair (2026-08-21)

Clean published implementation `4d1737c0ce24502bb3ec7c383aa008dad14a642e`, package SHA-256 `5793c05dffb69bd1417bfd6ce9f239f34be788673092da526fff1df4ffc3e1dd`, and suite `20260821T153600Z-opportunity-isolation-metadata-repair-suite1` / snapshot SHA-256 `16315b6f15e8c39fb4046578efedbb5c24dd31307c069aa65be0f42a1bdb0446` produced a fresh complete set. RT A/B `20260821T160100Z-opportunity-isolation-metadata-repair-rt-passA` / `20260821T160700Z-opportunity-isolation-metadata-repair-rt-passB` each pass schema v34 `55/0`; TB A/B `20260821T163100Z-opportunity-isolation-metadata-repair-tb-passA` / `20260821T164700Z-opportunity-isolation-metadata-repair-tb-passB` each pass schema v35 `59/0`. Combined credit is `228/0`.

All four mandatory independent audits ran after launcher completion and before evidence inspection. Each proves the same admitted save digest `99b21a2ca3864dbaac7fac80dde5e382df4b6ab3738f59feccd8117bab9f4c89`, Mods digest `1f724e85026dd868b3f8bad795f159c969f9ac126e9a55a41c82a78fc5f52b0a`, immutable Baseline, restored Working, and zero process/lock/sentinel/deployment residue. Every credited row proves one exact rider attack/roll/damage chain, zero incidental opportunity attacks or duplicate chains, Mammoth-only approach authority and Move-slot ownership, zero repath, stationary target, mode-correct rider costs, zero Mammoth Standard cost, accepted presentation/UI, and exact cleanup. This supersedes the pending restart immediately below; the failed TB launcher attempt remains immutable and uncredited. Explicit mounted-AoO remains a separate unqualified stretch feature.

## Opportunity-isolation package restart after TB launcher metadata race (2026-08-21)

RT runs `20260821T134400Z-opportunity-isolation-rt-passA` and `20260821T140900Z-opportunity-isolation-rt-passB` pass `55/0` each on the current historical package/suite. TB `20260821T151200Z-opportunity-isolation-tb-passA` later emits game `PASS 59/0` but is uncredited: a transient null process path caused launcher failure before game evidence, restoration was blocked, and guarded recovery was required. The bounded process-metadata repair preserves the existing capture deadline and exact identity checks. After publication/package change, restart RT A/B and TB A/B from zero.

## Core combat-control finalization qualified (2026-08-21)

`combat-core-control-suite` is `PASS` A/B on one exact package and suite. Runs `20260821T125000Z-core-combat-controls-launcher-observation-passA` and `20260821T125800Z-core-combat-controls-launcher-observation-passB` each pass `80/0`: invalid target `17/0`, exact pre-child target death `24/0`, cleanup `22/0`, and non-mounted control `17/0`. Combined credit is `8/8` rows and `160/0` assertions. Both independent audits precede evidence inspection and prove exact full save/Mods restoration and zero residue. This closes the control-suite blocker only; movement-to-attack opportunity isolation remains unqualified pending RT A/B and TB A/B.

## Core control game PASS / launcher-observation checkpoint (2026-08-21)

From clean package `5f8fbbc3b21797bdb72842d7e59ba8ecfb3563fe84892aac4ac8e42c51cbb145` and suite snapshot `d60ee93fda6575c5160a7df1b6f3b740b7d6570ce0394984d0924a2c6160f15d`, uncredited run `20260821T120800Z-core-combat-controls-finalization-passA` produces exact game `PASS 80/0` and an exact manifested combat artifact: invalid `17/0`, target-death `24/0`, cleanup `22/0`, non-mounted `17/0`. It proves the target-admission, outcome-reset, and game manifest repairs, but receives no A credit because an earlier launcher exception caused exit handling, fallback validation masked that first error, no final launcher result was written, and guarded recovery was required after natural game exit. Audit-before-read passed exactly.

The next package changes observation/fallback behavior only: persist the original launcher error in orchestration before exit waiting and allow FAIL fallback construction to bind an incomplete manifest without calling scenario-semantic PASS validation. Ordinary game/final/artifact validators still reject missing or incomplete evidence. No timeout, process attribution, suspicious-window, target, attack, resource, cleanup, or restoration gate changes. Fresh A/B remains mandatory.

## Core combat-control interrupted-process diagnosis (2026-08-21)

Clean published commit/package `7fb77e80e045a995efaf27a2cb9ed869b83db9c7` / `8fd35041e627a66007bdf556b486d5c6e1e23fea17c6057358440a6303c7c2df` and suite snapshot `6489837481e74e32b2cd0d7afce14d1ceb59a4875a516c3eb3a916e36d2dd9de` produced interrupted uncredited attempt `20260821T105600Z-core-combat-controls-admission-passA`. Guarded recovery and the independent restoration audit completed before evidence inspection and proved exact suite save/Mods state plus zero residue. The game evidence is `FAIL 79/1`: invalid-target `17/0`, target-death `24/0`, cleanup `21/1`, and non-mounted `17/0`. Target admission and the exact pre-child invalidation repair are therefore observed healthy; only cleanup's prior-outcome diagnostic failed.

The controller intentionally clears `LastOutcome` on every accepted new command, so the cleanup row cannot require reference equality with target-death's completed prior outcome. It now requires the exact admitted active command and a null current terminal outcome while preserving unchanged historical identity for rows that submit no command. Separately, an empty orchestration manifest was created before the combat JSONL and silently reused by game finalization; finalization now atomically replaces it with the exact allowlisted closed artifacts, while external unmanifested/mutated-artifact rejection remains strict. Kingmaker's later failure to exit is not attributed and no timeout or terminal-result gate changes. Full offline totals pass `21/217/17/207/255`; fresh clean-package A/B remains mandatory.

## Core combat-control repaired-process admission diagnosis (2026-08-21)

Clean published input `2c81081dc4a60212fb864aeb98aeb365925546ad`, package/manifest/DLL SHA-256 `7235315a2d03ab2566e7495981a8adb845a9a8aeca4e8773124d512c9e8f6d75` / `c6ed6ff0873bd43d9d9d142c72f98f9d3c1f390bcfe1360ec85c45370c71ec1c` / `1787f067843a9ca9397112bf9c17fc67eaae9fef2601796880246620a7ed5033`, MVID `732fbf7f-c65d-4c66-aa4b-1105c6b6f207`, and suite snapshot `4b6231f84ca5a3624fbae8d0d719b2c235a7a1e713e208f31bd372a0ad13843a` passed separate RT/TB WhatIf gates and independent zero-mutation audits. Repaired process `20260821T102109Z-core-combat-controls-repair-passA` is immutable uncredited `FAIL 77/1`; its immediate independent audit passed before evidence read. Invalid-target `17/0`, exception-cleanup `21/0`, and the repaired non-mounted row `17/0` pass. Target-death remains `22/1` with exact lethal damage, dead target, retained pair, zero child/rule/resource chain, and full cleanup.

The bounded game log proves the diagnostic changed the target after click submission but before `MountedPairAttackCommand.OnStart` accepted it; `OnStart` therefore rejected `target-conscious-or-child-started,target-not-finally-dead`, so the new pre-child `OnTick` cancellation seam never ran. The control engine now waits for the exact wrapper to report the exact transaction target in `Approaching` or `Attacking` with zero child starts before applying target death or exception cleanup. Source-order coverage requires that admission predicate before either mutation. Complete offline gates pass `21/217/17/207/255`; a new clean package/suite and fresh A/B are required, and neither failed process receives credit.

## Core combat-control first-process diagnosis and repair checkpoint (2026-08-21)

Clean published input `c8994d5b6bccfe3b4944b0c11581ffff652045b2`, package/manifest/DLL SHA-256 `7045cce712a7286e0b943904d241f33537b238ffc3bee53267bc468333d50aa7` / `9348d4602aeda61ccee34f9a299f3b02e30671459efc50c0c58e673c53c04048` / `2e917a4bce0219c3231ecb708a31d1208ba8c7ced9c76f4b72e0fb8b6f42b7e4`, MVID `8b65f641-321c-44cf-a3bb-e30ffc45d2c9`, and suite snapshot `080aa600afebb4fe8c8fb7b0f6f9ef70ee37a9c43efc7091694e731f2d61869f` passed separate RT/TB WhatIf gates plus independent zero-mutation audits. First live process `20260821T100300Z-core-combat-controls-passA` is immutable uncredited `FAIL 76/2`; its immediate audit passed before evidence read. Invalid-target `17/0` and exception-cleanup `21/0` passed. Target-death `22/1` proved exact lethal public damage, death transition, pair retention, zero child/rule/resource chain, and cleanup, but the command transaction faulted before its native Interrupt terminal reason. Non-mounted `16/1` proved every stock-delegation/agent/resource observation true but incorrectly treated the unchanged historical `LastOutcome` as an active command.

The narrow repair cancels only the exact accepted target before child start with terminal reason `target invalidated before child attack`, then issues native Interrupt; wrong-target, terminal, and post-child transactions remain rejected. The control engine now proves no active command and identity-unchanged outcome evidence instead of requiring history to be empty. Deterministic coverage pins exact-target/pre-child/idempotent/post-child semantics and source order before generic liveness faulting. Full offline test totals are `21/217/17/207/255`; fresh clean-package suite admission, separate WhatIf gates, and restarted A/B remain required.

## Core combat-control suite implementation checkpoint (2026-08-21)

The additive save-backed `combat-core-control-suite` executes exactly four ordered real-time rows: `mounted-rider-melee-invalid-target`, `mounted-rider-melee-target-death`, `mounted-rider-melee-cleanup`, and `non-mounted-melee-control`. It uses the already-qualified Medium-humanoid/Mammoth pair and disposable target, places every target outside the exact measured pair radius, and exercises only production click/controller, public target `Damage`, relationship `Dismount(Exception)`, and controller `NotHandled` seams. PASS requires null rider/Mammoth target rejection without a command or resource cost; active-command target death and repeated exception cleanup before child admission; non-mounted stock delegation; zero child/attack/roll/damage/opportunity/forced-d20 chains; exact action-resource equality; and complete target/combat/agent/pause/relationship restoration. One schema-v1 JSONL artifact binds exact row order, pair and unique target identities, production paths, results, and cleanup. Historical attack schemas and credited evidence are unchanged.

Status: offline implementation and deterministic validator coverage PASS; fresh clean-package same-suite A/B runtime evidence remains required before any of these four rows receives credit.

## Qualification-suite external-state admission (2026-08-20)

All future save-backed rows are bound to one append-only qualification-suite snapshot. Admission requires quiescence plus two identical complete save-root and Mods-root scans. The snapshot binds exact commit/package/manifest/DLL/MVID, permanent KMC fixture identity, historical authority identifiers, and full path/type/length/timestamp/raw-hash inventories. Same-package A/B rows may be paired only when suite ID and snapshot SHA-256 match. Stable foreign drift between suites starts a new suite; any drift during a suite rejects that suite's remaining credit and requires fresh A/B. Restoration audit against the exact snapshot precedes gameplay evidence inspection after every process.

Status: IN PROGRESS

Native incapacitation cleanup is qualified on clean commit/package `ee08638ff19923b4f0aaf0d1ca03830de802d0cc` / `e68d90a25730760a52167e7ee41366c6490f49a7f134bb447e805aed745c7dc0`, suite snapshot `37a1989d174cdb0f44026ba64470e4fda1d1febc673fabffb668d977466f5897`. Rider A/B `20260821T083800Z-rider-native-incapacitation-passA` / `20260821T084000Z-rider-native-incapacitation-passB` and Mammoth A/B `20260821T084500Z-mammoth-native-incapacitation-passA` / `20260821T085000Z-mammoth-native-incapacitation-passB` each pass schema v7 `49/0`. All four bind one exact public nonlethal damage transition, one native exact-actor `Conscious -> Unconscious` callback, one `UnitIncapacitated` delivery, first-delivery successful cleanup, zero cleanup errors/death/duplicates, accepted presentation and exact external restoration. Four independent audits passed before evidence reads.

Repaired rider attempt `20260821T073500Z-rider-native-incapacitation-passA` is immutable uncredited final-validator `FAIL`, with immediate audit-before-read. Game evidence passes schema v7 `49/0` and proves first-delivery cleanup, but Unity omitted the empty `cleanupErrors` array. The validator now accepts absent-or-empty only for successful cleanup, requires present non-empty errors for the failed diagnostic shape, and rejects errors on success. Fresh package/suite rider A/B and Mammoth A/B remain required.

Cause probe `20260821T063244Z-rider-native-incapacitation-cause-probe` is immutable uncredited structured `FAIL`, with exact audit-before-read. It proves the first cleanup failure is the avoidance effective-state check after successful KMC lease release. Installed code makes effective avoidance disabled whenever the actor is unconscious, independent of the CountingGuard setter. The repair separates lease ownership from effective native state, retains strict mismatches, and requires fresh same-package rider A/B plus Mammoth A/B; no historical process receives credit.

Schema-v7 rider probe `20260821T055657Z-rider-native-incapacitation-probe-v7` is immutable uncredited `FAIL 47/2` with audit-before-read. It confirms the first delivery faults inside `RestoreMovementAuthority`, while the ordinary retry still restores exact Unmounted state. The outer wrapper's inner cause was not retained, so a bounded cause-chain diagnostic and fresh process are required before the narrowest behavioral repair can be selected.

Schema-v6 rider probe `20260821T050300Z-rider-native-incapacitation-probe-v6` is immutable uncredited `FAIL 47/2`, with immediate restoration audit before read. It proves exact `Conscious -> Unconscious`, later `Unconscious -> Conscious` recovery, and one native delivery, but that delivery records `Mounted -> Faulted`/cleanup false before a later residue-free retry. Schema v7 captures the first cleanup errors and admits only this exact shape as structured FAIL; PASS still requires first-delivery `Mounted -> Unmounted` cleanup success.

Native rider probes `20260821T032700Z-rider-native-incapacitation-passA` and `20260821T040400Z-rider-native-incapacitation-probe-v5` are immutable uncredited `FAIL 44/1`; each received an independent restoration audit before evidence inspection. Schema v5 proves the rider began Conscious/awake/in `AwakeUnits`, public `Damage=46` was applied for HP/Constitution `45/11`, one stock lifecycle delivery dismounted the pair, and later state was Conscious/awake at damage `38`. It therefore rejects the controller-exclusion theory but cannot identify the transient callback. Schema v6 adds only bounded exact-pair callback observation before cleanup plus immediate-damage/recovery evidence. PASS requires one exact `Conscious -> Unconscious` callback, one native cleanup delivery, no death, and full restoration; historical schema v2-v5 shapes remain valid. Fresh clean-package evidence is required.

Native incapacitation delivery is offline-green from starting clean HEAD `c1338ec6721a5d6627b1a568c4d7979b745de355`. Separate single-row scenarios `mounted-pair-rider-native-incapacitated-cleanup` and `mounted-pair-mount-native-incapacitated-cleanup` use schema v4. Each requires a healthy conscious actor, applies only public `UnitEntityData.Damage = HitPoints + 1` within the nonlethal Constitution margin, then waits for exact stock `UnitLifeController.TickOnUnit -> IUnitLifeStateChanged.HandleUnitLifeStateChanged` delivery. Evidence binds actor identity, damage/HP/Constitution values, `Conscious -> Unconscious`, exactly one ordered `UnitIncapacitated` ledger record, Mounted-to-Unmounted cleanup, accepted-pose restoration, and zero direct lifecycle-handler invocation. Historical schema v2/v3 rows and failed evidence are unchanged. Offline totals are source/component/visual/harness/assembly `21/213/17/203/255`; fresh clean-package rider A/B and Mammoth A/B with restoration-before-read remain required.

Active-command combat end is qualified on clean commit/package `6b9dd14c46907732ba48c3730bf8ab986974170c` / `6145369509eaf69eae30e22a42f72de63f0d5ddae707a22625cd54a47a03a6d1`, suite snapshot `ca0b8b9c1e926e5ddf3d847ff74d9646c1e52416e931e2949892be00e5e8783a`. RT A/B pass schema v40 `52/0` each and TB A/B pass schema v41 `56/0` each. Every row binds the exact direct handler and two mounted-state non-cleanup deliveries, zero child/rule/AoO chains, command/agent/queue restoration, retained pair/presentation/UI, mode-specific RT-zero/TB-rider-Move accounting, exact cleanup, and restoration-before-read. A naturally generated global transition is not claimed.

Active-command combat-end RT/TB rows are offline-green at source/component/visual/harness/assembly `21/213/17/202/254`. Schema v40/v41 extends the exact qualified termination shape with two ordered direct `IPartyCombatHandler.HandlePartyCombatStateChanged(false)` deliveries and exact mounted-state non-cleanup ledger evidence. Runtime credit requires a fresh clean package/suite, separate mode WhatIf audits, then RT A/B and TB A/B. This does not claim a naturally generated global combat-state transition or native incapacitation delivery.

Direct combat-lifecycle boundaries are qualified on clean commit/package `82935e9fdd263fac698a35d07fdc7a090e58927c` / `7aef5b851a75d6ba18e8cd4602eff14cbaf2cd4fdb764ac61e1e7254380e3ad5`, suite snapshot `c070d0d6697e8a21c58bb8c8952ac2d810c703db28e3f03ea25717be5d114df8`. Fresh Pass A/B each pass nine schema-v3 rows at `400/0`, with audit-before-read. Direct start/end retention, death, direct incapacitation fail-safe, companion removal, view destruction, exception cleanup, pose restoration, and external restoration are PASS. Native incapacitation delivery and active-command combat-end cancellation remain open.

Combat-lifecycle diagnostic protocol is offline-green at `21/213/17/199/254` from starting clean HEAD `dd768636e48b0ebfef98d1595f57662a22520f3d`. The additive ordered suite covers combat start/end retention; rider/Mammoth death; rider/Mammoth incapacitation; companion removal; view destruction; and exception cleanup under schema v3. It preserves the historical schema-v2 combat-start-cleanup row. Direct handler/service identity and delivery limits are explicit: native EventBus delivery, real incapacitation, and active-command combat-end cancellation remain open until their own evidence qualifies them.

First clean-package process `20260821T014600Z-combat-lifecycle-passA` is immutable uncredited final-validator `FAIL`; independent restoration passed before evidence inspection. Its game evidence is exact `PASS 400/0`, but the validator rejected truthful null/inactive pose identity during unmounted phases. The phase-aware validator repair is deterministic-green; fresh commit/package/suite A/B remains required.

Movement-to-attack opportunity isolation is qualified on clean commit/package `632e3710ea1be2d7331eea7f07cf803295b1ad1f` / `0cca1e30181e6021426c4bfdb8906178636db790a3e6fff4c3aff95ab46f6827`, bound to suite `20260820T220930Z-opportunity-isolation-suite2` and snapshot SHA-256 `5bc3ffc80819f72304d3ebc063265ab1d585ac64280059cb1fa786298e41f4d7`. RT A/B pass schema v34 `55/0` each; TB A/B pass schema v35 `59/0` each. The four runs prove the exact rider-owned attack, Mammoth-owned approach path and Move slot, mode-correct rider costs, zero Mammoth cost, one attack/roll/damage chain, zero incidental opportunity attacks, zero duplicate/repath, stationary durable target, accepted presentation/UI, exact cleanup, and independent suite restoration. This closes the core movement-to-attack row. Exact active-command cancellation/interruption is separately qualified below; explicit mounted-AoO remains open.

Mammoth-primary stationary combat is qualified on clean commit/package `defbbd0516ec1ad16eac7e83dd69ae1fc41fc8c6` / `8adb516b6970b07d714dcb46e3418d2fc0fc68a92af218b6170a3427598bab2d`. Fresh same-package RT A/B `20260816T194000Z-mammoth-primary-rt-passA` / `20260816T194500Z-mammoth-primary-rt-passB` pass schema v26 at `54/0` each; TB A/B `20260816T195000Z-mammoth-primary-tb-passA` / `20260816T195500Z-mammoth-primary-tb-passB` pass schema v27 at `59/0` each. All four bind exact Mammoth actor/readiness/command/resource/weapon/target/rule identity, exactly one attack, no rider duplicate/cost, mode-correct Standard accounting, zero movement/repath/AoO/charge, healthy accepted pose, cleanup, coherent manifests, and independent exact restoration. The prior actor-readiness attempt remains immutable uncredited history.

Prior hostile-brain attempt `20260816T183000Z-mammoth-primary-rt-passA` is immutable uncredited `FAIL` `51/1`. Exact Mammoth attack/resource/weapon/rule/life/pose/cleanup/restoration evidence passes; only target displacement `0.6614979` fails. Raw `m_AiEnabled=false` cannot disable effective AI on a non-direct target, while exact `AiBrainController.TickBrain` selects commands through `IsBrainActive`. A target-only reversible, continuously validated schema-v24/v25 brain lease is offline-green at `21/204/17/175/245`; schemas v1-v23 remain valid.

Prior finished-command attempt `20260816T181500Z-mammoth-primary-rt-passA` remains immutable uncredited `FAIL` `30/1`. It stops before click because the target retains an already-finished command slot after `InterruptAll`; cleanup and external restoration are exact. Stock `RemoveFinishedAndUpdateQueue` now drains only that supported target boundary after the queue is cleared and before stopped-agent/empty proof.

Current Mammoth-primary target-stationarity attempt `20260816T174500Z-mammoth-primary-rt-passA` is immutable uncredited `FAIL` `50/2`. Exact Mammoth attack/resource/weapon/rule identity, durability lease, target life, pair stationarity, accepted pose, and external restoration pass. Target-only displacement `0.675754547` and deferred lifecycle confirmation fail. Installed `UnitMovementAgent.Stop` proves the diagnostic target must clear both active and requested paths after translocation; the repair adds exact pre-click stopped-agent gates and a later zero-residue `DestroyRequested -> Removed` confirmation retry. Offline totals are `21/204/17/174/240`; fresh same-package RT A/B and TB A/B remain required.

All core claims require two consecutive fresh-process passes from one clean commit/package. Every run must bind branch, commit, version, package/DLL SHA-256, MVID, exact fixture authority, telemetry/artifacts, and external restoration. `KMC_AUTOMATION_BASELINE` is immutable; only exact `KMC_AUTOMATION_WORKING` may enter the guarded write transaction.

## Phase 2A presentation and safety

| Order | Scenario | Required observations | Pass A | Pass B |
|---:|---|---|---|---|
| 1 | `player-action-availability` | supported/unsupported eligibility and exact reason; no mutation | PASS — `20260814T234000Z-player-action-availability-passA`, `29/0` | PASS — `20260814T234500Z-player-action-availability-passB`, `29/0` |
| 2 | `mount-dismount-user-flow` | player action, selection normalization, rollback, repeat activation | PASS — `20260814T235000Z-mount-dismount-user-flow-passA`, `44/0` | PASS — `20260814T235500Z-mount-dismount-user-flow-passB`, `44/0` |
| 3 | `pose-idle` | mounted baseline, jitter, lower-body pose, cleanup restore | PASS — `20260815T044100Z-pose-idle-dev5-passA`, `26/0` | PASS — `20260815T044700Z-pose-idle-dev5-passB`, `26/0` |
| 4 | `pose-walk-run` | mount authority, pose stability, frame cost, upper-body freedom | PASS — `20260815T053000Z-pose-walk-run-dev7-passA`, `68/0` | PASS — `20260815T054500Z-pose-walk-run-dev7-passB`, `68/0` |
| 5 | `pose-turn-stop` | substantial turn/reversal, stop/start, no cumulative drift | PASS â€” `20260815T062000Z-pose-turn-stop-dev9-passA`, `80/0` | PASS â€” `20260815T063000Z-pose-turn-stop-dev9-passB`, `80/0` |
| 6 | `pose-doorway-formation` | selected doorway, stock group command, non-pair isolation | PASS — `20260815T065000Z-pose-doorway-formation-dev10-passA`, `88/0` | PASS — `20260815T071000Z-pose-doorway-formation-dev10-passB`, `88/0` |
| 7 | `pose-equipment-variants` | one-handed, shield when available, two-handed when safe | PASS — `20260815T072000Z-pose-equipment-variants-dev10-passA`, `28/0` | PASS — `20260815T073000Z-pose-equipment-variants-dev10-passB`, `28/0` |
| 8 | `ui-selection-portrait-actionbar` | selected identity, click rider/mount, portrait, circle, action bar | PASS - `20260815T080000Z-ui-selection-portrait-actionbar-dev11-passA`, `30/0` | PASS - `20260815T081500Z-ui-selection-portrait-actionbar-dev11-passB`, `30/0` |
| 9 | `camera-follow-and-command-routing` | camera subject through movement/switches and ground commands | PASS - `20260815T130000Z-camera-follow-command-routing-dev12-passA`, `61/0` | PASS - `20260815T131500Z-camera-follow-command-routing-dev12-passB`, `61/0` |
| 10 | `native-save-clean-dismount` | actual save request delivery, clean dismount, no serialized relationship | PASS — `20260815T010000Z-native-save-qualified-passA`, `63/0` | PASS — `20260815T010500Z-native-save-qualified-passB`, `63/0` |
| 11 | `native-area-clean-dismount` | actual area boundary delivery where safely observable | PASS — `20260815T014500Z-native-area-qualified-passA-exactphase`, `47/0` | PASS — `20260815T015000Z-native-area-qualified-passB-exactphase`, `47/0` |
| 12 | `native-mode-transition-cleanup` | actual RT/TB transition delivery where safely observable | PASS — `20260815T015500Z-native-mode-qualified-passA-exact`, `60/0` | PASS — `20260815T020000Z-native-mode-qualified-passB-exact`, `60/0` |
| 13 | `presentation-residue-and-uninstall-safety` | mod disable/feature absent, transform/UI/entity residue zero | PASS — `20260815T020500Z-residue-uninstall-qualified-passA-exact`, `63/0` | PASS — `20260815T021000Z-residue-uninstall-qualified-passB-exact`, `63/0` |

## Combat rows authorized after exact visual acceptance

| Tranche | Scenarios | Status |
|---|---|---|
| Rider melee | mounted hit/miss RT, hit TB, invalid target, target death, cleanup, non-mounted control | PASS - stationary hit RT/TB and miss RT PASS A/B; core-control A/B each `80/0` qualify invalid target, exact pre-child target death, cleanup stress, and non-mounted stock delegation |
| Mount attack | `mounted-mammoth-primary-hit-rt` and `mounted-mammoth-primary-hit-tb`; exact actor/initiative/command/resource/weapon/target/rule identity, rider unchanged, continuous target-only brain suppression/restoration, no duplicate/repath, bounded Mammoth turn, cleanup/restoration | PASS — RT A/B `54/0` each and TB A/B `59/0` each on exact commit/package `defbbd0` / `8adb516b...`; four independent restoration audits PASS |
| Action economy/movement | entry/exit RT/TB, movement-to-target, cancellation, obstacles/doorway, resource accounting | PASS for the scoped core - repaired-package movement-to-attack RT A/B schema v34 `55/0` each and TB A/B schema v35 `59/0` each; cancellation/interruption RT A/B `51/0` each and TB A/B `55/0` each; active combat-end RT/TB A/B qualified; exact mode-specific resource accounting and presentation doorway controls qualified |
| Lifecycle | rider/mount death and incapacitation, view/party/save/load/area/disable/exception boundaries | PASS - nine direct combat-alpha schema-v3 rows PASS A/B `400/0` each; active-command combat-end RT/TB A/B PASS; native rider and Mammoth incapacitation A/B PASS schema v7 `49/0` each, all with audit-before-read |
| Stretch | reach, AoO, and basic charge RT/TB plus isolation | Reach `IN PROGRESS` pending fresh RT/TB A/B; explicit mounted AoO `DEFER — EVIDENCED`, absent/default-off because exact installed ownership is independently per unit and safe pair-wide arbitration would require a broad engagement layer; basic charge `DEFER — EVIDENCED`, absent/default-off because stock inseparably binds one caster as mover/attacker and neither rider nor Mammoth preserves Architecture B ownership |

The user accepted exact review commit/package `09a63729e0847c540ae7e79e9e3876d005ee9afe` / `80b0498cd9d1e6d8dd518ffd8d343af56bcc711306492548c221b9559c98cef7`. Combat implementation is authorized; no runtime credit is inferred from that acceptance.

Current movement-to-attack checkpoint is the repaired-package four-run `228/0` matrix above. All earlier movement failures, including the raw-slot opportunity duplicate and diagnostic target-death failure, remain immutable uncredited evidence. Stationary rider hit RT/TB, rider miss RT, Mammoth primary RT/TB, rider movement-to-attack RT/TB, exact active-command cancellation/interruption and combat-end RT/TB, core invalid-target/target-death/cleanup/non-mounted controls, direct lifecycle/invalidation, and native rider/Mammoth incapacitation are qualified A/B. Only the bounded reach, explicit AoO, and charge stretch contracts remain open in this combat matrix.

Cancellation/interruption rows `mounted-rider-melee-command-cancel-rt/tb` and `mounted-rider-melee-command-interrupt-rt/tb` are qualified by two fresh processes per row from package `918c1dc...` and suite snapshot `31fb0f1...`. The set distinguishes the real Stop boundary from native wrapper interruption while proving pre-attack progress, pre-child zero-Standard accounting, RT-zero/TB-actual rider Move accounting, zero rules, exact command removal, relationship preservation, and exact restoration. Next work is the remaining action-economy and lifecycle/invalidation matrix.

First movement-to-attack attempt `20260816T210000Z-rider-move-attack-rt-passA` is immutable uncredited `FAIL` `45/10` from clean commit/package `2e90d6b64c9fffd30042104b07d5431dd18b1fa8` / `ff8ed2ff6f02ab32828b1fa6aba13cd833f3e959c2863183d7179c87d3384dbe`. The exact out-of-range placement, rider wrapper, Mammoth executor and stock agent, suppressed rider agent, accepted pose, selection/UI, stationary target, cleanup, and external restoration all pass, but delegated ticks and pair displacement are zero across 752 command observations. Installed `UnitActionController.TickOnUnit` stops stock movement while `UnitCommands.Empty` is true. Schema v30/v31 now binds exact Mammoth Move-slot ownership, an empty queue, no replacement, mode-correct control, positive progress, successful completion, and exact slot restoration. Historical v28/v29 remains valid and the failed run is not reinterpreted.

The stock-slot repair qualifies RT A/B on clean commit/package `d0f2f2de975bb779ef34ebb0b10a7cf415bef537` / `37a10286402730a8a105d74dad84e94ad34d8ebb82696ae5e1fe54891b51c7ed`: runs `20260816T214500Z-rider-move-attack-rt-passA` and `20260816T220000Z-rider-move-attack-rt-passB` each pass schema v30 `55/0`. First TB run `20260816T221500Z-rider-move-attack-tb-passA` is immutable uncredited `FAIL` `58/1`; every native turn, movement, resource, attack, pose, cleanup, and restoration gate passes, and only the filtered slot projection fails. Exact `UnitCommands.Move` returns null for a finished command still present in the raw slot. Schema v32/v33 reads `GetCommand(CommandType.Move)` and retains strict replacement/missing/queue/success/restoration gates; fresh repair-package RT/TB A/B is required.

Stationary mounted rider melee in real time is qualified by fresh same-package runs `20260816T084500Z-rider-hit-rt-probeQ` and `20260816T090000Z-rider-hit-rt-passB`, each `47/0`, from clean guarded-published commit `07d44eb89c6a533bc99e386bad6fd3dac4a9ac73`, package/manifest/DLL SHA-256 `af4855f93e31084b2406a2b9e527b5d8587c5a7b82a52d61bb3863d7af777459` / `10b8bae35848e919259839c4af9aaa265a6762b247b7b3a781a1f836ea42076a` / `e3ca251a6a04da607f9fb6bc21605d32756e2648769fc341d6a6575b1616db09`, MVID `06e14d27-85a7-49f6-900c-94ca99f68f04`. Each proves one rider-initiated native attack/roll/damage, eight damage, no unexpected pair attack, one rider Standard charge, zero Mammoth resource charge, zero repaths, exact combat-memory removal, healthy accepted Mammoth pose, zero owned residue, and exact external restoration. This fills only the stationary mounted-hit-RT claim; miss RT, hit TB, invalid target, target death, cleanup stress, and non-mounted control remain open.

Stationary rider-melee diagnostic ledger through Probe K remains unqualified. Probe K `20260816T070000Z-rider-hit-rt-probeK` is final `FAIL` `39/7`: it proves exact native memory/combat preparation, dispatch readiness, accepted click, target safety, Mammoth-only movement authority, and exact external restoration, then exhausts four no-op repaths because native child admission measures the offset rider executor while the authoritative Mammoth is already within `2.25020623 / 2.37020588` range. The bounded rider-native admission repair is offline-green at source `21/0`, component `189/0`, visual `17/0`, harness `154/0`, and assembly `166/0`; both real-time Pass cells remain `TODO` pending two fresh complete passes from one clean package.

Probe L `20260816T072000Z-rider-hit-rt-probeL` remains final uncredited `FAIL` `42/5` with exact restoration. It proves the repaired range bridge, one native child start, zero repaths, native rule object creation, and rider-only Standard charging, then reaches a generic in-flight liveness rejection before completed rule events. Schema v4 adds only exact terminal-reason/named-gate evidence and retains schema-v1/v2/v3 history; its offline gate is `21/0`, `191/0`, `17/0`, `155/0`, and `167/0`. Both rider-melee real-time Pass cells remain `TODO`.

Probe M `20260816T074000Z-rider-hit-rt-probeM` remains final uncredited `FAIL` `42/5` with exact restoration and names only `target-conscious`. The phase-aware repair requires consciousness at admission and permits unconsciousness only after exact one-child start so native animation completion can publish its result; target removal/final death and duplicate starts remain rejected. Offline gates are `21/0`, `192/0`, `17/0`, `155/0`, and `167/0`. Both real-time Pass cells remain `TODO`.

Camera qualification note: dev.11 Pass A passed all `59/0` assertions, but dev.11 Pass B produced one stationary LateUpdate view/entity terrain snap of `1.5942225456` units, position lag observed/permitted/violations `5/4/1`, and therefore failed `28/2`. That immutable failure remains preserved and uncredited. Dev.12's exact-token, exact-active-rider-only `UnitEntityView.ForcePlaceAboveGround` suppression and deterministic probe then qualified in two fresh same-package processes at `61/0` each. A/B recorded position lag observed/permitted/violations `3/3/0` and `2/2/0`, zero stationary violations, zero final residual, camera away/back and return, zero routing/selection/non-pair faults, healthy pose application, five reconciled captures each, and exact external restoration. The qualifying package binds commit `e1dabf412a0c7a80924b305752f1f84b1f0334dd`, ZIP SHA-256 `a748b5f00afc69cd50e3224ea9f737bbc440cf1914839248d834c20f54be025a`, DLL SHA-256 `d6c1222f933a8864c75ff42bf172c0c0b7fa42a8a34bc5cfec749a1701a4db1a`, and MVID `cdf65654-464e-4dc9-87a9-a75fc0d2c4fb`. No validator or threshold changed. All individual Phase 2A rows are technically qualified A/B; two complete-suite processes and manual review remain open.

Complete-suite checkpoint: dev.12 attempt `20260815T133000Z-presentation-suite-dev12-passA` is final `FAIL` and receives no Pass A credit. Six of seven rows passed; doorway/formation was `87/1` because one pose application measured `2017.7` microseconds against the unchanged `2000` maximum, producing aggregate `380/1`. The row average was `13.9`, every later row maximum was at most `38.7`, and all mechanics, cleanup, captures, and external restoration passed. Dev.13 performs one reversible exact pose-path prime after baseline acquisition and before evidence counters/activation; success and injected-failure tests prove exact restoration, and a source-order gate binds the boundary. Local totals are `21/0`, build, `149/0`, `16/0`, `141/0`, and `103/0`. Fresh dev.13 suite A/B are required; no threshold changed.

Presentation implementation checkpoint: `0.1.0-phase2a-dev.2` adds the exact seven presentation row allowlists, a deterministic seven-bone pose/baseline lease, equipment/UI/camera observations, ordered end-of-frame screenshots, and strict semantic/tamper validation. The pre-runtime local gate is source `21/0`, Release build, component `138/0`, visual capture `12/0`, harness/protocol `139/0`, and exact assembly contracts `101/0` (`90/0` Kingmaker, `11/0` Wrath). All row cells remain `TODO` until two clean fresh processes pass; local tests confer no runtime or visual credit.

Preserved calibration `20260815T034700Z-pose-idle-calibrationA` is `FAIL` `24/2` and receives no Pass A credit. Its 209 healthy/applied frames, microunit solver residuals, bounded idle deltas, zero drift, and exact cleanup/restoration rejected a pose or lifecycle failure. It exposed two evidence aggregation/phase-coverage defects: a first-frame cumulative-average high-water mark and an invalid Update requirement for a deliberately stationary presentation row. The dev.3 regression selects the latest completed cumulative average and admits LateUpdate-only coverage only for the three exact stationary rows; moving rows still require both phases, and all technical thresholds remain fixed. Repaired local totals are component `141/0`, visual `12/0`, harness `139/0`, and assembly `101/0`.

Preserved dev.3 calibration `20260815T041000Z-pose-idle-dev3-calibrationA` receives no Pass A credit despite an in-game `26/0` PASS. It measured completed average/max pose cost `17.92/1496.8` microseconds and passed final synchronization and next-frame cleanup checks, but strict external validation rejected the stale transition-frame cleanup snapshot because its deferred-destruction pose-component count was still one. Dev.4 recaptures cleanup-after evidence on the already-required later frame before publication and adds a source-order regression; the external zero-component validator is unchanged. Dev.4 local totals remain component `141/0`, visual `12/0`, harness `139/0`, and assembly `101/0`.

Current native-lifecycle checkpoint: all four native rows have exact protocol allowlists and schema-v2 semantic validators and are qualified twice in fresh processes. The qualifying area, mode, and disable/residue runs bind clean guarded-published commit `7ea7105b7100c7dbceedf606cfb59d0076432e2b`, package SHA-256 `2909011239621e840416905abcaeaa55cf47462eacca658c0a65721d8db15f22`, manifest SHA-256 `8ce34977cdeeb76ef3cd68379a26cc0aa78fc0993b9692a2d840efcaa73d2918`, and DLL SHA-256/MVID `894f419c7dbaa176eb07a4dccc2a13e1c8f623153a2d3d1fa8665203c9fcb2a9` / `784a9e68-9b04-4cc4-b598-91e25319acd7`. Its complete technical gate is source `21/0`, Release build PASS, component `128/0`, visual-capture contract `12/0`, harness/protocol `138/0`, assembly-backed contracts `75/0` (`64/0` Kingmaker, `11/0` Wrath), and package validation `10/0`.

Pre-launch incident `20260815T005000Z-native-save-clean-dismount-passA` is a truthful orchestration `FAIL`, not a scenario execution: a redundant request-validator allowlist rejected the row after transactional staging but before Steam or Kingmaker launch. Guarded `finally` restoration completed for Mods and Working, all protected-save checks passed, and no lock remains. The three duplicated allowlists are repaired together with a regression over all four native rows; the failed run ID is retired and neither Pass cell receives credit.

Native-save qualification used clean published commit `3781a56f5fa000c89fca1e0809b4eb9fa821734d`, package SHA-256 `3266557bb437f7a8c6bc0570db7e63f0f3c2a322d4d0d2014163cf0f69c7e87f`, DLL SHA-256 `c2fff5b084a695780f3834cfe55741cb033cb86da886014a4a9d58b5dd2278dd`, and MVID `abec6038-807f-4ed1-a54f-70b62fda06dd`. Each fresh process delivered exactly one `SaveManager.SaveRoutine` prefix while Mounted, synchronously cleaned to Unmounted through `SaveRequested`, consumed exactly one bounded Working suppression, performed zero authorized or unauthorized file writes, left the post-load Working identity unchanged, and restored all external state.

Preserved area attempt `20260815T011000Z-native-area-qualified-passA` is a truthful `FAIL` at `48/3`, not Pass A. `Game.ReloadArea` returned on frame 2 before `ISceneHandler.OnAreaBeginUnloading`; the producer prematurely captured a Mounted cleanup latch with no delivery. By frame 65 the ledger contained the exact successful Mounted-to-Unmounted unload delivery and all later area stages, proving asynchronous dispatch rather than missing native cleanup. External validation rejected the malformed latch, and guarded restoration still completed exactly. A deterministic progress-state regression now forbids cleanup or loading-start evidence before actual delivery and preserves cleanup-before-loading ordering. The failed run ID is retired.

First repaired-package attempt `20260815T013000Z-native-area-qualified-passA-retry` is also retained without Pass credit. Its in-game row passed `47/0` and recorded exact unload cleanup at frame 5 plus a clean fresh world at frame 74, but producer cleanup and loading-start records shared frame 5. Unity had not yet completed deferred destruction of the owned anchor/component, so the strict external loading-start validator correctly rejected counts `1/1` instead of zero. All transactions restored. The progress regression now requires a later Update observation before loading-start evidence, preserving the cleanup latch while allowing end-of-frame destruction; validators remain unchanged.

End-of-frame attempt `20260815T013500Z-native-area-qualified-passA-endframe` remains unqualified despite another in-game `47/0` PASS. It correctly separated cleanup frame 5 from residue-free loading-start frame 6, but the generic engine path had already copied a private early loading observation into the cleanup-latch record. The strict phase validator rejected `loading.observed=true` before the declared loading-start phase. The native path now publishes sticky loading flags only through its progress decision; early loading remains private until cleanup and the next Update. No validator changed, and all transactions restored.

The exact-phase repair is qualified by `20260815T014500Z-native-area-qualified-passA-exactphase` and `20260815T015000Z-native-area-qualified-passB-exactphase`, each `47/0`. Cleanup is frame 5 with all public loading flags false, loading-start is frame 6 with zero owned anchor/component residue, loading-stop is frame 65, and the clean fresh fixture world is proved at frame 74. Nine ordered native deliveries include `OnAreaBeginUnloading` before the later loading stages. Every combined/save/Mods transaction restored exactly.

Native mode is qualified by `20260815T015500Z-native-mode-qualified-passA-exact` and `20260815T020000Z-native-mode-qualified-passB-exact`, each `60/0`. Both prove the exact false-to-true-to-false in-memory cache/callback route, exact entry and restore EventBus deliveries, identical persisted setting text before/after, and exact external restoration. The claim remains limited to this native callback route and does not assert settings-UI interaction.

Disable/residue safety is qualified by `20260815T020500Z-residue-uninstall-qualified-passA-exact` and `20260815T021000Z-residue-uninstall-qualified-passB-exact`, each `63/0`. Each observes exactly one overlay before disable, synchronous `ModDisable` Mounted-to-Unmounted cleanup, zero overlay/anchor/agent residue on the following disabled frame, and exactly one overlay after re-enable. It proves transient feature-disable/uninstall residue safety, not a physical file deletion or UMM mouse click.

The first dev.6 `pose-walk-run` attempt, `20260815T050600Z-pose-walk-run-dev6-passA`, is preserved without Pass A credit. Its mechanics and cleanup passed, but the row was `67/1`: 442/442 pose observations were healthy/applied, 2/2 route endpoints completed, walk/run samples were `241/80`, speeds were `1.25/4.064`, Update/LateUpdate counts were `319/442`, maximum direction change was `86.9282` degrees, final synchronization passed, and external state restored exactly. One pose application took `123729.0` microseconds, exceeding the unchanged `2000`-microsecond maximum despite a `294.7244`-microsecond completed mean under the unchanged `500`-microsecond gate. The timed path still allocated seven baseline-snapshot reference objects and two two-bone solution reference objects per evaluated frame, making an isolated collection pause the evidence-backed working hypothesis rather than a mechanics or solver failure.

Version `0.1.0-phase2a-dev.7` removes those per-frame reference allocations: both state carriers are immutable value types, the reusable snapshot list is pre-sized, and successful restoration no longer allocates lambdas, closures, or a failure list. Failure collection and exact restoration verification remain intact. The complete local gate passes source `21/0`, Release build, component `142/0`, visual `12/0`, harness/protocol `139/0`, and assembly-backed `101/0`; no runtime credit is claimed until a fresh exact-package process passes.

Dev.7 walk/run is now qualified twice from clean commit `ce4e4d46d07d12ca4418170412f75003fcaaa556` and package SHA-256 `54d33b52008694e15ab349fac3667f9a237893022ce27f5a651b533731744ca1`. Pass A/B each passed `68/0`; 448/449 pose frames were all healthy and applied, maximum/mean application costs were `1623.1/13.7308` and `1672.5/13.6069` microseconds, 2/2 endpoints completed, both walk/run phases were observed, final synchronization passed, cleanup restored the exact baseline/attachment with zero pose components, and all protected external state restored. Automated frames are consistent engineering evidence only; subjective acceptance remains reserved for the exact manual-review build.

`20260815T055000Z-pose-turn-stop-dev7-passA` receives no Pass A credit. Its in-game row passed `80/0` with `167.6377` degrees of turn/reversal, one authoritative stop, 388 healthy pose observations, `1533.6/14.1714`-microsecond maximum/mean cost, final synchronization, and zero-residue cleanup. The strict external screenshot contract rejected it because `pose-stop-motion` was missing: the stop threshold could be reached while the stock agent's `IsReallyMoving` flag had not yet scheduled that moving milestone. The immutable final result is `FAIL`, and external state restored exactly.

Dev.8 adds a pure stop-early capture boundary. At the stop threshold it queues any missing moving milestone, retains one full render boundary, and only then calls the authoritative stop. Existing captures receive the same one-boundary protection. Component regressions cover both branches, and a source-order visual regression binds capture/wait before stop. Screenshot counts/order and every mechanics threshold remain unchanged. Complete local gates pass source `21/0`, Release build, component `144/0`, visual `13/0`, harness/protocol `139/0`, and assembly-backed `101/0`; a fresh exact-package turn/stop pair is still required.

The exact dev.8 package run `20260815T061000Z-pose-turn-stop-dev8-passA` passed in game `80/0` but remains final `FAIL` and uncredited because `pose-stop-motion` was still absent. The run proved one authoritative stop, `167.19148` degrees of turn/reversal, 404 healthy pose frames, `1576.2/14.00297`-microsecond maximum/mean cost, final synchronization, zero component residue, and exact restoration. Root cause is narrower than render timing: the static capture allowlist omitted `pose-stop-motion`, so `CaptureMilestone` discarded both ordinary and forced requests before enqueue. Dev.9 adds the existing expected milestone to that allowlist and binds it with a scoped regression; complete local gates pass `21/0`, build, `144/0`, `14/0`, `139/0`, and `101/0`. Exact-package A/B remain required and no threshold is weakened.

Dev.9 turn/stop is now qualified twice from clean guarded-published commit `43eb67be89cd491f049cd395359b990d72090b4e`, package/manifest/DLL SHA-256 `908063740a0b9d2fda2f65e0ef70513b6b8a6d36637d4b6304be0799498118a3` / `c110984a82bc25143e673699a9f4559e230dba6268d78c1a30a4185714250de0` / `408fad45f8292102ca360cb81dc806ffcce7b4073a56702906047b32b2fc6d8e`; MVID `d5fb83b9-acfd-444f-9e01-003a0a0d71dd`. Runs `20260815T062000Z-pose-turn-stop-dev9-passA` and `20260815T063000Z-pose-turn-stop-dev9-passB` each pass `80/0` and contain the exact seven-milestone order. A/B prove one authoritative stop, 2/2 endpoint-qualified legs, `167.4615/167.2724` degrees of turn/reversal, 383/402 healthy and applied pose frames, maximum/mean costs `1529.8/13.9830` and `1524.2/14.6726` microseconds, final synchronization, zero cleanup residue, and exact external restoration. Automated frames are repeatable engineering evidence only; pillar occlusion and selection highlighting remain visible in later captures, and subjective acceptance is reserved for the final review build.

Dev.9 doorway/formation attempt `20260815T064000Z-pose-doorway-formation-dev9-passA` passed in game `88/0` but is final `FAIL` and uncredited. It proved the selected authored door and unmounted control, 3/3 qualified legs, normalized formation, bounded non-pair separation, 532 healthy pose frames, `1704.8/13.8917`-microsecond maximum/mean cost, final synchronization, zero residue, and exact restoration. Mounting after the control leg produced a truthful one-time InitialConfiguration residual of `10.26192` units / `169.67844` degrees, corrected in the same sample to approximately zero; every calibrated maximum passed. The external latest-sample validator already classified that phase as uncalibrated but applied calibrated pre-correction bounds unconditionally and contradicted its own nullable unmatched-yaw authority-age branch. Dev.10 admits only corrected initial pre-residuals, preserves all calibrated and post-correction limits, and rejects four unsafe mutations. Local gates pass `21/0`, build, `144/0`, `14/0`, `140/0`, and `101/0`; fresh A/B are required.

Dev.10 doorway/formation is qualified twice from clean guarded-published commit `f343695fe0b09eec4b164e6c67b06e05af465ed6`, package/manifest/DLL SHA-256 `9f05a433f7bb3eb0d0e211a14b170d2e082dd48d21e97528a9a9eda7adcd93c7` / `27ad1f667aecd84b80046e7d658b72685acbb70b6d9fc6173190c07b70711bd9` / `7a7bfd2b3e63c66ad9f98f65e7bf37003e486b5e94831074ca2ac14efa0d66b6`; MVID `4c04cac2-574b-4100-be28-b11d897ba423`. Runs `20260815T065000Z-pose-doorway-formation-dev10-passA` and `20260815T071000Z-pose-doorway-formation-dev10-passB` each pass `88/0`. A/B select exact door `DoorHolder/dwarf_dungeon_door_01`, pass the unmounted control, complete 3/3 endpoint-qualified legs, normalize stock formation selection, record zero non-pair interference, retain `2.09395/2.08391` separation above required `1.56061`, pass final synchronization, and clean to zero residue. They record 532/530 healthy and applied pose frames with maximum/mean costs `1685.4/13.5716` and `1634.7/13.6538` microseconds. All twelve screenshots reconcile across row, manifest, and filesystem. Door-mounted captures repeat the native blue through-wall selection silhouette and strong wall/door occlusion; that is technical evidence, not subjective visual acceptance.

Dev.10 equipment variants are qualified twice from clean guarded-published record commit `b8db204b6bd58b26ac211909e18028bcd388b224`, package/manifest/DLL SHA-256 `8d25375a08299d26aa7efb72c5c37860f348fabbbdce0c046c3170b4f180b2ca` / `51dec29a01770771b9260be50f1ad0ce8633f4eca86e37441b8de377d4cfff36` / `7a7bfd2b3e63c66ad9f98f65e7bf37003e486b5e94831074ca2ac14efa0d66b6`; MVID `4c04cac2-574b-4100-be28-b11d897ba423`. Runs `20260815T072000Z-pose-equipment-variants-dev10-passA` and `20260815T073000Z-pose-equipment-variants-dev10-passB` each pass `28/0` and independently inventory the same single safe original one-handed weapon set, primary blueprint `5363519e36752d84698e03a86fb33afb`, with no secondary/shield and no safe two-handed variant in the protected fixture. A/B each record 99/99 healthy and applied pose frames with maximum/mean costs `1495.5/25.3939` and `1532.5/24.9182` microseconds, exact stationary final synchronization, zero cleanup residue, three reconciled screenshots, and exact external restoration. No inventory mutation or unavailable-variant claim is made.

UI attempt `20260815T074500Z-ui-selection-portrait-actionbar-dev10-passA` is a preserved final `FAIL` and receives no Pass A credit despite its game row passing `30/0`. The row proves four exact native ownership observations in order—rider, mount normalized to rider, away, back—each with exact selection, active owned action bar, selected portrait controller, and selected `CharacterUIDecal`; actual overlay repaint advanced `23` to `229`; six captures reconcile; pose, final synchronization, cleanup, and external restoration pass. The immutable row nevertheless published `uiOverlayLabel="Mount"` and `uiOverlayEnabled=false` because result serialization re-read the mutable player-action overlay after dismount instead of publishing the already-qualified mounted snapshot. Dev.11 introduces an immutable primitive overlay observation captured before cleanup, uses it for both in-game qualification and result fields, and forbids live post-cleanup publication through component and source-order regressions. Complete local gates pass `21/0`, build, `146/0`, `15/0`, `140/0`, and `101/0`; fresh exact-package UI A/B remain required.
