# Runtime scenario matrix

Status: IN PROGRESS

The guarded F1 descriptor and protected-save authority gates are PASS. The exact audit reports one canonical `KMC_AUTOMATION_BASELINE`, one canonical `KMC_AUTOMATION_WORKING`, and zero KMC-looking near-matches. It verified distinct paths, exact internal names, shared campaign/area identity, immutable Baseline, and Working-only write authorization. The user-attested Auto/Quick transition is an exact protected baseline, never project write authority. The six missing F1 processes passed, completing the named-row ledger at `25 PASS / 0 attributable FAIL / 0 DEFER`.

Frozen F0 offline gate ledger: source validation 21 PASS / 0 FAIL; Release build PASS; pure/component 112 PASS / 0 FAIL; visual-capture contracts 12 PASS / 0 FAIL; guarded harness/protocol 105 PASS / 0 FAIL; assembly-backed 69 PASS / 0 FAIL (Kingmaker 58, Wrath 11).

Protected-save authority implementation commit `6abc293f12bacbc250fc4c012fbced05b3763881` is on `codex/mounted-combat-feasibility`. It changes `scripts/runtime/RuntimeHarness.Common.ps1`, adds `scripts/runtime/New-KmcProtectedSaveContinuityAuthority.ps1`, and changes `scripts/runtime/Invoke-KingmakerRuntimeScenario.ps1` plus `scripts/Test-Harness.ps1`; SHA-256 values are respectively `59e24b0156f3f8c44ebd99c7bb403952108a7cef505bf55d633ca173f47d0ee1`, `9a5b983a7863b54457bbb4b869a5346b8d41d5744f901536ded647fb8127b2f3`, `b425fce49d329ccea6cba04dd423c242a656d4aff15ed63234f248c559db16d4`, and `cd6f3aa14e5c08211cc39d6e51fe86e304140993e137a5b8ed2c66fd1a41cf8`. Gates passed AST `4/4`, source `21/0`, Release build, component `112/0`, visual `12/0`, harness `134/0`, assembly-backed `69/0`, and diff check. This validates the admission guard and supplies no F1 runtime row.

## Fixture evidence epochs

F0 is the frozen runtime epoch at commit `fc7215481acf97ce1863eb1c75b3433889d2af7d`. It used Working SHA-256 `a5f7a7fb77f0465df1591360ecd1730e2c28215d83e27aafc70efef3110e6dc5`, length `718821`, ticks `639222573406912936`; all executed evidence below belongs to F0 unless explicitly stated otherwise. Its named-row ledger remains `22 PASS / 0 attributable FAIL / 3 DEFER — EVIDENCED`.

F1 is descriptor-requalified and runtime-qualified for the Phase 1 slice. Transaction `fixture-requalification-20260814T1140026497594Z` committed at `2026-08-14T11:40:02.8457604+00:00`. Qualification SHA-256/length/ticks are `95e9a3c6203f11cfdd7e615849eb61ebc060131075f6a7f40771dcd7eeb68a7a` / `1272` / `639223044028647586`; phase-`committed` state SHA-256/length/ticks are `68fdc0f759e7f7daffaffaeb27996357e74c8f5861c5bf9345005edbefa66cce` / `1707` / `639223044029797599`. The exact F0 qualification backup is SHA-256 `c1e33c75004212039258d6d90ba20e43c37c50966c94e685dbad1ca0e653654f`, length `1272`.

F1 retains unchanged Baseline SHA-256/length/ticks `c29d965c9ff5dc0f971659d9ae154877aa4a9a461ca220d1ce28e7c7fd9d2512` / `686605` / `639222474845172002` and descriptor-qualifies only revised Working `5eb4e0b4cbd8d60dc879a02ff71aadfde3f517304754857f0cc68d0f9a93f1c6` / `816452` / `639222975870964407`. The 1/1/0 audit, exact names, distinct paths, Manual/v1 descriptors, and shared `cvb` / `d41185be-edc1-47c0-b9d5-e1d7a9c8e65f` / `9d1278a2f599b2a4daab53abdfe88d2e` identity passed. Working alone is writable. The requalification transaction preserved its current before/after inventory at digest `973f203e16aad29d7e4865bae6f81149d959a4ac179953c87781a06d1c71898d`, `271` files, `3178473776` bytes. Normal guard and recovery `-WhatIf` passed, with no process, lock, sentinel, live KMC tree, or transaction debris.

Frozen F0 inventory was digest `58a54974423316041e29d32b1093c5d6775dbd67fe2b37ffaea3c9ae27de19be`, `271` files, `3178063612` bytes. Current F1 differs in exactly three entries and no others:

| Entry | Authorization | F0 length / ticks | Current F1 length / ticks |
|---|---|---|---|
| Working | Authorized | `718821` / `639222573406912936` | `816452` / `639222975870964407` |
| `Auto_1120` | User-authorized protected baseline only | `204829` / `639222474628040540` | `333208` / `639222975467301685` |
| `Quick_438` | User-authorized protected baseline only | `625411` / `639220694761623881` | `809565` / `639222975512345112` |

The user attested that Auto and Quick changed during the external Kingmaker fixture-preparation session, not in a KMC transaction. Their current exact bytes and metadata are protected baselines only. The project must not restore or mutate either save.

The append-only authority is `C:\Dev\KingmakerMountedCombatLab\runtime-state\protected-save-authorities\20260814T1445257441387Z-user-fixture-preparation.json`, epoch `20260814T1445257441387Z-user-fixture-preparation`, SHA-256 `77fd415d177423dd7da58e3685e418f38cf954dead75068424a717e2be6b3ca9`, length `119319`, ticks `639223155954515237`. It binds current inventory digest `973f203e16aad29d7e4865bae6f81149d959a4ac179953c87781a06d1c71898d`, Auto SHA-256 `9599e9e15bdd04cde7b15d47795510b95cd8e1333f107f2e3b64469ebf3330fc`, and Quick SHA-256 `e26dc2ce00e6ed68953c2c9b1e89584ae3c3bd50f4cf698ecc2c624e21d10575`. Creation WhatIf, append-only actual creation, and independent continuity validation passed with no save mutation or residue.

## Executed F1 completion evidence

All six rows used commit `d5bd7fa9c434f04c6f8487b61ea49e3cf983c397`, package SHA-256 `5ce3bd7d98a090ee05405cc4b4725fa58f13f1926958a69905ba478374c75a4d`, DLL SHA-256 `202701ed9232e4f5a4d5ff65c468c684d1ed1dd53e4a8030be93b960a7cd202a`, and MVID `a702808c-e8a0-4755-bc24-5ed4e945866a`. Each passed request/game/final validation `31/0`, `39/0`, and `29/0`.

| Scenario | Process A | Process B | Qualified evidence |
|---|---|---|---|
| `mounted-pair-doorway` | `20260814T150000Z-doorway-passA`, 60/0 | `20260814T151500Z-doorway-passB`, 60/0 | Same open `DoorHolder/dwarf_dungeon_door_01`; same-current-Mammoth unmounted control true; approach legitimately skipped because start was already near; 2/2 strict path legs; max final/best `1.2317738508` / `1.2061703079`; no instability or residue |
| `mounted-pair-selection` | `20260814T153000Z-selection-passA`, 54/0 | `20260814T154500Z-selection-passB`, 54/0 | Same non-pair `d17c8fd0-6627-40c4-bf9a-024cf001c1f7`; mount normalized to rider; away/back true; 1/1 routed endpoint at `1.1999048650` / `1.2147087582`; zero selection loss/interference |
| `mounted-pair-party-formation` | `20260814T160000Z-formation-passA`, 58/0 | `20260814T161500Z-formation-passB`, 58/0 | Stock group movement; normalized rider+non-pair selection; mount final `1.2357761832` / `1.2424143584`; non-pair final `0.0239113005` / `0.0450875333`; minimum separation `2.0965969637` / `2.0840256324` above required `1.5606060028`; zero interference/replacement/loss |

F1 runtime assertions total `344 PASS / 0 FAIL`. Every row has exact final synchronization and residue-free cleanup. Every combined/Mods/save transaction is `restored` with all five restoration flags true and no errors. Live Mods are exact pre/post digest `e62320bbe7d4b83edc128a62f9f5852b0669c5549859602c335c649318578d47` (`348` files / `44` directories / `69797076` bytes); protected save inventory remains exact digest `973f203e16aad29d7e4865bae6f81149d959a4ac179953c87781a06d1c71898d` (`271` files / `3178473776` bytes).

## Executed F0 runtime evidence

| Evidence group | Result | Exact evidence |
|---|---|---|
| `mod-load-smoke` | PASS twice in consecutive fresh processes | `20260813T185500Z-mod-load-smoke-passA` and `20260813T185700Z-mod-load-smoke-passB`; same commit/package, zero save/load requests, exact restoration |
| Fixture intake and forensic rows | PASS 3/3 rows, 13/0 assertions | `20260813T233100Z-fixture-intake-overlay-pilot`: `export-mounted-contracts` 3/0, `export-candidate-mount-rigs` 4/0, `observe-mount-diagnostic-availability` 6/0; exact Medium rider and larger active Mammoth resolved |
| Lifecycle pass A | PASS 8/8 rows, 339/0 assertions | `20260814T034950Z-lifecycle-suite-passA` |
| Lifecycle pass B | PASS 8/8 rows, 339/0 assertions | `20260814T035800Z-lifecycle-suite-passB`; same commit/package as A |
| Pause/unpause A/B | PASS 1/1 row and 49/0 assertions in each process | `20260814T053000Z-pause-passA-qualified` and `20260814T054800Z-pause-passB-qualified` |
| Destination-cancel A/B | PASS 1/1 row and 49/0 assertions in each process | `20260814T060500Z-cancel-passA-qualified` and `20260814T062200Z-cancel-passB-qualified` |
| Open-ground A/B | PASS 1/1 row and 47/0 assertions in each process | `20260814T064000Z-open-ground-passA-qualified` and `20260814T065700Z-open-ground-passB-qualified` |
| Stop/start A/B | PASS 1/1 row and 61/0 assertions in each process | `20260814T073500Z-stop-start-recovery-passA` and `20260814T075000Z-stop-start-recovery-passB` |
| Turns/corners A/B | PASS 1/1 row and 74/0 assertions in each process | `20260814T081500Z-turns-corners-passA` and `20260814T083000Z-turns-corners-passB` |
| Doorway control | DEFER — EVIDENCED | `20260814T045500Z-movement-suite-corrected-passA`: unmounted Mammoth completed 2/2 endpoints at maximum `1.1952427374 < 1.25`; mounted reverse path was accepted and stable before native combat triggered residue-free `CombatStarted` cleanup |
| Selection / formation availability | DEFER — EVIDENCED | `20260814T051000Z-selection-passA` proved the fixture has no eligible directly controllable non-pair; formation was not launched merely to reproduce the same missing prerequisite |
| Turn-based boundary | PASS 1/1 row, 56/0 assertions | `20260814T090000Z-boundary-tb-pass`; direct lifecycle-handler cleanup only, `nativeDeliveryObserved=false` |
| Real-time boundary | PASS 1/1 row, 56/0 assertions | `20260814T091500Z-boundary-rt-pass`; direct lifecycle-handler cleanup only, `nativeDeliveryObserved=false` |
| Save boundary | PASS 1/1 row, 59/0 assertions | `20260814T093000Z-boundary-save-pass`; direct `GuardBoundary(SaveRequested)` cleanup, no stock `SaveRoutine`, save write, serialization, or save-round-trip claim |
| Load boundary | PASS 1/1 row, 44/0 assertions | `20260814T094500Z-boundary-load-pass`; real exact-Working `Game.LoadGame` plus native `LoadRoutine`-prefix authorization, observed loading/callback/fresh-world cleanup; no UI-load claim |
| Area boundary | PASS 1/1 row, 44/0 assertions | `20260814T100000Z-boundary-area-pass`; direct `OnAreaBeginUnloading` cleanup latch before real `ReloadArea(AutoSaveMode.None)`; native area-event delivery not independently qualified |

The five boundary runs use F0 commit `fc7215481acf97ce1863eb1c75b3433889d2af7d`, DLL SHA-256 `202701ed9232e4f5a4d5ff65c468c684d1ed1dd53e4a8030be93b960a7cd202a`, and MVID `a702808c-e8a0-4755-bc24-5ed4e945866a`. Each has strict request/game/final and boundary-JSONL validation, exact F0 Working identity continuity, terminal pre-restoration byte verification, and exact external restoration.

Historical engineering FAIL results remain preserved. They include fixture bootstrap, wrapper/restoration, endpoint-accounting, camera-capture, pause-validator, and stop/start recovery-oracle defects, plus the deterministic fixture-availability results. Each attributable harness/instrumentation defect has a regression. None is reclassified as a gameplay PASS, and none fired an architecture kill criterion.

## Frozen F0 required-scenario disposition

| Group | Scenario | Status | Qualified claim / exact limitation |
|---|---|---|---|
| Scaffold | `mod-load-smoke` | PASS | Two same-commit fresh-process passes with no-save identity and exact restoration |
| Forensics | `export-mounted-contracts` | PASS | Fixture-intake subscenario plus exact contract maps and assembly checks |
| Forensics | `export-candidate-mount-rigs` | PASS | Fixture-intake subscenario plus exact native metadata inventory |
| Forensics | `observe-mount-diagnostic-availability` | PASS | Exact Medium rider and larger active Mammoth pair available |
| Lifecycle | `mounted-pair-create-and-clear` | PASS | Exact state transition and zero movement/view/selection residue, A/B |
| Lifecycle | `mounted-pair-double-mount-rejected` | PASS | Exact rejection and unchanged pair state, A/B |
| Lifecycle | `mounted-pair-invalid-pair-rejected` | PASS | Side-effect-free validation rejection, A/B |
| Lifecycle | `mounted-pair-cleanup-idempotent` | PASS | Repeated cleanup and zero residue, A/B |
| Lifecycle | `mounted-pair-death-cleanup` | PASS | Direct lifecycle-handler cleanup, A/B; native EventBus delivery not claimed |
| Lifecycle | `mounted-pair-combat-start-cleanup` | PASS | Direct lifecycle-handler cleanup, A/B; native combat delivery was separately observed during contaminated traversal |
| Lifecycle | `mounted-pair-area-unload-cleanup` | PASS | Direct lifecycle-handler cleanup, A/B; native area-event delivery not claimed |
| Lifecycle | `mounted-pair-mod-disable-cleanup` | PASS | Direct relationship-service cleanup, A/B; native UMM callback delivery not claimed |
| Movement | `mounted-pair-open-ground` | PASS | A/B; one mover, no oscillation, fixed `<= 0.10` synchronization gates, residue-free cleanup |
| Movement | `mounted-pair-stop-start` | PASS | A/B; exact zero stopped drift, distinct restart path, fixed synchronization/recovery gates |
| Movement | `mounted-pair-turns-and-corners` | PASS | A/B; 3/3 endpoints and substantial direction change, zero instability |
| Movement | `mounted-pair-doorway` | DEFER — EVIDENCED | F0 valid unmounted Mammoth control and stable mounted entry; native hostile contamination prevented corrected repeatability |
| Movement | `mounted-pair-selection` | DEFER — EVIDENCED | F0 had no eligible directly controllable non-pair, so required away/back could not run |
| Movement | `mounted-pair-party-formation` | DEFER — EVIDENCED | F0 had the same missing third-unit prerequisite; non-mounted party interference remained unproved |
| Movement | `mounted-pair-pause-unpause` | PASS | A/B; stable command/destination, exact paused drift zero, mode-aware telemetry |
| Movement | `mounted-pair-destination-cancel` | PASS | A/B; command and movement representations stop, final synchronization and cleanup pass |
| Boundary | `mounted-pair-turn-based-entry-cleanup` | PASS | Direct handler only; no native controller/EventBus delivery claim |
| Boundary | `mounted-pair-realtime-entry-cleanup` | PASS | Direct handler only; no native controller/EventBus delivery claim |
| Boundary | `mounted-pair-save-safety` | PASS | Direct service cleanup; unchanged Working and no custom relationship serialization; no stock save |
| Boundary | `mounted-pair-load-safety` | PASS | Real exact-Working load and native prefix; clean fresh world |
| Boundary | `mounted-pair-area-transition-safety` | PASS | Direct pre-clean then real reload; native area-event delivery not independently qualified |

## F1 completion queue

| Order | Scenario | F1 status | Required control |
|---|---|---|---|
| 1 | `mounted-pair-doorway` A/B | PASS | Exact same current-size Mammoth unmounted matched control, then mounted route, each in a fresh process |
| 2 | `mounted-pair-selection` A/B | PASS | Select away to the eligible directly controllable third unit and back, each in a fresh process |
| 3 | `mounted-pair-party-formation` A/B | PASS | Meaningful stock group movement command with the eligible third unit, each in a fresh process |

The queue is complete. Do not rerun completed archaeology, lifecycle/boundary suites, or movement rows absent a narrow regression gate. Do not synthesize units, weaken thresholds, or execute Phase 2 under this mission.

## Phase 2A presentation qualification addendum

Phase 2A presentation runtime qualification is technically PASS and stopped at `BLOCKED - MANUAL VISUAL ACCEPTANCE REQUIRED`. The seven exact rows are `pose-idle`, `pose-walk-run`, `pose-turn-stop`, `pose-doorway-formation`, `pose-equipment-variants`, `ui-selection-portrait-actionbar`, and `camera-follow-and-command-routing`; all seven are technically qualified twice in fresh processes. Two complete review.2 suites and the exact guarded manual-review build/launcher are also qualified. Subjective acceptance alone remains required.

Three early guarded `pose-idle` attempts are preserved without qualification credit. `20260815T034700Z-pose-idle-calibrationA` failed in game at `24/2` and exposed cumulative-average and stationary-phase evidence defects. `20260815T041000Z-pose-idle-dev3-calibrationA` passed in game at `26/0` but final validation rejected stale transition-frame cleanup evidence. `20260815T042100Z-pose-idle-dev4-passA` passed in game at `26/0` and proved exact later-frame cleanup, but the independent external validator still duplicated the obsolete unconditional Update-sample rule. In all three processes, external Working, protected saves, and Mods restored exactly; none is Pass A.

Version `0.1.0-phase2a-dev.5` aligned the external stationary-row policy, after which `20260815T044100Z-pose-idle-dev5-passA` and `20260815T044700Z-pose-idle-dev5-passB` qualified `pose-idle` twice at `26/0` from the same clean commit/package. Both proved healthy/applied pose coverage, microunit residuals, zero drift, exact stationary final synchronization, exact cleanup, and external restoration. At that checkpoint, `pose-idle` was the only qualified presentation row; subjective acceptance remained open.

`20260815T045300Z-pose-walk-run-dev5-passA` passed in game at `68/0` but its final result is `FAIL` and receives no credit: the validator rejected the route's deliberately measured `86.85`-degree inter-leg direction change as turns/corners-only evidence. Dev.6 corrects that exact contract while preserving the dedicated `>=75`-degree turn-row gate and rejecting stray turn metrics on unrelated rows. Its complete local gate is source `21/0`, Release build PASS, component `141/0`, visual `12/0`, harness/protocol `139/0`, and assembly-backed `101/0`. The next runtime cell is a fresh dev.6 `pose-walk-run` attempt.

That fresh dev.6 cell, `20260815T050600Z-pose-walk-run-dev6-passA`, failed at `67/1` and receives no qualification credit. It passed both route endpoints, collected `241/80` walk/run samples at `1.25/4.064`, kept 442/442 pose observations healthy and applied, measured `86.9282` degrees of direction change, passed final synchronization and exact cleanup, and restored Working/protected saves/Mods exactly. Its completed mean pose cost was `294.7244` microseconds, but one application took `123729.0` microseconds and correctly violated the unchanged `2000`-microsecond maximum. Inspection found nine reference allocations per evaluated frame in the timed pose path: seven baseline snapshots and two two-bone solutions. Dev.7 converts both carriers to immutable value types, pre-sizes the reused frame list, and makes successful restoration allocation-free while retaining exact failure aggregation. Its complete local gate passes source `21/0`, Release build, component `142/0`, visual `12/0`, harness/protocol `139/0`, and assembly-backed `101/0`. The next runtime cell is a fresh exact-package dev.7 walk/run attempt.

Dev.7 exact-package attempts `20260815T053000Z-pose-walk-run-dev7-passA` and `20260815T054500Z-pose-walk-run-dev7-passB` then qualified walk/run at `68/0` each. They recorded 448/449 healthy and applied frames with maximum/mean costs `1623.1/13.7308` and `1672.5/13.6069` microseconds, completed 2/2 endpoints, exercised walk/run and substantial inter-leg direction change, passed final synchronization, left zero pose-component or attachment residue, and restored every external authority. `pose-idle` and `pose-walk-run` are now the two qualified presentation rows; five individual rows, two complete-suite processes, and subjective acceptance remain open.

`20260815T055000Z-pose-turn-stop-dev7-passA` passed its in-game row at `80/0` but final validation is `FAIL` and no Pass A credit is assigned. Mechanics, pose cost, synchronization, and cleanup passed; the exact ordered capture list lacked only `pose-stop-motion` because the stop threshold could precede the stock `IsReallyMoving` capture condition. Dev.8 queues a missing moving capture at that proven-motion threshold, waits one render boundary, and then stops. Pure behavior and source-order regressions bring the local totals to component `144/0` and visual `13/0`; the screenshot validator is unchanged.

## Measured F0 movement and visual disposition

All runnable pair-only rows passed twice with final synchronization, zero cleanup residue, and exact external restoration. Open-ground pass B finished `1.2466372098` from the endpoint, retaining only `0.0033627902` margin inside the unchanged `1.25` gate. Stop/start A/B held exact zero stopped drift for approximately `0.761` / `0.759` seconds and then moved approximately `11.436` / `11.417` units on distinct restart paths. Turns/corners A/B reached 3/3 endpoints and measured maximum direction changes of approximately `139.03` degrees. Position and rotation qualification retained the unchanged `0.10` gates, explicit raw-lag bounds/recovery, and zero final outstanding recovery.

Usable game-camera frames classify presentation `MECHANICALLY VIABLE, NEW ANIMATION/POSE WORK REQUIRED`. The silhouette is stable and readable, but the rider is rigidly upright with the lower body intersecting or occluded by the Mammoth back and has no saddle, reins, or seated pose. Some later-state frames are black or clipped; camera-only evidence does not prove portrait state, camera follow, UI selection, away/back selection, or party formation.

Frozen F0 named-row disposition remains **22 PASS / 0 attributable FAIL / 3 DEFER — EVIDENCED** for provenance. F1 supersedes each deferral with repeatable PASS evidence. The final Phase 1 named-row disposition is **25 PASS / 0 attributable FAIL / 0 DEFER**. No kill criterion fired; the controlling status is `PHASE 1 COMPLETE — PROCEED RECOMMENDED` with Architecture B.

## Phase 2B stationary rider combat diagnostic ledger

The user accepted exact Phase 2A presentation commit `09a63729e0847c540ae7e79e9e3876d005ee9afe`; combat qualification is therefore authorized but must stand on its own evidence. Probes F-N are immutable engineering diagnostics and receive no PASS credit. Each completed with exact protected-save/Mods restoration. Probe N `20260816T075000Z-rider-hit-rt-probeN` is the first to prove native child `Success`, terminal `completed`, exact one-start/zero-repath, and a created attack rule. It remains `FAIL` `43/4`: its unmarked Rulebook handler observed no events or forced roll, and manual pre-tick `IsActed` bypassed Kingmaker's native cooldown transition. The exact global Rulebook interface and false-to-true wrapper action repairs are locally green; a fresh Probe O is required. No Phase 2B runtime row is qualified yet.

Probe O `20260816T081000Z-rider-hit-rt-probeO` remains uncredited `FAIL` `20/1`: it times out before click with only player-group-to-target memory present. The buffered controller path has faction/stealth admission that cannot guarantee a reverse synthetic-target entry. The diagnostic provisioner now owns an exact two-group public memory lease, timestamp refresh, in-flight validation, and symmetric removal; no production combat or persistent state changes. Fresh Probe P is required. No Phase 2B runtime row is qualified yet.

Probe P `20260816T083000Z-rider-hit-rt-probeP` remains uncredited `FAIL` `20/1`: both exact group-memory entries exist and are removed exactly, but rider, Mammoth, target, and player group never enter combat and the rider never prepares. Exact installed `UnitMemoryController.Tick` calls `Observer.Wake()` after adding a memory entry; the direct lease omitted that stock-equivalent completion. The diagnostic provisioner now conditionally wakes only a sleeping leased endpoint and records target awake/fog/peaceful deadline state. No combat membership, initiative, cooldown, faction, stealth, production attack, pathing, or save state is edited. Fresh Probe Q is required. No Phase 2B runtime row is qualified yet.

The wake-complete package then qualifies stationary rider melee in real time twice: `20260816T084500Z-rider-hit-rt-probeQ` and `20260816T090000Z-rider-hit-rt-passB` each pass `47/0` from exact commit/package `07d44eb89c6a533bc99e386bad6fd3dac4a9ac73` / `af4855f93e31084b2406a2b9e527b5d8587c5a7b82a52d61bb3863d7af777459`. Both independently prove one rider-owned attack/roll/damage, eight damage, one rider Standard charge, zero duplicate pair attacks, zero repaths, healthy accepted Mammoth pose, exact transient-target cleanup, and exact external restoration. The stationary hit-RT claim is PASS A/B; TB and every other Tranche C row remain open.

The distinct `mounted-rider-melee-hit-tb` row is now implemented but not yet runtime-qualified. It begins from exact real-time state, leases only the registered non-persisting turn-mode callback, waits for native mode/controller initialization and an exact rider/Mammoth/target roster, starts one public native rider turn, and requires rider current-turn/acting identity through the native single-attack outcome. Schema v5 also requires callback delivery, return to real time, raw setting-cache restoration, and unchanged persisted settings after complete target/pair/combat cleanup. RT schema v4 remains compatible. Fresh same-clean-package TB A/B are mandatory before PASS.

First TB attempt `20260816T093000Z-rider-hit-tb-passA` is preserved uncredited `FAIL` `10/1`. It proves callback/cache/persisted-setting restoration but reaches no mount or combat because the initial row incorrectly waited for `CombatController.IsInTurnBasedCombat`, whose exact installed definition requires `Player.IsInCombat`. The repair waits for the exact temporary setting value before mount and retains the full combat predicate after native entry. Schema-v5 FAIL structure also permits unobserved null turn IDs and round `-1`; PASS remains exact. Fresh package evidence is required.

Second TB attempt `20260816T094500Z-rider-hit-tb-probeB` is preserved uncredited `FAIL` `22/1`. It reaches exact mounted native combat, initialized three-unit roster, and `StartTurn(rider)`, then proves a directly controllable turn begins in `Preparing` and advances to `Acting` only after a command is queued. The repair admits only exact-rider native `Preparing`/`Acting`, queues the unchanged click path, and requires later exact-rider `Acting` at dispatch observation and outcome. Fresh package evidence is required.

Probe C `20260816T100000Z-rider-hit-tb-probeC` is preserved uncredited `FAIL` `35/1`. All native pre-click gates pass; production alone rejects the exact rider's `Preparing` state through a stale inline `IsActing` test. Production and diagnostics now share the same pure exact-rider `Preparing`/`Acting` admission policy, while post-dispatch and outcome still require `Acting`. Fresh package evidence is required.

Probe D `20260816T101500Z-rider-hit-tb-probeD` is preserved uncredited `FAIL` `50/1`. It reaches one exact rider-owned native attack/roll/damage under the acting rider turn, forced-d20 critical hit, nine damage, exact rider-only Standard cost, and zero duplicates/repaths; cleanup and external restoration are exact. Its native child subsequently ends `Interrupt`, so TB remains open. A bounded terminal-lifecycle trace will identify the exact caller without accepting `Interrupt` as success or weakening schema-v5 PASS semantics.

Probe E `20260816T103000Z-rider-hit-tb-probeE` is preserved uncredited `FAIL` `50/1`. It repeats the exact one-child attack/roll/damage path with a seven-damage forced-d20 critical hit, rider-only Standard cost, exact acting-rider turn, zero duplicates/repaths, and exact cleanup/restoration. Its child terminal snapshot is `Interrupt` with an observed attack rule and unfinished animation; the first-frame caller records only `UnitCommand.Interrupt` and cannot identify the external source. A bounded 12-frame caller chain is required before any repair; no TB credit is inferred.

Probe F `20260816T110000Z-rider-hit-tb-probeF` is preserved uncredited `FAIL` `50/1`. It repeats all exact mechanical, resource, ownership, turn, cleanup, and restoration evidence. Its caller chain proves exact `UnitAttack.OnTick` enters the nonexistent-next-attack branch at the one-second turn-based delay after native single-attack `Success` and before animation completion. The scoped repair retains that existing Success only for an acted, observed, exact `1/1`, no-planned-attack mounted child and lets base command completion await the animation. Deterministic gates pass; fresh same-package TB A/B are required.

Fresh repaired TB runs `20260816T111500Z-rider-hit-tb-passA` and `20260816T113000Z-rider-hit-tb-passB` each pass `51/0` from clean commit/package `943156a7f580abf15e56dfd70a0a2b23688d335c` / `a87330d32836f7b359106df767832c15fd46849d87c2e08b2fe156b79cb143d0`. Each proves exact acting-rider turn identity, one rider attack/roll/damage, forced critical hit, rider-only Standard cost, zero duplicate pair attacks/repaths, native terminal Success, healthy accepted profile, complete cleanup, and exact external restoration. Stationary rider hit RT/TB is PASS A/B; the remaining Tranche C rows stay open.

`mounted-rider-melee-miss-rt` uses the exact qualified mounted hit path except for deterministic natural `1`. Earlier codomain probes remain uncredited history. Fresh sleepless-repaired Pass A `20260816T130000Z-rider-miss-rt-passA` passes schema-v8 `47/0`, proving exact lease/awake/release, one natural-1 rider miss, zero damage, rider-only Standard cost, pose health, cleanup, and restoration. Same-package Pass B `20260816T131500Z-rider-miss-rt-passB` is preserved uncredited `FAIL` `20/1`: both memory and awake directions remain true, yet native combat never begins. Observation-only schema v10/v11 now binds every remaining known `UnitCombatJoinController` gate—live entities, consciousness, ignored flags, exact groups/enemy lists, fog, and stealth/ambush—while preserving v1-v9. One fresh diagnostic process is required before any behavior repair; later two fresh same-package miss passes remain mandatory.
Phase 2A turn/stop qualification remains open after two preserved restored failures. Dev.7 run `20260815T055000Z-pose-turn-stop-dev7-passA` and dev.8 run `20260815T061000Z-pose-turn-stop-dev8-passA` each passed the game row `80/0` but omitted required screenshot milestone `pose-stop-motion`, so neither receives Pass A credit. Dev.8 proved a pre-stop render boundary was necessary but insufficient. Exact source inspection then proved the milestone was absent from `RuntimeMovementScenarioEngine.CaptureMilestones`, causing all requests for it to return before enqueue. Dev.9 adds exactly that allowlist member and a scoped regression; local gates pass source `21/0`, Release build, component `144/0`, visual `14/0`, harness/protocol `139/0`, and assembly-backed `101/0`. Fresh exact-package A/B remain required; runtime safety and thresholds are unchanged.

Phase 2A turn/stop is qualified by dev.9 runs `20260815T062000Z-pose-turn-stop-dev9-passA` and `20260815T063000Z-pose-turn-stop-dev9-passB`, each `80/0`, from exact package `908063740a0b9d2fda2f65e0ef70513b6b8a6d36637d4b6304be0799498118a3`. Both contain the required `mounted-idle`, `pose-stop-motion`, `pose-stopped`, `pose-turn`, `pose-reversal`, `pose-stopped`, `dismounted` capture order and prove one scoped stop, two endpoint-qualified legs, substantial reversal, healthy pose cost, final synchronization, zero component residue, and exact Working/protected-save/Mods restoration. This is technical runtime qualification, not subjective visual acceptance.

Phase 2A doorway/formation attempt `20260815T064000Z-pose-doorway-formation-dev9-passA` remains final `FAIL` with no Pass A credit. The in-game `88/0` evidence proves the doorway control, mounted/formation mechanics, non-pair isolation, calibrated synchronization, pose cost, and cleanup, but the external validator incorrectly applied calibrated latest pre-correction bounds to the explicit InitialConfiguration mount correction. Dev.10 permits that initial displacement only when same-sample post-correction residual is bounded and retains every calibrated threshold; a focused regression accepts the corrected phase and rejects uncorrected position, uncorrected rotation, calibrated position, and calibrated rotation mutations. Fresh exact-package A/B remain required.

Phase 2A doorway/formation is qualified by dev.10 runs `20260815T065000Z-pose-doorway-formation-dev10-passA` and `20260815T071000Z-pose-doorway-formation-dev10-passB`, each `88/0`, from exact package `9f05a433f7bb3eb0d0e211a14b170d2e082dd48d21e97528a9a9eda7adcd93c7`. Both prove the selected authored door, valid unmounted control, 3/3 endpoint-qualified legs, normalized stock formation selection, zero non-pair interference, clearance above the fixed corpulence-derived bound, healthy pose performance, final synchronization, zero cleanup residue, exact screenshot reconciliation, and exact Working/protected-save/Mods restoration. Heavy doorway occlusion and native through-wall selection highlighting remain for subjective review.

Phase 2A equipment presentation is qualified by `20260815T072000Z-pose-equipment-variants-dev10-passA` and `20260815T073000Z-pose-equipment-variants-dev10-passB`, each `28/0`, from exact package `8d25375a08299d26aa7efb72c5c37860f348fabbbdce0c046c3170b4f180b2ca`. Both independently observe the protected fixture's single original one-handed weapon set and prove healthy pose application, stationary synchronization, screenshot integrity, zero residue, and exact external restoration. Shield and safe two-handed variants are absent from the fixture and are not claimed.

Phase 2A UI attempt `20260815T074500Z-ui-selection-portrait-actionbar-dev10-passA` is final `FAIL` with no Pass A credit. Its game row `30/0` proves every exact native ownership phase and mounted overlay repaint, but result publication re-read player-action overlay fields after cleanup and emitted the later disabled `Mount` state. Dev.11 captures one immutable mounted overlay observation before cleanup and publishes that same evidence; behavioral and source-order regressions prevent recurrence. Fresh exact-package A/B remain required.

Phase 2A UI is qualified by dev.11 runs `20260815T080000Z-ui-selection-portrait-actionbar-dev11-passA` and `20260815T081500Z-ui-selection-portrait-actionbar-dev11-passB`, each row/game/final `30/0`, from exact package SHA-256 `5fdf4592c89d14526d9dd8974ced280fa7d04a524b9097e31946c17b84c31805`. Both prove exact rider, mount-normalized, away, and back native ownership; publish the same immutable mounted `Dismount`/enabled/visible overlay used by the game assertion; reconcile six screenshots; retain healthy bounded pose application and final synchronization; clean to zero residue; and restore all external state exactly. Gameplay-camera frames omit full UI chrome, so direct controller/decal/action-bar observations are the technical authority and pointer interaction remains manual-review-only.

Phase 2A camera follow/command routing is qualified by dev.12 runs `20260815T130000Z-camera-follow-command-routing-dev12-passA` and `20260815T131500Z-camera-follow-command-routing-dev12-passB`, each `61/0`, from exact clean commit `e1dabf412a0c7a80924b305752f1f84b1f0334dd` and package SHA-256 `a748b5f00afc69cd50e3224ea9f737bbc440cf1914839248d834c20f54be025a`. Both complete the exact five-unit route, observe native camera follow through movement plus away/back selection and return to zero target residual, retain zero command replacement/selection-loss/non-pair interference, and close final synchronization at zero. Position lag observed/permitted/violations is `3/3/0` and `2/2/0`; stationary-position violations are zero. Pose is healthy/applied for `248/248` and `251/251` observations, five captures per run reconcile, cleanup has zero residue, and Working/protected saves/Mods restore exactly. The dev.11 failed repeat remains immutable and uncredited; subjective camera feel and pose appearance remain manual-review-only.

First complete presentation-suite attempt `20260815T133000Z-presentation-suite-dev12-passA` is preserved final `FAIL` at aggregate `380/1` and receives no Pass A credit. All seven rows executed and six passed. Doorway/formation alone failed `87/1` because one of 533 pose applications measured `2017.7` microseconds against the unchanged `2000` maximum; its completed mean was `13.9`, and later row maxima were all at most `38.7`. Exact mechanics, synchronization, 35 manifested screenshots, zero-residue cleanup, and Working/protected-save/Mods restoration otherwise passed. Dev.13 primes the exact reversible pose frame once after baseline acquisition and before measured counters/configuration; deterministic success/failure restoration and source-order gates pass. Fresh exact-package dev.13 suite A/B remain required.

Dev.13 complete-suite runs `20260815T134500Z-presentation-suite-dev13-passA` and `20260815T140000Z-presentation-suite-dev13-passB` supersede that open repeatability question. Both use exact clean guarded-published commit `a8c194ff116594b20c83eb8b10a6218bbb70d4f7`, package/DLL SHA-256 `cada85a521750a08c81e1c05428ac3c6ad053c97a40171dddabd8eb82960fdbc` / `c46c6bd85897a7ec588a39c1976fa5cea7e82ed8412fc7054e01e5f7f745c62a`, and MVID `a9914c72-d33f-417b-b8e1-e350feafe41e`. Each passes all seven rows and aggregate `381/0`, reconciles 37/37 artifacts and 35/35 screenshots, has zero lag/stationary/cleanup violations, and restores exact Working/protected-save/Mods state. First-row doorway maxima are only `24.8` and `15.4` microseconds under the unchanged `2000` gate. Phase 2A technical repeatability is therefore PASS for dev.13; visual acceptance remains human-only.

The review.1 manual scenario is intentionally not an automated qualification row. It is schema-v2, exact-Working-load-once, write-never, noncombat, same-area, Default-mode, and `visualAcceptance=PENDING`. Its guarded launcher must reach exact READY evidence, then waits for the user to exit and proves combined restoration before a session-level PASS can be recorded. Fabricated acceptance is rejected by the validators. The manual scenario is eligible for live use only after exact review package publication and successful `-WhatIf`; combat remains forbidden until the user accepts that exact package.

Review.1 package `a7f4bbd57f90069e42c8aef6e3687904bd463b8e08c64f0f9ed43e45a5e7a02b` is preserved unqualified. Its first exact manual-launcher WhatIf stopped before ShouldProcess or mutation because the thin wrapper retained dynamically scoped WhatIf while the guarded child performed mandatory package reads. Review.2 captures and clears the wrapper preference before delegation and retains the child launcher's own ShouldProcess boundary; the full offline regression gate passes. Review.2 still requires an exact clean package, successful full-pin manual WhatIf, and fresh same-package presentation-suite A/B.

Review.2 closes those remaining technical requirements at exact clean guarded-published implementation commit `9686105e6ebc7531764b2f614e9d323b6d427410`, version `0.1.0-phase2a-review.2`, package/DLL SHA-256 `266b007892ebe6e09ecd09612fd6fa5e8ce63e533da719cbeebdb2e1eec81604` / `68e71ff952b8ea48aa4c77479d968680837c54b975f94ae977cc872be485b097`, and MVID `f4347f61-7a6b-43c0-8c4c-ccacb0002944`. Its full-pin manual launcher WhatIf is pure. Runs `20260815T143000Z-presentation-suite-review2-passA` and `20260815T142900Z-presentation-suite-review2-passB` each pass all seven rows at `381/0`, reconcile 35/35 unique screenshots, report zero synchronization/stationary/cleanup violations, and restore exact Working/protected-save/Mods state. Automated evidence does not confer visual acceptance; combat remains forbidden.
## Current target-life diagnostic rows

| Scenario / checkpoint | Run ID | Status | Exact claim |
|---|---|---|---|
| `mounted-rider-melee-miss-rt` native-join package A | `20260816T133000Z-rider-miss-rt-joinprobe` | `PASS` `48/0` | All native join gates; natural `1`; native `Miss`; one rider attack/roll; zero damage/duplicates/repaths; rider-only Standard; exact cleanup/restoration. Counts only as package A. |
| `mounted-rider-melee-miss-rt` native-join package B | `20260816T134500Z-rider-miss-rt-passB` | `FAIL` `20/1` — uncredited | Every retained gate except target consciousness passes; no combat/click/attack. Schema v12/v13 life-state diagnostic required. External restoration exact. |
| `mounted-rider-melee-miss-rt` life-state diagnostic | `20260816T140000Z-rider-miss-rt-lifeprobe` | `FAIL` `20/1` - uncredited | Exact Conscious/0-damage creation and activation, then damage 15 and one native Conscious-to-Dead transition before combat. Independent external restoration exact. |
| `mounted-rider-melee-miss-rt` incoming-rule diagnostic A | `20260816T143000Z-rider-miss-rt-ruleprobe` | `PASS` `49/0` | Zero pre-dispatch attack/damage; one rider natural-1 `DodgeAC` miss; zero damage; rider-only cost; stable target; exact cleanup/restoration. Diagnostic package A only. |
| `mounted-rider-melee-miss-rt` incoming-rule diagnostic B | `20260816T150000Z-rider-miss-rt-ruleprobeB` | `FAIL` `20/1` - uncredited | One third player-faction actor attacks before rider dispatch and deals 17 real non-DOT damage; target dies; no intended click. Exact cleanup/restoration. |
| `mounted-rider-melee-miss-rt` actor-context diagnostic A/B | `20260816T151500Z-rider-miss-rt-contextprobe`, `20260816T153000Z-rider-miss-rt-contextprobeB` | `PASS` `49/0` each | Same package; one exact rider natural-1 miss, zero damage, stable target, rider-only cost, exact cleanup/restoration. Diagnostic evidence only; not yet repaired-package qualification. |
| `mounted-rider-melee-miss-rt` actor-context diagnostic C | `20260816T154500Z-rider-miss-rt-contextprobeC` | `FAIL` `20/1` - uncredited | Two pre-dispatch attacks by the exact same-group/player-party/directly-controllable/raw-and-effective-AI-enabled non-pair actor; total damage `45`, target Dead; exact cleanup/restoration. |
| `mounted-rider-melee-miss-rt` exact non-pair AI lease A/B | `20260816T152500Z-rider-miss-rt-ailease-passA`, `20260816T154000Z-rider-miss-rt-ailease-passB` | `PASS` `49/0` each | Same clean commit/package; exact one-member command-pure raw/effective AI lease and restoration; zero pre-dispatch interference; one rider natural-1 native miss; zero damage/duplicates/repaths; rider-only Standard; exact cleanup and independent external audit. Stationary miss RT qualified A/B. |

## Mammoth-primary qualification rows

| Scenario | Status | Required claim |
|---|---|---|
| `mounted-mammoth-primary-hit-rt` | PASS A/B — `20260816T194000Z-mammoth-primary-rt-passA`, `20260816T194500Z-mammoth-primary-rt-passB`, schema v26 `54/0` each | Exact Mammoth actor/initiative/command/resource/primary-natural weapon/target/rule identity; one hit and bounded damage; rider unchanged; continuous target-only brain lease; no duplicate, movement, repath, AI interference, pose loss, or cleanup/restoration residue. |
| `mounted-mammoth-primary-hit-tb` | PASS A/B — `20260816T195000Z-mammoth-primary-tb-passA`, `20260816T195500Z-mammoth-primary-tb-passB`, schema v27 `59/0` each | Same actor-specific proof and continuous target-only brain lease on the exact native Mammoth turn; turn admitted only while explicitly armed and ended after the command without Move/Swift manufacture. |
| `mounted-mammoth-primary-hit-rt` target-placement attempt | `20260816T164500Z-mammoth-primary-rt-passA` - uncredited `FAIL` `33/1` | Exact radius/distance `3.589406 / 3.06238842` failed the retained near-boundary contract before click. Zero action evidence; restoration exact. Target-only current-radius refresh and regression are offline-green; restart A/B on a new clean package. |
| `mounted-mammoth-primary-hit-rt` terminal-durability attempt | `20260816T170000Z-mammoth-primary-rt-passA` - uncredited `FAIL` `48/3` | Repaired placement; exact one Mammoth natural critical hit for 29 damage; Mammoth-only Standard cost; rider unchanged; zero duplicate/AoO/charge/repath; healthy pose and exact restoration. The one-HP target became FinallyDead before terminal animation, native teardown cleared late weapon identity, and death made the old combined stationarity check ambiguous. Schema-v22 repair uses an exact-restored Mammoth-only 128-temporary-HP lease, retained weapon identity, outcome life capture, and three displacement bounds. Restart A/B on a new clean package. |
| `mounted-mammoth-primary-hit-rt` target-stationarity attempt | `20260816T174500Z-mammoth-primary-rt-passA` - uncredited `FAIL` `50/2` | Durability, terminal weapon, exact Mammoth attack/resource/rule identity, pair stationarity, pose, and restoration all pass. Target-only displacement `0.675754547` and deferred domain cleanup confirmation fail. Exact target-agent Stop plus pre-click zero-path/speed/velocity gates and zero-residue cleanup retry are offline-green at `21/204/17/174/240`; restart RT A/B on a new clean package. |
| `mounted-mammoth-primary-hit-rt` finished-command attempt | `20260816T181500Z-mammoth-primary-rt-passA` - uncredited `FAIL` `30/1` | Stops before click: `InterruptAll` leaves an already-finished target command slot for the stock removal sweep. Exact target-only `RemoveFinishedAndUpdateQueue` now runs after queue clearing and before Stop/empty proof; cleanup/restoration pass. Restart RT A/B on a new clean package. |
| `mounted-mammoth-primary-hit-rt` hostile-brain attempt | `20260816T183000Z-mammoth-primary-rt-passA` - uncredited `FAIL` `51/1` | Exact Mammoth attack/rule/resource/weapon/life/pose/cleanup/restoration path passes; only target displacement `0.6614979` fails. Raw AI cannot disable effective AI on a non-direct target; exact `AiBrainController.TickBrain` uses `IsBrainActive`. Schema-v24/v25 target-only reversible brain lease is offline-green at `21/204/17/175/245`; restart RT A/B on a new clean package. |
| `mounted-mammoth-primary-hit-rt` actor-readiness attempt | `20260816T190500Z-mammoth-primary-rt-passA` - uncredited final `FAIL` `0/1`; game/combat `PASS` `53/0` | Entire Mammoth action, all three stationary bounds, target-brain lease, pose, cleanup, and restoration pass. External validation incorrectly substitutes rider initiative `4.99591351` for the ready Mammoth actor. Schema-v26/v27 exact action-actor readiness is offline-green at `21/206/17/176/247`; v1-v25 remain unchanged; restart RT/TB A/B on a new clean package. |

## Rider movement-to-attack qualification rows

| Scenario | Status | Required claim |
|---|---|---|
| `mounted-rider-melee-move-to-attack-rt` | IN PROGRESS - schema v30 repair offline `21/210/17/180/250` PASS; first schema-v28 attempt `20260816T210000Z-rider-move-attack-rt-passA` is immutable uncredited `FAIL` `45/10`; fresh same-package A/B required | Exact rider wrapper coexists with one exact Mammoth Move-slot command and an empty Mammoth queue; stock Mammoth controller is the only RT command driver/pathfinder; pair closes from radius + `2.0`; the slot succeeds and restores before exactly one rider attack consumes rider Standard only; neither Move ledger changes; zero replacement/repath/duplicate/pose/selection/UI/cleanup/restoration defect. |
| `mounted-rider-melee-move-to-attack-tb` | IN PROGRESS - schema v31 repair offline `21/210/17/180/250` PASS; fresh same-package A/B required | Same exact slot/queue ownership on the native rider turn; the rider-turn adapter advances the slot-owned Mammoth move, stock movement accounting advances rider Move only, Mammoth Move remains unchanged, the slot succeeds/restores, rider Standard is consumed once, and exact turn/cleanup/restoration behavior remains coherent. |
