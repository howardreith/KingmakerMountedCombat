# Phase 2 contract matrix

Status: IN PROGRESS

Authority: `planning/MOUNTED-COMBAT-PHASE-2-MASTER-MISSION.md`, SHA-256 `d4e7340954af34fb011434ec5952e893a2b109769f7941bd6e52c45f455b541e`.

Historical baseline: Phase 1 evidence commit `d5bd7fa9c434f04c6f8487b61ea49e3cf983c397`, final ledger `25 PASS / 0 attributable FAIL / 0 DEFER`, Architecture B selected. Phase 1 claims remain bounded exactly as recorded; no Phase 2 row is qualified by inheritance alone.

| Responsibility | Exact inherited contract | Phase 2 contract / narrow seam | Required evidence | Status |
|---|---|---|---|---|
| Relationship state | Runtime-only coordinator; no serializable unit part | Retain one transient rider/Mammoth pair and explicit transition guards; never intentionally serialize or remount | Component transitions plus native save/load/area/uninstall scenarios | PASS - bounded transient relationship and all pre-combat lifecycle rows qualified |
| Persistence/uninstall | Direct pre-save cleanup only; real load prefix; no save round trip | Clean dismount before save/load/area; no saved fact, unit part, or relationship; disabled/uninstalled mod leaves ordinary units | One-shot exact-Working serialization suppression, native delivery ledger, unchanged file identity, disabled-frame overlay/entity/transform residue, and exact re-enable | PASS - native save, area, mode, and disable/residue each PASS A/B |
| Player action | Diagnostic-only activation in Phase 1 | Owned `HideAndDontSave` overlay delegates to a deterministic evaluator/controller; `Mount`/`Dismount`, exact multi-reason feedback, rider selection normalization, fail-closed cleanup | Controller availability `29/0` twice; mount/dismount `44/0` twice; UI ownership/repaint `30/0` twice | PASS - controller, action, and actual mounted repaint qualified |
| Native lifecycle | Several Phase 1 handlers were invoked directly | Exact global handlers plus save/load Harmony prefixes write a bounded ordered delivery ledger with relationship before/after and cleanup result. Mode probe changes only `SettingsEntityBool.m_Cached`, invokes the registered callback, then restores cache and persisted-value identity. | Save `63/0`, area `47/0`, mode `60/0`, and disable/residue `63/0`, each twice, with stated claim limits | PASS - four bounded native rows repeatably qualified |
| Combat lifecycle/invalidation | Phase 2A combat start forced cleanup before combat was authorized | Superseding combat-alpha policy retains a valid pair at combat start/end, cancels active combat work on end, and residue-free dismounts on death, incapacitation, companion removal, view loss, save/load/area/mod-disable, or exception. Historical evidence remains immutable. | Schema-v3 direct boundary suite plus separately bounded native-delivery and active-command rows; RT/TB where behavior is mode-sensitive | IN PROGRESS - nine direct rows PASS A/B `400/0` each and active-command direct combat-end RT/TB A/B PASS `216/0`; first native rider process is uncredited `44/1`, and observation-only schema v5 must identify stock controller eligibility before repair |
| Movement authority | Mammoth is sole stock pathfinding mover; rider is logical principal | Preserve Architecture B without a second pathing agent or global movement replacement; suppress exact `ForcePlaceAboveGround` only for the active mounted rider | Six presentation rows A/B; camera A/B; movement-to-attack RT/TB A/B with exact Move-slot ownership | PASS for Phase 2A, stationary combat, and exact rider movement-to-attack; broader obstacle/control rows remain open |
| Pose/presentation | Stable root-local `Spine` position anchor; rigid upright pose unacceptable | Exact rider-view-owned `medium-humanoid-mammoth-v1` seven-bone procedural adapter, late-frame evaluated basis, retryable baseline lease; upper body unowned | Individual and aggregate A/B technical rows plus explicit human acceptance of exact review artifact | PASS - accepted Mammoth-specific foundation; known seat gap/stiffness/no-saddle limits retained |
| Selection/UI/camera | Rider selection normalization qualified; portrait/camera/action bar unproved | Rider remains principal selection, portrait/action-bar owner; exact native SelectionManager, portrait sprite, CharacterUIDecal, ActionBarManager, CameraUnitFollower observations; physical pointer review remains manual | UI and camera A/B plus human review; post-review status-feedback regression | PASS - accepted; contradictory stale feedback repaired without presentation change |
| Rider melee | Not present | Rider-owned Standard wrapper drives exactly one native rider `UnitAttack` with `IsSingleAttack=true`; Mammoth attack absent | RT/TB rule-event identity, attack/damage/action counts, controls | IN PROGRESS — stationary hit RT/TB and miss RT each have same-package fresh A/B qualification; invalid-target, target-death, cleanup-stress, and non-mounted controls remain open |
| Mount attack | Not present | Exact Mammoth-owned Standard wrapper drives one ignored-child-cooldown Mammoth primary natural single attack; rider does not attack or pay | RT/TB command/rule/weapon/target identity, attack/damage/action counts, actor-specific initiative/readiness controls | PASS — schema-v26 RT A/B `54/0` and schema-v27 TB A/B `59/0` on exact same package; schemas v1-v25 retained |
| Action economy | Absent in Kingmaker; Wrath uses paired state | Each action actor owns its wrapper and Standard cost; unarmed Mammoth TB turn is suppressed; an explicitly armed Mammoth-primary turn is bounded and ended after its command; Mammoth movement remains authoritative | Exact resources before/after, command owner, initiative/turn, cancellation, RT/TB | IN PROGRESS - stationary rider/Mammoth, movement-to-attack, and exact pre-child cancellation/interruption accounting qualified; remaining entry/exit, lifecycle, and control rows are open |
| Targeting/reach | Not present | Armed exact-pair click; Mammoth-root mechanics origin; actor weapon + Mammoth/target corpulence; no global mutation | Range boundaries, target identity, non-mounted isolation | IN PROGRESS — contract pinned, implementation/evidence pending |
| Diagnostic hostile | No Phase 1 target | Runtime-only stock Mammoth blueprint clone, project-local hostile faction, XP disabled, exact two-group combat-memory and non-pair AI leases; every row leases and continuously validates the target-only native brain gate; Mammoth-primary rows additionally lease exactly 128 temporary HP; every lease restores before deterministic destroy/drain under exact Working | Spawn/removal/faction/reward/entity/memory/AI/brain/durability residue and external restoration | PASS for stationary rider/Mammoth rows — repaired target brain/durability paths qualified in four Mammoth A/B runs; later scenarios must re-prove their own cleanup |
| Stretch reach/AoO/charge | Not authorized in Phase 1 | Only after core alpha is green; disable/defer independently if unsafe | Feature-specific RT/TB and non-mounted isolation | TODO |
| Packaging/publication | Frozen Phase 1 diagnostic only | Phase 2A aggregate-suite cold-path repair identity `0.1.0-phase2a-dev.13`; exact Kingmaker AnimationModule allowlisted; guarded non-force branch publication; no release/main merge | Clean package allowlist, hashes/MVID, local/remote equality | IN PROGRESS |

Latest combat-entry checkpoint: Probe P proves both exact leased group-memory directions and symmetric removal, but remains uncredited `FAIL` `20/1` because combat membership and rider preparation never start. Exact installed `UnitMemoryController.Tick` wakes its observer after adding memory; the direct deterministic lease omitted that native completion. The diagnostic seam now conditionally wakes only a sleeping captured endpoint, preserving every memory, timestamp, cleanup, Working-only, and non-mounted boundary. This supersedes the older Rider-melee and Diagnostic-hostile status text above for the next run only; fresh Probe Q is required and no Phase 2B row is yet qualified.

Qualified update: Probe Q and real-time Pass B each pass `47/0` from one exact clean commit/package and independently restore all external state. The Rider-melee contract is therefore PASS for stationary mounted hit in real time, including exact rider rule identity, single attack/roll/damage, rider-only Standard cost, zero duplicate pair attack, Mammoth-only movement authority, pose health, and cleanup. Turn-based rider melee and the remaining hit/miss/invalid/death/non-mounted controls remain IN PROGRESS; Mount attack remains unqualified.

Turn-based implementation update: the separate `mounted-rider-melee-hit-tb` row leases the already-qualified in-memory native mode callback, mounts only after turn-based mode is active, requires the initialized controller and exact rider/Mammoth/target roster, starts one public native rider turn, and dispatches only while `CurrentTurn.Unit` is the rider and `IsActing` is true. Schema v5 binds those facts plus round identity at dispatch/outcome and exact callback/cache/persisted-setting restoration after cleanup. Deterministic source/component/protocol and exact-token tests pass; no live TB credit is claimed before fresh clean-package A/B.

Current combat overlay: stationary rider hit is repeatably PASS A/B in both real time and turn based. Fresh sleepless-repaired miss Pass A `20260816T130000Z-rider-miss-rt-passA` passes `47/0`; same-package Pass B `20260816T131500Z-rider-miss-rt-passB` is immutable uncredited `FAIL` `20/1`. B proves both memory entries, both awake states, target-only sleepless acquisition/release, visibility, hostility, mode, time, cleanup, and restoration, but rider/Mammoth/target/player combat never begins. Exact `UnitCombatJoinController.Tick/TickUnit/ShouldEngageEnemy` tokens `0x06009360/61/62` identify additional unrecorded predicates. Observation-only schema v10/v11 binds live entity/group membership, consciousness, ignored flags, `UnitGroupMemory.Enemies`, fog, and stealth/ambush, preserving schema v1-v9 without changing behavior. One fresh diagnostic process must isolate the predicate before repair; a later fresh same-package miss A/B pair plus invalid-target, target-death, cleanup, and non-mounted controls remain mandatory.

Dev.11 camera Pass A passed `59/0`; same-package Pass B failed `28/2` on one real `1.5942225456`-unit stationary terrain snap and is preserved without credit. Dev.12 pins public parameterless `UnitEntityView.ForcePlaceAboveGround()` token `0x06001848`, suppresses it only for the exact active mounted rider, and adds a deterministic runtime interception/no-position-change probe. Local gates pass source `21/0`, Release build, component `147/0`, visual `15/0`, harness/protocol `141/0`, and assembly contracts `103/0` (`92/0` Kingmaker, `11/0` Wrath). No validator or threshold changed.

## Ordering gate

Persistence, native lifecycle, player action, presentation, UI, and camera contracts qualified before the exact manual-review build was published. The user explicitly accepted exact review commit `09a63729e0847c540ae7e79e9e3876d005ee9afe` and its pinned package. The combat ordering gate is open; stationary rider hit RT/TB, rider miss RT, and Mammoth-primary RT/TB are qualified. Every remaining combat row stays open until its own contract, implementation, and evidence qualify it.
## Current target-life diagnostic contract

| Responsibility | Status | Exact installed authority | Current seam | Runtime evidence |
|---|---|---|---|---|
| Transient diagnostic target life stability | `IN PROGRESS` | `UnitLifeController.TickOnUnit` `0x06009162` plus raw life/damage/HP/Constitution/death flags; global `RuleAttackWithWeapon` and `RuleDealDamage` observation uses exact EventBus/Rulebook marker contracts and pinned attack/damage getters. | Schema v14/v15 preserves creation/activation/transition evidence and adds exact first incoming rule, initiator, weapon/source, damage, and pre-dispatch classification. No health/stat mutation. | `20260816T140000Z-rider-miss-rt-lifeprobe` `FAIL` `20/1`: Conscious/0 damage at activation, then exact 15 damage and one Conscious-to-Dead transition. Incoming source probe next. |

Superseding diagnostic update: schema-v14 A `20260816T143000Z-rider-miss-rt-ruleprobe` passes `49/0`; B `20260816T150000Z-rider-miss-rt-ruleprobeB` is uncredited `FAIL` `20/1` and proves one third player-faction actor attacks before rider dispatch for 17 real non-DOT damage. Schema v16/v17 now observes exact group, player-party, pair-group equality, direct-control, effective/raw-AI, and command-empty context. It changes no behavior and preserves v1-v15. A fresh clean-package context probe must precede any scoped AI lease.

Mammoth-primary stationarity update: `20260816T174500Z-mammoth-primary-rt-passA` is uncredited `FAIL` `50/2` but proves every attack/resource/lease/pair-motion gate. Exact target displacement `0.675754547` comes from a residual diagnostic-target movement path: the installed target agent requires `Stop()` in addition to command interruption after translocation. The click gate now proves commands empty, stock agent enabled, no wanted/real movement, and zero speed/velocity. Deferred Unity destruction is confirmed on a later cleanup call only after every concrete residue flag is true. Offline gates pass `21/204/17/174/240`; fresh package RT/TB A/B remain mandatory.

Finished-command drain update: `20260816T181500Z-mammoth-primary-rt-passA` is uncredited `FAIL` `30/1` before click because `InterruptAll` deliberately leaves a finished command slot non-null. The narrow target-only seam now runs exact stock `RemoveFinishedAndUpdateQueue` after queue clearing and before Stop/empty proof. Noninterruptible live commands remain rejected, and all pair/resource/pathing gates are untouched.

Hostile-brain update: `20260816T183000Z-mammoth-primary-rt-passA` is uncredited `FAIL` `51/1` after proving the entire exact Mammoth action path; only the disposable target moves `0.6614979`. Installed `IsAIEnabled` remains true for a non-direct unit despite raw `m_AiEnabled=false`, while `AiBrainController.TickBrain` token `0x0600940A` selects commands through `IsBrainActive` token `0x06008319`. The target-only public-property lease captures and restores `true -> false -> true`, validates at every retained runtime boundary, and changes no pair/player/direct-control/production/pathfinding/resource/save gate. Schema v24/v25 binds it; schemas v1-v23 remain compatible. Offline gates pass `21/204/17/175/245`; fresh package RT/TB A/B remain mandatory.

Actor-readiness update: `20260816T190500Z-mammoth-primary-rt-passA` is uncredited final `FAIL` despite exact in-game/schema-v24 combat `PASS` `53/0`, zero displacement for rider/Mammoth/target, and all attack/resource/brain/pose/cleanup/restoration gates. The external validator incorrectly required rider initiative zero when the Mammoth was the actor; exact native preparation assigns each unit an independent 0-6 value, and the observed rider `4.99591351` did not consume rider resources. Schema v26/v27 binds the exact action actor's ID, preparation, eligibility, and initiative while retaining bounded rider state and v1-v25 history. Offline gates pass `21/206/17/176/247`; fresh package RT/TB A/B remain mandatory.

Mammoth-primary qualification update: clean commit/package `defbbd0516ec1ad16eac7e83dd69ae1fc41fc8c6` / `8adb516b6970b07d714dcb46e3418d2fc0fc68a92af218b6170a3427598bab2d` passes fresh RT A/B at `54/0` each and TB A/B at `59/0` each. Exact actor/readiness/command/resource/weapon/target/rule identities, one Mammoth attack, no rider duplicate/cost, native TB turn bounds, no movement/repath/AoO/charge, accepted pose, cleanup, manifests, and independent restoration all pass. The earlier attempt remains immutable uncredited history. Stationary Mammoth primary is closed; action-economy movement and cancellation remain open.
