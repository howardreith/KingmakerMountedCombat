# BLOCKED: Chunk 6A native qualification needs the owner's current installation registered as a starting payload - 2026-09-25 UTC

**This is the one open Chunk 6A blocker.** Status is IMPLEMENTATION CANDIDATE - NATIVE QUALIFICATION BLOCKED: the action contract is frozen, the implementation is built and gated offline, the runtime scenarios and the 82-id acceptance ledger exist, and every offline gate passes. That is not completion. The completion ledger stands at 0 of 82 mandatory behaviors PASS, so no part of Chunk 6A may be called complete or accepted.

**What the gate needs.** A save-backed runtime scenario requires a qualification-suite snapshot. `scripts/runtime/New-KmcQualificationSuiteSnapshot.ps1` refuses to create one and throws `Existing KMC tree differs from the exact registered starting payload.` from `scripts/runtime/QualificationSuiteContinuity.ps1:63`.

**Why.** `Assert-KmcRegisteredStartingPayload` holds a fixed registry of accepted starting installations, keyed by the installed `Info.json` hash. It has six entries; the newest is Chunk 5's starting payload preview.54 (`Info.json` `1224394f59ec598895a0d6ffd1db05527d04f334a063461a019c11f98ddbe528`, DLL `2203a68ca13dfebd1fc52be7c15521f3c2503c98cd53a891dd210ba0611019e9`). The owner's current installation is the accepted preview.105 alpha: `Info.json` `917523483b5850ac53ab8bd39ab9a34caaacfa0abfeae64b6d111fcdf7a71476`, DLL `8e231c388540cee50087ae47a2843bff06c69b6bf668b4a35f0ddfc3844f61a2`. That hash is not registered, so no pin set is selected. The installed tree also holds only **two** entries, `Info.json` and the DLL, with no UMM loader cache, because the guarded deployment replaced the DLL and the game has not been run since; every registered pin set describes a **three**-entry tree including that cache, so the entry-count comparison fails before any hash is checked.

**Why it is not taken autonomously.** Registering a new accepted starting installation is an identity-guard change, and the Chunk 6A mission explicitly withholds authority to broaden identity exceptions to make a run pass. It is also substantively the owner's decision: that registry is the record of which installations the owner has accepted as a qualification starting point, and the three preceding entries were each added under the mission that accepted them.

**What would unblock it, with no guard weakened.** Register the already-accepted preview.105 installation exactly as preview.13, preview.37 and preview.54 were registered: keyed on `Info.json` `917523483b5850ac53ab8bd39ab9a34caaacfa0abfeae64b6d111fcdf7a71476` with DLL `8e231c388540cee50087ae47a2843bff06c69b6bf668b4a35f0ddfc3844f61a2`, and with the entry-count expectation matching a two-entry tree that carries no loader cache yet. Its provenance is already recorded: guarded deployment receipt `runtime-state/deployment-operations/20260925T0200587550503Z-94de251601b24a04a1f5394f57a92f64.json`, SHA-256 `8029c975a087d9307f143bec3dcdd4b24dcf6f2074fbf38b7be9abdd58e0c92a`. Nothing else changes: the frozen candidate `0.1.0-chunk6a-preview.107` needs no rebuild, and the next step is one suite snapshot followed by `chunk6a-combat-mount-rt` and `chunk6a-combat-mount-tb`.

**Separately retained, and unresolved.** One `-WhatIf` purity proof failed after 54 minutes with `WhatIf purity failed: an external tree changed`, naming neither the root nor the entry. Measured since: no file in the four lab trees had a modification time inside the window; `runtime-state`, the live `Mods` root, `runtime-evidence` and `runtime-backups` are each byte-stable across two consecutive manifest passes; and `Test-Harness.ps1`, the whole `Test.ps1` umbrella and every persistence gate leave `runtime-evidence`, `runtime-staging` and `runtime-backups` unchanged in path and length. Which manifest differed, and why, is **not** established and is not attributed to any component. The proof now names the root and up to forty differing entries so a recurrence is locatable; the comparison itself is unchanged and still rehashes every byte of all five roots.

---
# Active Chunk 6A: legal combat Mount/Dismount - 2026-09-24 UTC

**IN PROGRESS.** The owner's Chunk 6A mission newly authorizes implementing legal voluntary combat Mount, requalifying voluntary combat Dismount through normal native controls, freezing the remaining Chunk 6 action contract, mapping the later native seams read-only, coherent commits on `codex/mounted-combat-phase3f-playable-core`, guarded publication to that integration branch, private diagnostic packaging with new identities, and bounded guarded native runtime campaigns on the existing isolated machinery. The stale statement that Chunk 6 implementation is not authorized is superseded for this bounded 6A scope only; it is preserved below as history.

This mission does **not** authorize merging to `main`, a release PR or tag, a public release, permanently replacing the owner's current preview.105 installation, writing to human campaigns or protected saves, weakening any guard or threshold, force-push, or erasing failed evidence. It does not implement mounted Charge, mounted casting or item use, staged moving actions, or the Mounted Combat feat; those are mapped only.

Verified intake for this mission: local and remote integration head `747156adfa5101579ad5f8b9c6dcbfc21c0bee30`, `origin/main` `4cc591aa7dfc0ba7fb4a6d88575bd7a9381f9f6f`, clean worktree, no unknown local work. **The current human installation is preview.105** (`Mods/KingmakerMountedCombat/KingmakerMountedCombat.dll` SHA256 `8e231c388540cee50087ae47a2843bff06c69b6bf668b4a35f0ddfc3844f61a2`), seven mod directories including the deliberate SkipIntro, no game process and no runtime transaction lock. Every native transaction restores **that** installation; the preview.54 tree under `runtime-backups/deployment-management` is a rollback backup and is never the restoration target.

Required configuration is unchanged: paired activation enabled for the supported pair, both legacy turn authorities and the diagnostic overlay off, one active pair, Horse and Mammoth profiles, C# 7.3 / .NET Framework 4.7 / Harmony12 against the exact installed Kingmaker contract (`Assembly-CSharp.dll` SHA256 `3b6450ffec440e296e586f71c711b195aed144b28d53e1cbb29406d18fef5afb`, MVID `07fa1e4d-8618-41b3-9b8d-faa17d3b26f7`).

The frozen product and action contract is [planning/CHUNK6-ACTION-CONTRACT.md](planning/CHUNK6-ACTION-CONTRACT.md), with the superseding disposition in [planning/COMBAT-MOUNT-DISMOUNT-CONTRACT.md](planning/COMBAT-MOUNT-DISMOUNT-CONTRACT.md). In one sentence: Kingmaker's native Move shell is the sole cost owner and commits at the command's acted transition, after approach and before KMC's delivery; `TurnController.Prepare` is the per-round grant and no relationship transition may call it; a pair created mid-encounter adopts the rider's already running native turn through bookkeeping alone and disposes of the partner's transition round from the exact positional observable in `ChooseNextUnit`, refusing the transition outright when that disposition cannot be resolved.

---
# Owner-approved local installation - 2026-09-25 UTC

**PASS - exact preview.105 installed for the owner's alpha test.** The owner authorized installing the accepted Chunk 5 alpha into local UMM after its merge and prerelease. The deterministic build reproduces the qualified DLL byte for byte at the documentation-only HEAD `10e32e00ab68239464ebc984bfc479e53b99e263`, so the install package was built there (ZIP SHA256 `70dac23c6ca16c4f9b0a73181418cc62031614f2eecd2e91ea5195103698fdc0`, manifest bound to that commit) with `Info.json` and `KingmakerMountedCombat.dll` byte-identical to the released package (ZIP `35b7c82808ab8ecf264be0d511f24735c070374ca73b8259e544eee0d6200113`; DLL `8e231c388540cee50087ae47a2843bff06c69b6bf668b4a35f0ddfc3844f61a2`, MVID `638259af-9d31-4738-be8a-2784135d4235`); the deployment guard's exact commit binding held without any policy change.

Guarded installation completed at 2026-09-25T02:01:03.1241803Z. DLL SHA256 `8e231c388540cee50087ae47a2843bff06c69b6bf668b4a35f0ddfc3844f61a2`; actual preview.54 DLL/cache retained under `C:/Dev/KingmakerMountedCombatLab/runtime-backups/deployment-management/20260925T0200581541614Z-cca7789be532442dbeafff8ce4697c97/KingmakerMountedCombat` (backup manifest `1ca2b37d37770646454e59b0aa0b4f36e508ff8573c77fa05d13295fb066aee5`). Protected saves (`511077a04981fe3657d47eb0fc8727f67ea5602f755575c6b4aae1142974a426`), foreign Mods and UMM Params (`b4a135f4bc05fc150abf1a4106b4f2edbfad66730583ba8443e87fe69df7fe87`) are unchanged, and the WhatIf proved all five deployment trees and Params unchanged before the replace. Kingmaker is closed; no runtime transaction remains. Current human installation is now preview.105: future runtime campaigns require a fresh actual intake and must not restore the historical preview.54 or preview.37 snapshots over it. Deployment receipt `C:/Dev/KingmakerMountedCombatLab/runtime-state/deployment-operations/20260925T0200587550503Z-94de251601b24a04a1f5394f57a92f64.json` (SHA256 `8029c975a087d9307f143bec3dcdd4b24dcf6f2074fbf38b7be9abdd58e0c92a`); guard SHA256 `c39156c053cd859316d7b13c8ab53a57ffdc16b3c813dc4c8c2b86f9d57ffd5d`.

Paired activation starts off in this unchanged developer build. Before mounting each game process, enable **Enable paired activation prototype (before mounting)** in KMC's UMM panel while dismounted and outside combat; keep both legacy authorities and the overlay off. [Setup and focused playtest](docs/ALPHA-PLAYTEST.md). Delivery receipts: `analysis-cache/chunk5-alpha105/ACTIVE.json`.

---
# Owner-approved Chunk 5 completion and alpha delivery - 2026-09-25 UTC

**ACCEPTED - Chunk 5 complete; alpha delivery of the exact preview.105 payload.** The owner accepted the Chunk 5 completion candidate on 2026-09-25 and authorized finalizing it, merging into default branch `main`, pushing, and cutting an alpha prerelease. These instructions supersede the historical no-merge/no-release limits for this delivery only. The tested source remains `471e1df92ee919c92bf42d0bc03ab6c2d2cc4114`; the qualified ZIP/DLL is unchanged (ZIP SHA256 `35b7c82808ab8ecf264be0d511f24735c070374ca73b8259e544eee0d6200113`, DLL `8e231c388540cee50087ae47a2843bff06c69b6bf668b4a35f0ddfc3844f61a2`, MVID `638259af-9d31-4738-be8a-2784135d4235`, suite `20260924-chunk5-suite122`). Local installation was not requested and was not performed: the owner's installed build remains chunk4-preview.54 (DLL `2203a68ca13dfebd1fc52be7c15521f3c2503c98cd53a891dd210ba0611019e9`). [Setup and focused playtest](docs/ALPHA-PLAYTEST.md), [qualification and ledger](docs/CHUNK5-PERSISTENCE.md).

Delivery: this documentation commit on the integration branch through the guarded push helper, a pull request merged into `main` with a merge commit (integration history preserved), then the alpha prerelease `v0.1.0-chunk5-preview.105` targeting that merge commit with the exact qualified ZIP attached and verified by download before publication. Receipts and the final merge/release identities belong in `analysis-cache/chunk5-alpha105/ACTIVE.json`. On resumption, first run `git status --short` and read that receipt before changing external state.

Paired activation starts off in this unchanged developer build. Before mounting each game process, enable **Enable paired activation prototype (before mounting)** in KMC's UMM panel while dismounted and outside combat; keep both legacy authorities and the overlay off. No new gameplay change, rebuild or Chunk 6 implementation is part of delivery.

---

# Chunk 5: every mandatory behavior PASS natively on one frozen payload; completion gate PASS — 2026-09-24 UTC

**COMPLETION CANDIDATE — every mandatory Chunk 5 behavior PASS natively on one frozen payload; completion gate PASS; not accepted by the owner, not installed, not merged, not released.** Frozen source 471e1df / 0.1.0-chunk5-preview.105, qualifier chunk5-completion. DLL 8e231c388540cee50087ae47a2843bff06c69b6bf668b4a35f0ddfc3844f61a2, MVID 638259af-9d31-4738-be8a-2784135d4235. Package SHA 35b7c82808ab8ecf264be0d511f24735c070374ca73b8259e544eee0d6200113, manifest d772d8dd, suite122 SHA 5200fee3; removal observer 0.1.0-observer.1 package 46a2f3e8 from the same commit. Offline gates source 29, components 483, contracts 180, data 56, owned fixtures 620, validation copies 118, harness 262, profile protection 53, package 11, observer package 7, all FAIL=0. Required paired=true; legacy authorities/overlay=false. Not installed, not merged, not released.

**The ledger is a completion gate now.** `docs/chunk5-ledger.json` holds one entry per mandatory behavior of the fixed list in `scripts/Test-Chunk5Ledger.ps1` (105 ids) plus the non-mandatory `P08-tb` known issue: 106 entries: PASS 105, NOT RUN 1 (`P08-tb`, non-mandatory), FAIL 0, MAPPED 0, BLOCKED 0, EXCLUDED 0. Record consistency 106/0; `-Completion`: **CHUNK5 COMPLETION GATE PASS: all 105 mandatory behaviors PASS on 0.1.0-chunk5-preview.105**. No entry is MAPPED, NOT RUN, BLOCKED or EXCLUDED among the mandatory ids; every PASS entry binds its run to this exact payload, suite, assertion counts, restoration flags and evidence bytes, each cold entry to its source run, and the no-DLL entry to the observer result and the prepare-removal run whose archive it opened.

**What changed since the preview.98 checkpoint (review "request changes").** (1) Removal safety: the assessment refuses any party member in combat by its own rule (the engine's `IsSaveAllowed` admits a manual save there and is not the rule), any outstanding mounted command, attack intent or paired activation, pending combat restoration, any save/load phase, a held world, a pending reset or a non-Default mode; the permanent-reference scan fails closed; the cleanup archive's KMC member must carry no pair, no `Combat` supplement and no control binding; every retained member is scanned for KMC-registered blueprint identities in the actual archive. (2) The cleanup save is bound to the persistence service's own per-operation completion record for exactly the requested descriptor (reference identity), never selected by name; a failed read-back, scan or hash is "unconfirmed". The installed engine never updates the requested descriptor and a first-ever save never reaches the replacement site (`SerializeAndSaveThread` IL_031C), so it records no commit: runs `final101-p07-removal` and `final102-p07-removal` found exactly that and are retained. (3) The checker's `-Completion` mode holds the fixed mandatory list and fails on anything missing, NOT RUN, BLOCKED, FAIL or MAPPED/EXCLUDED without the owner's recorded decision; the P02/P03/P04/P05 families, P06 refusals, P07 worker/area/lifecycle cases and the accepted turn-based controls each ran on this payload as their own entries. (4) The accepted `ordinary-attack-controls-tb` control ran on this payload (PASS 64/0) with its assertions unchanged, alongside `chunk4-sustained-tb` (PASS 52/0) and `mounted-mammoth-primary-hit-tb` (PASS 66/0). (5) The genuine no-DLL cleanup-save load ran natively: a separate minimal observer mod staged in KMC's place, KMC absent from Mods, loaded assemblies, UMM's list and Harmony owners, the cleanup archive opened through the engine's own main-menu path, the party moved (PASS 12/0); campaign B's archive opened cold (PASS 7/0) and the foreign native header against B's archive was refused before enumeration (PASS 33/0); the live eligibility change (rider enlarged by the engine's own effect) ended the pair by rule and persisted (PASS 22/0, cold PASS 3/0).

**Section 4 (worker lifetime, consistency, outcome)** on this payload: timeout PASS 58/0 + cold PASS 20/0, cancel-wait PASS 60/0 + cold PASS 20/0, locked replacement PASS 59/0 + cold PASS 20/0, serialization cancel PASS 52/0, cancel output PASS 51/0 + cold PASS 20/0, disable/re-enable PASS 53/0. **5A** campaign B PASS 63/0 + cold PASS 7/0. **5B** removal PASS 40/0, integration-absent PASS 9/0, no-DLL PASS 12/0, disable during load PASS 33/0. **5C** rider death PASS 26/0 + cold PASS 3/0, mount death PASS 26/0 + cold PASS 3/0, rider size change PASS 22/0 + cold PASS 3/0; area reload PASS 33/0 + cold PASS 20/0, cross-entry PASS 34/0 + cold PASS 20/0 + autosave PASS 24/0, cross-exit PASS 34/0 + cold PASS 20/0 + autosave PASS 24/0.

**Retained failures on the way to this payload** (each a payload-changing finding, none dropped): `final99-p07-removal` (real combat pauses the game; the walk unpauses before gating on Default mode), `final99-p06-foreign-header` (the isolation authority must declare the foreign-identity leaf), `final100-p07-removal` (the diagnostic enemy's memory lease is refreshed only while the encounter continues), `final101-p07-removal` (facts read from the requested descriptor the engine never updates), `final102-p07-removal` (a binding through the replacement-commit record cannot observe a first-ever save), `final103-p07-absent` (the isolation's write leases lived in the persistence controller the integration-absent case detaches, so the engine-only save was refused at the commit guard; the leases are now the isolation's own seams), `final104-p03-save-suspended` (the P03 suspended-Delay fixture left its same-round later controllable target to the initiative dice and recorded no timing facts when the precondition was missed; the target is now arranged pre-encounter like the rider and every candidate's timing is recorded), `final104-p04-load-mounted-casting` (the debt-continuity helper's fixed 1 ms slack sat below the installed clock's own millisecond rounding of each frame's delta against the float cooldown tick; the slack is now derived from that rounding, 1 ms plus a tenth of the elapsed game time, and debt may still only fall).

Every native transaction on this payload (105 runs) restored the actual intake: human chunk4-preview54 DLL 2203a68ca13dfebd1fc52be7c15521f3c2503c98cd53a891dd210ba0611019e9 installed, 275 saves, seven mod directories (SkipIntro deliberate), both automation fixtures byte-identical (BASELINE c29d965c, WORKING 5eb4e0b4), BASELINE immutable; last restoration 2026-09-25T01:08:15.9725085+00:00; no game process, no runtime lock.

**Chunk 6 judgment.** Chunk 5 is complete as an engineering candidate on this payload: sections 4, 5A, 5B, 5C and 6 each have their own native PASS entries and the completion gate PASS; the owner's acceptance (manual checklist, docs/CHUNK5-PERSISTENCE.md "Restoration and removal") is the next step. Chunk 6 (full mounted Charge, moving casting, feat expansion) is NOT started and must begin from a fresh mission after the owner's decision.

---
# Chunk 5: completion candidate qualified on one frozen payload — 2026-09-24 UTC

**COMPLETION CANDIDATE — natively qualified on one frozen payload; not accepted by the owner, not installed, not merged, not released.** Frozen source 2705a4b / 0.1.0-chunk5-preview.98, qualifier chunk5-completion. DLL 02f07ab634103d9c2224e3e5b1c49fb7973c2f7cdd94a29043bca9c05517637e, MVID 2db9c165-49b6-43ea-ba1b-53041ae48b40. Package SHA dfc05a31c70b335bd9d7390e3727a8f5cf4166e183ac988a1913fd61363a966e, manifest 6a336217, suite 115 SHA 86ee9740. Source 28 / components 463 / contracts 178 / owned fixtures 500 / harness 261 / package 11 / ledger 37 PASS, all FAIL=0. Required paired=true; legacy authorities/overlay=false. Not installed, not merged, not released.

**The ledger.** `docs/chunk5-ledger.json`, checked by `scripts/Test-Chunk5Ledger.ps1`: 37 entries: PASS 26, MAPPED 2, NOT RUN 8, EXCLUDED 1, FAIL 0, BLOCKED 0. Every PASS entry binds its run to this exact payload, suite, assertion counts, restoration flags and evidence bytes; MAPPED entries name PASS targets with their justification; NOT RUN and EXCLUDED entries carry their reason. [Report](docs/CHUNK5-PERSISTENCE.md).

**Section 4 (worker lifetime, consistency, outcome)** natively qualified on preview.89 and repeated here: `final98-p07-cancel` 52/0, `final98-p07-output` 51/0 + `final98-p07-output-cold` 20/0, `final98-p07-disable` 53/0.

**5A campaign B.** `final98-p07-campaign-b` 63/0: the engine minted a second campaign under isolated routing after a real A expenditure, B's autosave and manual save are clean by rows and bytes, and A came back under its own identity with the exact pair once.

**5B disable/removal.** Prepare-to-Disable refuses a permanent KMC Horse reference and writes a verified `KMC_CLEANUP` save through the engine (`final98-p07-removal` 34/0); the registered disable is refused at every live-load frame including presentation-pending, the equivalent-state cycle returns the same pair with clean invariants, and the pre-routine disable is judged by outcome (`final98-p07-disable-load` 33/0); the cleanup archive opens in a fresh process with KMC's integration detached before the load, nothing restored, admitted or snapshotted (`final98-p07-absent` 5/0). Integration absent is not DLL absent: the true no-DLL load is a manual checklist item.

**5C harmful boundaries.** Rider and mount death by real enemy damage under the scoped permanent-death policy (`final98-p07-rider-death` 26/0, `final98-p07-mount-death` 26/0, cold `final98-p07-rider-death-cold` 3/0, `final98-p07-mount-death-cold` 3/0): the pair ends, a new no-pair save is written over an untouched first archive, reload and cold load invent nothing while the native death persists, and the policy is restored exactly. The owner's setting "dead companions rise after combat" is ON; the preview.95/96 death runs measured that revival (retained) and are why the policy lease is used. Other boundaries are MAPPED in the ledger.

**Triaged, not dropped.** The 30 s `Phase3gControls` leaf deadline on `final86-p08-ordinary-tb` is explained from its own evidence and the installed IL (a far-side preparation move exceeding the 3 s move budget spent the standard action the fixture waited on); fixture geometry on an unmounted case, no KMC production code, not changed here. P08 turn-based stays the bounded known issue it was.

**Two payload-changing fixes found by this set** (each retained as its failing run): the integration-absent load needed the isolation's own read-only load seam once the persistence controller is unpatched (`final94-p07-absent`), and the disable-during-load remount needed the same native settle frames the disable-reenable case uses (`final94-p07-disable-load`).

Every one of the 26 native transactions on this payload, and every failed run on preview.94-97, restored the actual intake: human chunk4-preview54 DLL 2203a68ca13dfebd1fc52be7c15521f3c2503c98cd53a891dd210ba0611019e9 installed, 275 saves, seven mod directories (SkipIntro deliberate), both automation fixtures byte-identical (BASELINE c29d965c, WORKING 5eb4e0b4), BASELINE immutable; last restoration 2026-09-24T02:18:33.5458033+00:00; no game process, no runtime lock.

**Chunk 6 judgment.** Chunk 5 persistence is complete as an engineering candidate: sections 4, 5A, 5B, 5C and 6 each have native PASS evidence on one frozen payload, the residual items are named in the ledger with their reasons, and the manual checklist is the owner's next step. Chunk 6 (full mounted Charge, moving casting, feat expansion) is NOT started; it should begin only from a fresh mission after the owner's decision on this candidate. Nothing in Chunk 5 blocks it; the death-policy and integration-absent findings are inputs to its fixture design.

---
# Chunk 5: code-review remediation R1-R4/R7 qualified; R5/R6 incomplete — 2026-09-23 UTC

**IN PROGRESS — NOT completed acceptance.** Frozen source 245a340 / 0.1.0-chunk5-preview.86, qualifier chunk5-remediation. DLL 86e27fa5beb98aaba1968f614de710369bf1801787439dfdf1857cc9a03d13a5, MVID 05c5cdbf-35ce-4882-9ddc-e0e009dbf556. Package SHA a83d80d0b3cf6e9cbae97dada4b377160347c0a29a4e13de4ee87309e271593b, manifest b44fa858, suite101. Source26/components30/contracts**140**/data56/owned-fixtures321/validation-copies104/harness261/profile**48**/package11 PASS. Required paired=true; legacy authorities/overlay=false.

**Status correction.** The preview.83 report was a PARTIAL qualification checkpoint, not completed acceptance: mandatory cases were unstarted and it compared a turn-based failure against an old failed candidate. The branch is now published at 245a340 so the reviewed source resolves; the external review had only 8ea70c8, the old branch head.

**R1 worker ownership.** The double capture and boundary re-read were already present. Added: the latch is taken in a finally, and the read reports whether ownership was ESTABLISHED, so an unreadable routine defers instead of releasing. Static inspection settles the premise: the native iterator stores <saveTask>5__2 0x04008CEA exactly once at IL_0621 of MoveNext and never rewrites or nulls it, so it outlives disposal. A per-frame drain bug this introduced (releasing a null scope every quiet frame) was caught by the first native run and fixed, with a source contract pinning the ordering.

**R2 interrupted-save consistency.** The worker reads LIVE state on its own thread: Game.Instance.Player.CrossSceneState at IL_0063-006D, the live LoadedAreaState three times, and b__2 stashes the live area state. Area transfer now refuses while an owned worker can still commit, joining save, load and teardown. New serialization-cancel-output case stops at settlement so the interrupted archive survives: **final86-p07-output PASS46/0 and final86-p07-output-cold PASS20/0** load that operation's OWN committed bytes, hash d8805fdd, in a fresh process.

**R3 outcome reporting.** The commit is recorded the instant the native replacement returns, before descriptor rebinding and ownership completion. Outcome is committed / not written / unconfirmed, decided at that boundary rather than inferred from task state. Unchanged previous bytes are claimed only when a previous archive existed and is still present; a first-ever save says so instead.

**R4 turn-based.** Reclassified against the ACCEPTED Chunk 4 controls, which all match their accepted counts here: ordinary-attack-controls-tb 64/0 (HG), chunk4-sustained-tb 52/0 (GN), mounted-mammoth-primary-hit-tb 66/0 (HL). The Phase 3H fixture's own evidence shows nativeFullAttack=false, actualFullAttackRestrictedByMove=true, one planned and one completed attack with range satisfied: a disproved obsolete fixture expectation. Original FAIL retained; fixture not rebuilt; nothing forced.

**R7 profile/harness.** Byte-identical Params.xml passes without requiring a SkipIntro append. The analytics exception is shape-based, one-sided-only (creation or dispatch), keeps both-sides entries in the identity digest, and reports every admitted entry with exact path/length/hash. Four path-shape negatives plus the in-place-rewrite negative.

**NOT DONE.** R5's bounded Prepare-to-Disable/removal contract and mod-absent loading are not implemented; disable during a live load is guarded in production and covered by source contracts only, not natively qualified. R6's disposable campaign B, A->B->A isolation and the remaining P04 active TB/overlapping-effect boundaries are not done. P05 quick/auto/rotation/queued/renamed and P07 area transitions were not re-run on this payload.

**Recorded, not dropped:** one ordinary-attack-controls-tb attempt on this payload failed 60/2 on a 30-second Phase3gControls leaf deadline; the re-run passed 64/0. Unexplained intermittent timing failure.

Two runs needed harness recovery this session and both used the project's own narrow paths: Recover-KmcPersistenceProfile for two blocked profile comparisons, and Recover-KingmakerRuntimeTransaction after a scenario exceeded the 360s harness bound and left the game running. All external state restored: human chunk4-preview54 DLL 2203a68ca13dfebd1fc52be7c15521f3c2503c98cd53a891dd210ba0611019e9 installed, 275 saves, seven mod directories, both fixtures byte-identical (BASELINE c29d965c, WORKING 5eb4e0b4), no game process or lock.

---
# Chunk 5: save-worker lifetime after interruption qualified — 2026-09-23 UTC

IN PROGRESS. Frozen source 0eb6b98 / 0.1.0-chunk5-preview.70. DLL 552cc09c1f25e96b64f5a17642d34292f139c4b842492170eb754d16b47559bd, MVID a0ba85f3-0a99-453e-8e20-f324a53eff30. Package KingmakerMountedCombat-0.1.0-chunk5-preview.70-p07-worker-drain-diagnostic.zip SHAdcdf26824a89981b88046031476f1c9c9e35cdaee8eae844c85db243270637c9, suite83 SHA1b843459a1c513b9. Source22/components439/contracts**116**/data56/owned-fixtures**321**/validation-copies104/harness261/profile38/package11 PASS. Required paired=true; legacy authorities/overlay=false.

**The reported path was real.** StopAll to AbandonOwned to wrapper Dispose to ScopedEnumerator cleanup was releasing AI/control/serialization leases and the active-save scope while the archive worker could still commit, clearing the overlap guard and skipping the world-reference restoration, with no failure reported. TrackNativeSave's wait is only on the enumerated completion path; WaitForAll protects the worker's subtasks, not the outer scope's lifetime.

**Policy: refuse and drain.** Nothing in the engine can cancel a started worker — no CancellationToken exists on any persistence type and StopAll only drops enumerators — so early disposal now retains the scope, releases nothing, reports no cancellation, and marks the save draining. A per-frame drain in Update (non-blocking) releases exactly once when the task settles, restores the native world reference and reports the truthful outcome. While draining, load admission refuses before world disposal.

**20260923-chunk5-P07-worker-drain-F PASS43/0.** In-flight boundary is the operation's own: prepared leaf Manual_301_KMC_P01.zks, the scope's own Task identity, IsCompleted false, one hold at that leaf. Leases retained through StopAll; one deferral, zero drains, no cancellation callback; second serialization refused by the real overlap guard; conflicting load refused with zero world disposals; repeated cancellation safe; disable could not remove the drain owner. Settled committed, replaced **in place** 6ce1eafd to 31954ce1, failedSaves0, released exactly once, controls and world reference restored, further Update/StopAll no-ops, debt conserved, subsequent write 4df7b606, then movement, a delivered attack and usable continuation. **-cold-A PASS20/0** (PID20352) loads that subsequent save in a fresh process and plays.

**Measured limitation.** Across StopAll the engine's own suspension ends while the worker still writes (IsLoadingInProcess True to False; IsPaused false; mode Default). KMC's leases are not a substitute, and no containment of serializer-visible mutation is claimed. Observed in this modded configuration only.

Both runs restored actual intake with profileBytesRestoredExactly true and zero cache churn; final restoration 2026-09-23T14:45:35.5658948+00:00. Human chunk4-preview54 DLL2203a68ca13dfebd1fc52be7c15521f3c2503c98cd53a891dd210ba0611019e9 installed, 275 saves, seven mod directories, both automation fixtures byte-identical (BASELINE c29d965c, WORKING 5eb4e0b4), BASELINE immutable, no game process or lock.

**Next, already authorized:** remaining lifecycle and disable/re-enable/removal, P04 TB/overlap, disposable campaign B (mission section 7), and the exact-final P01-P08 consolidation. The P06 damaged-area result stands as measured containment plus restart-only recovery; no in-session recovery is claimed. Arbitrary DLL deletion remains outside the claim.

---
# Chunk 5: post-disposal load failure qualified, recovery is restart-only — 2026-09-23 UTC

IN PROGRESS. Frozen source 07a6ba4 / 0.1.0-chunk5-preview.70. DLL d923e4555158c514e14061a08f9a9307f42fb9bb722dfd85d044d93710bac098, MVID beeb7cda-592f-47e4-9cc3-219e01c38014. Package KingmakerMountedCombat-0.1.0-chunk5-preview.70-p06-failed-area-load-diagnostic.zip SHA7e3df277d504e24da770c3e30be9ae7a523e62fb2c0ef95dbf1dca7c25a17e11, suite77 SHA72319dece9b50473. Source22/components439/contracts110/data56/owned-fixtures295/**validation-copies104**/harness261/profile38/package11 PASS.

**Native load failure after the old world is destroyed.** 20260923-chunk5-P06-failed-area-load-F (PID20084) PASS26/0. A derivative corrupting exactly one native area member passes normal admission and fails only after Game.DisposeState. The measured boundary corrected a wrong prediction: the area member is read by AreaDataStash.UnstashAreaState inside **SceneLoader.LoadAreaCoroutine**, a later process than SaveManager.LoadRoutine, and LoadingProcess.Update rethrows it as LoadGameException. LoadRoutine itself completes, so **the engine reports a successful load while leaving no world** (GameModeType.None, no loaded area). KMC presents nothing into it: presentation unchanged, Unmounted, zero units, no combat fence, no held scope. Early debt restoration legitimately ran because Player.PostLoad completed, so the retained selection is inert rather than absent.

**Recovery is restart-only, and is reported as such.** Retrying the known-good archive in the same process also fails with ArgumentException: Destination scene is not valid; the Unity scene state is left invalid and every later load in that process fails. 20260923-chunk5-P01-restart-recovery-A (PID2256) PASS20/0 proves a **fresh process** loads that exact archive and plays: loaded area, Default mode, real movement, delivered attack, usable continuation.

**Still blocked:** the P06 native foreign-header campaign needs a disposable campaign B. Section 7 of the overnight mission authorizes creating one through the engine's own new-game flow; it is lower priority than the recovery queue and is timeboxed.

Both runs restored actual intake with profileBytesRestoredExactly true and zero cache churn; final restoration 2026-09-23T08:05:52.4153493+00:00. Human chunk4-preview54 DLL2203a68ca13dfebd1fc52be7c15521f3c2503c98cd53a891dd210ba0611019e9, 275 saves, seven mod directories, both fixtures byte-identical, BASELINE immutable, no game process or lock.

---

# Chunk 5 P07 cross-area: all three roles qualified — 2026-09-23 UTC

IN PROGRESS. The P07 **area** family is qualified source and cold in all three of its distinct roles and no blocker remains in it; P07 as a gate is **not** finished. Frozen source 749d6236d7936ebf56b7ed7d4742a9e9bdd48f45 / 0.1.0-chunk5-preview.69. Source22/components439/contracts110/data56/**owned-fixtures295**/validation-copies61/full-harness261/**profile38**/package11 PASS. The owned-fixture and profile rises are test-only strengthening after that payload was frozen; no `src/` file changed, so preview.69 remains the exact tested payload for every result below and is not rebuilt.

**The three roles, each on its own evidence.** Completing this family meant three separate things had to be true, and none is inherited from another. *Autosave creation and order*: preview63's after-entry barrier records resumes1 in the destination, preview64's before-exit barrier resumes0 in the departure area. *Destination Manual cold load*: state written after arrival survives a full process exit (preview63/64 -load roots, cold20/0 each). *Transition Auto cold load*: the autosave is itself a loadable world (preview69, cold24/0 each). The older "never the autosave" phrasing describes only the destination-Manual subcase — which archive those roots select — and was never a claim about the autosave's own loadability.

**Same-area** (preview58, published): rider view -339308 and mount view -340732 measured identical across a real ReloadArea, real post-area write, movement, attack and a fresh-process cold load. Installed Game.LoadArea06000CD5 passes (saveInfo != null) as UnloadEntitiesCoroutine06008096's unloadCrossScene, so ordinary transfers retain party views; preview57's FAIL9/1 was a disproved fixture premise and no production gameplay code changed.

**Cross-area** (preview63/64) at the campaign's own hub DungeonStartHub_Roguelike c49315fe499f0e5468af6f19242499a2, entered through d27d1d11f23ba9b46b6cdf00584ad08c. area-cross-entry -save-F PASS34/0 and -load-F PASS20/0; area-cross-exit -save-G PASS34/0 and -load-G PASS20/0. Both transfer 9d1278a2 to c49315fe across ~87 loading frames with one suspension and one resume, retain both native views, conserve debt and cast no Mount. The two authored autosave modes are proven distinct at their own native header barriers: AfterEntry records resumes1 in the **destination**, so restoration precedes it, while BeforeExit records resumes0 in the **departure** area and carries no destination state. Each case then makes a separate destination write that its own fresh process cold-loads, restoring the same pair with semantics2/presentation1, three facts and two slots.

**Not qualified, kept separate.** No BlueprintAreaEnterPoint targets Area_Dwarf_1, so the directed return leg cannot be performed over the public native path; delve floors are reached through the DLC's DungeonStageInitializer. The story-hub arrival route at (-25.8238926, 11.7294607, 25.18078), where native movement ended Interrupt 1.19m short after 1.97m of progress with clean probes and a settled party, remains its own open known issue. The hub retarget supports but does not prove a fixture-only explanation and must not be described as fixing it.

**Transition Auto cold load** (preview69). -entry-auto-F PID17440 PASS24/0 loads Auto_1.zks f4ef4d57aab073805559a7f80f72b199b4a606f9b02ae291e6b45d586bbd8e16 and opens destination c49315fe; -exit-auto-F PID17508 PASS24/0 loads Auto_1.zks 290d4e12f7bd700d517731c4074f222df2e50a056c0e1a4b59cf3fd7e78ab9dd and opens **departure** 9d1278a2, inheriting no destination identity, no pending transfer and no spurious resume (suspensions0/resumes0/pendingFalse). Each then makes a distinct mounted write in the world it opened, and both source archives stay byte-identical. Two native contracts had to be established rather than assumed: a transition autosave is an **Auto** save, so the cold loader must expect that type; and FindUnusedSaveNumber06008024 maxes only over the matching type, so an isolated root holding just the Auto source starts Manual numbering at 1, not 300. The generic "a cold process writes nothing" rule is therefore **scoped, not removed** — exactly these two cases may make one write, and two protections were added beside it: the loaded archive must be byte-identical afterwards and the write must be a distinct leaf.

The achievement-cache exception was rewritten after review found it too weak. It had recognized an unescaped `achievements.dat` pattern accepting the native size **or zero** while checking only path existence, so a preexisting settled cache truncated to zero passed as untouched. It is now a before/after transition rule: an existing leaf must keep its exact prior length (bytes may change, size may not, and it may not be emptied), a new leaf must arrive at exactly 12288, and every other transition throws. Accepted churn is reported in `profile-cache-changes.json` as **expected external changes** with both hashes, and `profileBytesRestoredExactly` is true only when that list is empty. Deletion, nested/unrelated/dotted lookalikes, invalid sizes, Params.xml and PlayerPrefs stay fully protected; no cache or setting snapshot is ever auto-restored to make a guard pass.

The owner's SkipIntro mod is deliberate, needed no special case, and its actual UMM registration is preserved. An earlier PlayerPrefs drift (EternalKingdom, KingdomDifficulty, Unity session counters) did **not** recur on either clean preview69 run, confirming it was an artifact of that run's abnormal deadline termination; no prefs exemption was widened.

Both preview69 processes exited and restored actual intake; final restoration 2026-09-23T05:58:01.4574559+00:00, each with profileBytesRestoredExactly true and zero accepted cache churn. Human chunk4-preview54 DLL2203a68ca13dfebd1fc52be7c15521f3c2503c98cd53a891dd210ba0611019e9 installed, seven mod directories intact, 275 saves, both automation fixtures byte-identical (BASELINE c29d965c…, WORKING 5eb4e0b4…), BASELINE immutable, no game process or transaction lock. Receipts: lab analysis-cache/chunk5-persistence/ACTIVE.json, return-entry-research63.md, area-transition-contract58.md, achievement-cache-contract64.txt.

Next, already authorized and needing no further permission stop: native foreign-header campaign, native failed-load and mid-serialization cancellation, disable/re-enable/removal and harmful lifecycle paths, remaining P04 TB/overlapping-effect cases, P08, and one frozen exact-final P01-P08 suite. Required paired=true; legacy authorities/overlay=false. Chunk4 engineering accepted; visual/HUD/physical-input/HUMAN PLAY pending. Full Charge remains Chunk6.

---
# Owner-approved alpha delivery - 2026-09-20 UTC

**PASS - exact preview.54 installed for the owner's alpha test.** The owner accepted Chunk 4 and authorized committing, merging into default branch `main`, publishing an alpha prerelease and installing it locally. These instructions supersede historical delivery restrictions. The tested source remains `429377d707a9976be65639e0c27954d8b4ff3717`; the qualified ZIP/DLL is unchanged. [Setup and manual checklist](docs/ALPHA-PLAYTEST.md), [qualification](docs/CHUNK4-PLAYABILITY.md).

Guarded installation completed at 2026-09-20T15:53:37.1343622Z. DLL SHA256 `2203a68ca13dfebd1fc52be7c15521f3c2503c98cd53a891dd210ba0611019e9`; actual preview.37 DLL/cache retained under `C:/Dev/KingmakerMountedCombatLab/runtime-backups/deployment-management/20260920T1553321732377Z-dff666c373544f848061522b19567b50/KingmakerMountedCombat`. Protected saves, foreign Mods and UMM Params are unchanged. Kingmaker is closed; no runtime transaction remains. Current human installation is now preview.54: future runtime campaigns require a fresh actual intake and must not restore the historical preview.37 campaign snapshot over it. The separate preview.13 backup is preserved.

Paired activation starts off in this unchanged developer build. Before mounting each game process, enable **Enable paired activation prototype (before mounting)** in KMC's UMM panel while dismounted and outside combat; keep both legacy authorities and the overlay off. Native engineering gates PASS; visual/physical-input and HUMAN PLAY checks remain TODO. No Chunk 5 implementation was begun.

Delivery receipts and final merge/release identities belong in `analysis-cache/chunk4-alpha54/ACTIVE.json`. On resumption, first run `git status --short` and read that receipt before changing external state. The existing deployment helper's documentation-only binding now recognizes exact `AGENTS.md`; actual policy tests pass 18/0, package 11/0, and a fresh WhatIf proves five deployment trees and UMM Params unchanged. All binary/source identity, clean-tree, process, exact-target, backup and foreign-mod guards remain in force.

---
# Chunk 4 native qualification - 2026-09-20 UTC

**PASS for native engineering gates; TODO for targeted visual/physical-input and HUMAN PLAY checks.** Tested source `429377d707a9976be65639e0c27954d8b4ff3717`, private `0.1.0-chunk4-preview.54`, on `codex/mounted-combat-phase3f-playable-core`. All 31 new exact-candidate roots and outer validators pass: 20 required Chunk 4 roots, fresh Horse capture, paired full-round/A05/A10 and Horse/Mammoth regression. Required paired configuration was measured. [Single report, identities and manual checklist](docs/CHUNK4-PLAYABILITY.md).

All 142 native transactions plus the separately recorded EX prelaunch attempt restored actual intake. Human preview.37, protected campaigns/BASELINE, settings/caches/foreign Mods and separate preview.13 backup are preserved. No game/transaction remains; Steam is left open. No permanent deployment, main merge or release. Full mounted Charge remains missing for Chunk 6. Chunk 5 persistence is the next roadmap step only; its implementation is not authorized by this mission.

---
# Combined actor-allocation and paired-activation milestone

Status: **PASS for the supported pre-combat pair**. [Implementation, evidence, limits and private checklist](docs/PAIRED-ACTIVATION-MILESTONE.md).

Final `0.1.0-paired-preview.37`, tested source `ec5d44e6eddc9839d273176b345f7c9701520450`: three paired activations plus refresh in both initiative arrangements; supported A01-A09; final A05 with 36 matched preparation samples; accepted A10 with 32 cases / 287 assertions; Mammoth TB with 66 assertions. All ten final native runs and their outer validators PASS. Full final checks PASS. Configuration: paired activation true; both legacy authorities and diagnostic overlay false.

The private package remains the exact tested ZIP, SHA256 `2215156d43679913ee134c563f704cee37ce564ab70dc50d7009781250ee3738`; DLL `20080fdcf83c7628611c3f6354a3e47c3065e2b25998a9c01b39a69796ed57bb`. This documentation checkpoint does not rebuild the payload. Work remains on `codex/mounted-combat-phase3f-playable-core`, preserving reviewed `45e3d276754257f4513342d5bce7626dd609d252`, Chunk 2 source `c804ba052760063f747cde83265e660916984d72` and legitimate descendants. [Frozen Chunk 2 evidence](docs/CHUNK2-ACTOR-ALLOCATIONS.md).

All 56 campaign transactions independently restored their actual preview.13 intake, protected saves and full Mods tree; the last audit was at 2026-09-08 12:12 UTC. Later read-only reconciliation finds preview.37 already installed by the separately recorded 18:29 UTC deployment, with the exact preview.13 DLL/cache retained in its backup. Current saves and several foreign-mod settings also differ from the campaign intake; preserve this newer external state. UMM parameter bytes still match, and no game or transaction lock remains. This reconciliation performs no runtime transaction or installation. [Accepted preview.13 human play](docs/CHUNK1-HUMAN-PLAY.md) retains its scope.

One pair, Horse/Mammoth, mounted before combat; rider initiative is principal and native actor costs remain separate. Mounted save restoration and cold-load debt rebinding remain later work. Cross-round Delay is visibly rejected; other unqualified transitions and new-candidate human review remain explicit in the report. These limits do not qualify a complete mounted-combat feature.
