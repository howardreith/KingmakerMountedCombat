# Autonomous blockers

Status: BLOCKED - MANUAL VISUAL ACCEPTANCE REQUIRED

Branch: `codex/mounted-combat-phase2-alpha`

## Intake disposition

Repository identity, ancestry, origin, mission hash, frozen Phase 1 artifact/archive, exact installed environment, fixture authority, protected-save continuity, runtime residue audit, inherited offline gates, and frozen-package validation are PASS. The authorized mission file is expected user input, not contamination. No critical intake blocker exists.

## Active authorization gate

The complete Phase 2A presentation/UI/persistence tranche remains technically qualified. The user-attested save continuity transition has been represented by a new append-only schema-v2 authority and the review launcher now accepts only an exact schema-v2 protected-pin-set digest plus explicit package, manifest, DLL, branch, and commit pins. Combat production is forbidden solely until the user explicitly accepts the replacement exact review build identified in the final stop report.

The current truthful stop is:

```text
BLOCKED - MANUAL VISUAL ACCEPTANCE REQUIRED
```

After explicit acceptance, resume automatically at Tranche C without seeking approval between authorized combat tranches.

## Protected external state

Exactly one Baseline and one Working exist, with zero KMC near-matches. Baseline is immutable. Only exact `KMC_AUTOMATION_WORKING` may change inside the guarded transaction. Current chained authority epoch `20260816T0115379973604Z-user-attested-quicksave-continuity`, SHA-256 `111c794baf8bd1062ef7ecf8c307f4e3badb7a519cf3e66ae1f8f2442b770701`, protects inventory digest `03a91757453a8e875e376f31f1664decb0a6e60dcf6433f2348795a90be634c9` and protected-pin-set digest `6f137d57e3a519b3a315a4c654b5feee51c8495f7104c26bfd4da097930c9ff4`. Its immutable schema-v1 parent remains byte-identical at SHA-256 `77fd415d177423dd7da58e3685e418f38cf954dead75068424a717e2be6b3ca9`. The only attested transitions are `Quick_3.zks` and `Quick_438.zks`; both remain non-writable. Any later non-Working drift, restoration failure, unexpected process/dialog, Steam/account/update/cloud prompt, or runtime transaction ambiguity is an immediate critical stop.

## Current uncertainties, not blockers

- visual acceptability of the selected deterministic `medium-humanoid-mammoth-v1` profile; all seven individual rows and complete dev.13 suite A/B are technically qualified under unchanged gates;
- doorway/edge framing, fixture geometry occlusion, native blue selection silhouette, camera feel, physical pointer behavior, and subjective rider posture remain human-review decisions;
- subjective acceptance or rejection of the exact review.2 build remains genuinely human-only; review.1 is preserved unqualified after its manual-launcher WhatIf stopped before mutation on dynamic preference leakage.

Review.2 runs `20260815T143000Z-presentation-suite-review2-passA` and `20260815T142900Z-presentation-suite-review2-passB` each pass all seven rows at `381/0`; every row has zero lag/stationary/cleanup violations and external restoration was exact at execution time. The complete post-schema-v2 gate passes source `21/0`, Release build, component `152/0`, visual/source-order `17/0`, harness/protocol `147/0`, and assembly-backed contracts `103/0`, including schema-v1 compatibility, exact two-transition chaining, metadata-only exception restrictions, replacement pins, drift/link/replacement rejection, WhatIf purity, Working-only writes, and preflight/under-lock agreement. The final stop report, not any earlier command or schema-v1-only authority arguments, is authoritative for the rebound review package.

## Exact next action

After the final clean-HEAD package is guarded-published, the user runs only the replacement fully pinned command from the final stop report, waits for `KMC MANUAL VISUAL REVIEW READY.`, completes the exact checklist without saving/loading/combat/area or mode changes, exits Kingmaker normally, and waits for terminal launcher PASS plus exact restoration. The user then explicitly accepts or rejects the exact reported review.2 package SHA-256. On acceptance, resume automatically at Tranche C after revalidating the accepted commit/package/hash and external state. Do not reuse the superseded schema-v1 command, live-run the review on the user's behalf, implement combat before acceptance, or create a public release.
